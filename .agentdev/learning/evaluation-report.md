# 評価レポート

## メタデータ
- **実行日時**: 2026-06-04 (JST)
- **対象エントリ数**: 9件（inbox: 9件, archive: 0件）
- **問題クラス数**: 3（未分類含む）

## 問題クラス一覧

### 問題クラス1: 並行PR逐次マージ時のコンフリクト・base branch更新

- **根本原因**: 同一ファイルまたは同一ベースブランチを変更する複数PRを逐次マージする際、先行PRのマージがbase branchを更新し、後続PRのsquash mergeがコンフリクトまたは「base branch was modified」エラーで失敗する
- **再発条件**: (1) 複数PRが同一ファイルの同じ箇所を変更、(2) 逐次マージ、(3) 後続PRがrebaseなしでマージを試みる
- **予防策**: (1) 共通変更（path正規化等）は独立PRとして最初にマージ、(2) マージ間に短い待機を挟む、(3) マージ失敗時はrebase + conflict resolution → force push → retryのワークフローを標準化、(4) マージ前に`git merge origin/main --no-commit`で競合確認

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 3/5 | 3件（同一ファイルコンフリクト2件 + base branch modification 1件） |
| 影響度 | 3/5 | マージ失敗→リトライ・rebaseが必要。作業中断はあるがデータ損失なし |
| 横展開性 | 4/5 | 並行PR開発全般で発生。Epicの多Wave実行で特に顕著 |
| 反映先明確度 | 4/5 | case-close Step 4のリトライロジック、agentdev-gh-cliのVERIFY操作 |
| 自動化適性 | 4/5 | リトライ・待機・rebase手順は自動化可能 |
| プロジェクト固有知識再利用性 | 4/5 | 並行開発パターンの標準化として高い再利用価値 |
| 再発可能性 | 5/5 | 並行PRの逐次マージは運用上頻出。ほぼ確実に再発 |
| 費料対効果 | 5/5 | リトライロジック追加のみで大きなリスク低減 |
| **加重合計** | **32/40** | |

- **推奨処分案**: ワークフロー組み込み — case-close Step 4のPRマージに自動リトライ（間隔5秒、最大5回）とrebase手順を追加。並行Wave実行ガイドに共通変更の独立PR化を推奨

#### エントリ一覧
- 同一ファイルを変更する兄弟PRの逐次マージ時のコンフリクト対処 [inbox]
- 2026-06-02: 並行PRでの同一正規化変更によるdocs競合の反復発生 [inbox]
- 2026-06-04: Epic子Issueの逐次squash merge時のbase branch modification失敗 [inbox]

### 問題クラス2: worktree削除時のプロセスロック・tracked file干渉

- **根本原因**: worktree内のファイルがLSP/エディタのプロセスロック、または.sisyphus/内のtracked fileがcommitされていることで、`git worktree remove`が失敗する
- **再発条件**: (1) LSPがworktree内ファイルを監視中、(2) .sisyphus/内のtracked fileがcommitされている、(3) `git worktree remove`を実行
- **予防策**: (1) .sisyphus/クリーンアップはuntracked filesのみ対象、(2) worktree削除前に`git checkout .`でrestorationを挟む、(3) 削除失敗時の遅延リトライ（5-10秒後再試行）

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 2/5 | 2件（tracked file干渉1件 + プロセスロック1件） |
| 影響度 | 2/5 | ワークツリーディレクトリが残るが、ブランチ削除・git管理上の除外は可能 |
| 横展開性 | 4/5 | case-close全般で発生。TypeScript/Node.jsプロジェクトで特に顕著 |
| 反映先明確度 | 4/5 | agentdev-git-worktreeのworktree削除手順が明確な反映先 |
| 自動化適性 | 3/5 | untracked-only削除は自動化可能だが、LSP停止は環境依存 |
| プロジェクト固有知識再利用性 | 4/5 | git worktreeパターンの知識として高い再利用価値 |
| 再発可能性 | 4/5 | LSP環境下でのworktree操作で高確率に発生 |
| 費料対効果 | 4/5 | クリーンアップ手順の改良で効果大 |
| **加重合計** | **27/40** | |

- **推奨処分案**: スキル化 — agentdev-git-worktreeのworktree削除手順にuntracked-only削除と遅延リトライを追加。case-closeのエラー回復手順にも反映

#### エントリ一覧
- 2026-06-02: .sisyphus/内のtracked fileがworktree削除をブロック [inbox]
- worktree ディレクトリのプロセスロックによる削除失敗 [inbox]

### 未分類

単独エントリ（同一根本原因・再発条件・予防策のエントリが2件未満のため未分類）。

#### 未分類エントリ1: Epic Orchestrator完了後の子Issue未クローズ残存パターン

- **日付**: 2026-06-03
- **8軸評価**: 1 + 3 + 4 + 4 + 4 + 4 + 4 + 4 = **28/40**
- **推奨処分案**: ワークフロー組み込み — Orchestrator完了報告に未クローズ子Issue数を明記。case-closeのEpic検出時に子Issue状態の事前チェックを追加

#### 未分類エントリ2: cross-skill 参照の false positive

- **日付**: （日付なし）
- **8軸評価**: 1 + 2 + 3 + 3 + 3 + 3 + 3 + 3 = **21/40**
- **推奨処分案**: 保留 — false positiveの影響は限定的。REQ-0108のpath解決ルール明文化で対応可能

#### 未分類エントリ3: Node.js テンプレートリテラルのクォート競合

- **日付**: 2026-06-04
- **8軸評価**: 1 + 2 + 4 + 4 + 4 + 3 + 3 + 4 = **25/40**
- **推奨処分案**: スキル化 — agentdev-gh-cliのWindows回避策セクションに「一時スクリプトではテンプレートリテラルを使わず配列joinを使用」を追加

#### 未分類エントリ4: 大規模テストファイルの修正委譲でタイムアウト

- **日付**: 2026-06-04
- **8軸評価**: 1 + 2 + 4 + 3 + 4 + 3 + 4 + 4 = **25/40**
- **推奨処分案**: 保留 — 委譲分割ガイドラインの策定で対応。頻出パターンではなく都度判断で可

## refine時prune結果

- **対象エントリ数**: 0件（archive/active.mdにエントリなし）
- **prune実施**: なし（inboxエントリ9件 < 15件の閾値のためスキップ）

## 全体傾向

- **高頻出・高影響の問題クラス**: 問題クラス1（並行PR逐次マージ時のコンフリクト）が3件で最も頻出。加重合計32/40で最優先の昇格候補
- **横展開性が高い問題クラス**: 問題クラス1と2はどちらもcase-close・Epic実行の基本パターンとして高頻度で遭遇する
- **自動化適性が高い問題クラス**: 問題クラス1（マージリトライロジック）は即座に自動化可能
- **全体的な観察所見**: 9件中6件がcase-closeの各ステップで発生。case-closeのエラー回復手順の強化が全体最適として有効。Epic Orchestratorの成熟に伴い並行PRマージの摩擦が増加傾向にある
