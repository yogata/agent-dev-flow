# 評価レポート

## メタデータ
- **実行日時**: 2026-05-29 20:00
- **対象エントリ数**: 12件（inbox: 8件, archive: 4件）
- **問題クラス数**: 6（未分類含む）

## 問題クラス一覧

### 問題クラス1: worktree 削除時の残存ファイルによる git worktree remove 失敗

- **根本原因**: case-run が worktree 内に一時ファイル（`.sisyphus/`）や未コミット変更を残した状態で case-close が worktree 削除を試みると、git worktree remove が「contains modified or untracked files」で失敗する。またIDE・シェルのプロセスロックによりディレクトリ削除も失敗することがある
- **再発条件**: case-run で worktree を使用し、case-close で worktree を削除する全ケース
- **予防策**: case-close Step 7 で worktree 削除前に `.sisyphus/` のクリーンアップを追加。または case-run 完了Step で一時ファイルをクリーンアップ。ディレクトリ削除失敗時は警告付きでスキップ

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 4/5 | 3エントリ（.sisyphus/ 残存、プロセスロック、サブエージェント未コミット） |
| 影響度 | 3/5 | worktree 削除失敗は手戻り。リカバリ可能（--force） |
| 横展開性 | 3/5 | worktree使用するプロジェクト全般。非使用なら無関係 |
| 反映先明確度 | 5/5 | case-close.md Step 7、case-run.md 完了Step と特定済み |
| 自動化適性 | 4/5 | クリーンアップ手順の追加で対応可能 |
| プロジェクト固有知識再利用性 | 3/5 | worktree運用は共通だが、.sisyphus/ は runtime 固有 |
| 再発可能性 | 4/5 | case-close実行のたびに高確率で発生 |
| 費用対効果 | 4/5 | クリーンアップStep追加のみで防止。実装コスト低 |
| **加重合計** | **30/40** | |

- **推奨処分案**: 既存 command（case-close）への反映。Step 7 に worktree 削除前のクリーンアップStepを追加。既存 command（case-run）への反映。完了Stepに一時ファイルクリーンアップを追加

#### エントリ一覧
- case-run が worktree 内に残した .sisyphus/ 一時ファイルが git worktree remove を失敗させる [inbox]
- worktree ディレクトリがプロセスロックされ削除できない場合がある [inbox]
- case-run サブエージェントがソースファイルに未コミット変更を残して終了し worktree remove が失敗する [inbox]

---

### 問題クラス2: squash merge 後の git branch -d 失敗と自マージhash不一致

- **根本原因**: squash merge はマージコミットを作成しないため git がブランチを「未マージ」と判定し `git branch -d` が失敗する。また case-close が自ら PR を squash merge した後の Step 8b で、pull 前後の HEAD hash が不一致になり停止条件に該当する（自マージによる変更を除外する考慮がない）
- **再発条件**: case-close が PR を squash merge する場合（毎回再発）
- **予防策**: Step 8b で pull 前 hash を記録し、差分が PR merge 由来のみなら継続する条件を追加。Step 7 で PR merge 済みの場合は `-D` を許可する条件を追加。agentdev-git-worktree スキルに squash merge 後の対応を明記

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 2/5 | 2エントリ（branch -d 失敗、自マージhash不一致） |
| 影響度 | 3/5 | case-closeが停止するがリカバリ可能 |
| 横展開性 | 4/5 | squash mergeを使用するプロジェクト全般 |
| 反映先明確度 | 5/5 | case-close.md Step 7/8b、agentdev-git-worktree SKILL.md と特定済み |
| 自動化適性 | 4/5 | 自マージ検出ロジックの追加で対応可能 |
| プロジェクト固有知識再利用性 | 3/5 | squash merge運用は汎用的 |
| 再発可能性 | 5/5 | case-closeがsquash mergeする毎回必ず発生 |
| 費用対効果 | 4/5 | 条件分岐の追加のみで防止。実装コスト低 |
| **加重合計** | **30/40** | |

- **推奨処分案**: 既存 command（case-close）への反映。Step 8b に自マージ検出を追加。Step 7 に squash merge 後の条件付き `-D` 許可を追加。既存 skill（agentdev-git-worktree）への反映。squash merge 後のローカルブランチ削除ガイダンスを追加

#### エントリ一覧
- squash merge 後のローカルブランチ削除が -d で失敗する（-D は禁止） [inbox]
- 2026-05-27: case-close Step 8b hash check が自マージで常に不一致になる [inbox]

---

### 問題クラス3: case-close G08 完了ゲートとテスト戦略セクションの矛盾

- **根本原因**: G08完了ゲートが完了条件・テスト戦略両セクションの全 `[ ]` を検出するが、case-run でスコープ外のテスト項目を `[ ]` で残す運用や、case-open で実装PRでは達成不可能なE2Eテストを含める運用と矛盾している
- **再発条件**: テスト戦略セクションにスコープ外の `[ ]` を残したまま case-close を実行する場合
- **予防策**: case-run 完了Stepでスコープ外の `[ ]` を削除。または case-open でテスト戦略出力時に「実装PRで達成可能か」を判定基準に含める。G08の検出範囲を完了条件セクションのみに限定する

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 2/5 | 2エントリ（スコープ外 `[ ]` 検出、達成不可能E2Eテスト） |
| 影響度 | 3/5 | case-closeが停止するが、対象行削除でリカバリ可能 |
| 横展開性 | 3/5 | case-run/case-open/case-close パイプラインを使用する場合 |
| 反映先明確度 | 5/5 | case-close.md Step 2、case-open.md、case-run.md 完了Step と特定済み |
| 自動化適性 | 4/5 | スコープ外チェックボックスの自動削除で対応可能 |
| プロジェクト固有知識再利用性 | 3/5 | AgentDevFlowパイプライン固有 |
| 再発可能性 | 4/5 | スコープ外項目を残す運用が続く限り再発 |
| 費用対効果 | 4/5 | 完了Stepにクリーンアップを追加のみで防止 |
| **加重合計** | **28/40** | |

- **推奨処分案**: 既存 command（case-run）への反映。完了Stepにスコープ外 `[ ]` の削除を追加。既存 command（case-open）への反映。テスト戦略出力時に「実装PRで達成可能か」の判定を追加

#### エントリ一覧
- 完了ゲート（G08）がテスト戦略のスコープ外 `[ ]` を検出して case-close がエラー停止する [inbox]
- case-open のテスト戦略に実装PRで達成不可能なE2Eテストを含めるとcase-closeがG08でブロックされる [inbox]

---

### 問題クラス4: Node.js -e 内での3重クォート競合

- **根本原因**: Node.js `-e` の評価文字列をPowerShellのダブルクォートで囲むため、内部でシングルクォートを使うとNode.jsパーサーが文字列区切りとして扱えず、外部でダブルクォートを使うとPowerShellが展開する。JQ式のクォートとシェルのクォートが3重に競合する
- **再発条件**: `node -e "..."` 内でgh CLIの `-q` にシングルクォートを含むJQ式を渡す場合
- **予防策**: SKILL.md の execSync パターン例に `-q` で文字列クォートが必要な場合の回避策（JSON全体取得 + JSON.parse）を併記

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 1/5 | 1エントリ |
| 影響度 | 3/5 | SyntaxErrorで実行不能だが代替手段あり |
| 横展開性 | 4/5 | Node.js -e + gh CLI の組み合わせで発生 |
| 反映先明確度 | 5/5 | agentdev-gh-cli SKILL.md Section 3 と特定済み |
| 自動化適性 | 4/5 | JSON全体取得パターンの明記で防止 |
| プロジェクト固有知識再利用性 | 3/5 | Windows + Node.js 環境固有 |
| 再発可能性 | 3/5 | 代替手段が明記されれば低下 |
| 費用対効果 | 5/5 | SKILL.mdの補足追加のみで防止完了 |
| **加重合計** | **28/40** | |

- **推奨処分案**: 既存 skill（agentdev-gh-cli）への反映。Section 3 の execSync パターンに `-q` 回避策を併記

#### エントリ一覧
- Node.js -e 内で gh CLI の -q (JQ式) にシングルクォートを使うとエラーになる [inbox]

---

### 問題クラス5: issue-backlog 由来Issueの情報不足に対する壁打ち深掘りパターン

- **根本原因**: issue-backlog は構造的抽出のみを行う設計のため、元ソースに情報が少ない場合、Issueの概要がタイトルの再述に過ぎず極めて不明確になる
- **再発条件**: issue-backlog で情報量の少ないソースから Issue を抽出する場合
- **予防策**: 情報量の少ないIssueには「要壁打ち」注記を付与し、着手時に壁打ち深掘りするフローを確立

#### 8軸評価スコア

| 軸 | スコア | 判定理由 |
|---|---|---|
| 発生件数 | 1/5 | 1エントリ |
| 影響度 | 3/5 | 不明確なIssueは着手時の手戻り |
| 横展開性 | 3/5 | 自動抽出によるIssue作成全般 |
| 反映先明確度 | 3/5 | intake-open コマンドと関連 |
| 自動化適性 | 3/5 | ラベル付与は自動化可能 |
| プロジェクト固有知識再利用性 | 2/5 | AgentDevFlowパイプライン固有 |
| 再発可能性 | 3/5 | 情報量の少ないソースから抽出する限り再発 |
| 費用対効果 | 3/5 | 運用ルールの明文化で対応 |
| **加重合計** | **21/40** | |

- **推奨処分案**: 保留（出現回数が少なく、運用ルールの明文化で対応可能）

#### エントリ一覧
- 2026-05-17: issue-backlog由来Issueの不明確さへの対処方針 [archive]

---

### 未分類

以下のエントリは単独発生であり、他エントリとの根本原因の共通性が薄いため未分類とする。

#### エントリ一覧
- 2026-05-17: Markdownコマンド定義の段階的拡張 [archive]
- 2026-05-17: LLM + 正規表現ハイブリッド分析 [archive]
- 2026-05-22: skill rename 時の directory move は git mv で追跡性を保持 [archive]

## refine時prune結果

- **対象エントリ数**: 4件（archive.md）
- **prune実施**: なし（inboxエントリ数 8件 < 15件のためスキップ）

## 全体傾向
- **高頻出・高影響の問題クラス**: 問題クラス1「worktree削除時の残存ファイル」（30/40）と問題クラス2「squash merge後のbranch削除失敗」（30/40）が同率最高スコア。case-closeのworktree・ブランチ削除手順の改善が最優先
- **横展開性が高い問題クラス**: 問題クラス4「Node.js -e 内の3重クォート競合」（横展開性4/5）はWindows + Node.js環境全般に適用可能
- **自動化適性が高い問題クラス**: 問題クラス3「G08完了ゲートとテスト戦略の矛盾」（自動化適性4/5）はスコープ外チェックボックスの自動削除で対応可能。問題クラス4（自動化適性4/5）はSKILL.mdのパターン追加で防止
- **全体的な観察所見**: 12件の学びのうち9件（75%）が5つの問題クラスに集約され、3件は未分類。inbox 8件は全て case-close/case-run/case-open パイプラインの運用上の問題であり、コマンド・スキルの改善で防止可能。特に case-close Step 7（worktree・ブランチ削除）は3つの問題クラスが集中しており、Step全体の見直しが推奨される。archive 4件は前回 refine から残存しており、汎用的な設計原則を含む
