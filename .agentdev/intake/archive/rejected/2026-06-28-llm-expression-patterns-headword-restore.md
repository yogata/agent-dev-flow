# llm-expression-patterns.md 第3節 4行の head word 復元

## 観測

PR #1168（supplement empty cells in llm-expression-patterns.md section 3）は、第3節（lines 54-60）の空 backtick セルに明示的 N/A 表記を施した。本来の head word は解説の引用語も欠損しており一意に特定できないため、別 Issue での補充を推奨している。

## 今回扱わなかった理由

誤った表現で辞書を埋めると検出辞書の網羅性を損なうため、復元不能な4行（lines 54/55/59/60）は安全側に倒して N/A 表記とした。本来の表現が判明次第の補充を前提としている。

## 根拠

PR #1168 Findings:

> 2. **4 行の head word は復元不能**: lines 54/55/59/60 は解説の引用語も「」内で欠損しており、書き換え列の意味から候補を絞れるが一意に特定できない。誤った表現で辞書を埋え検出辞書の網羅性を損なうことを避けるため、明示的 N/A 表記とした。本来の表現が判明次第、別 Issue で補充推奨。

> 3. **line 57 の書き換え列に既存の冗長性**: `` `主要な〜`、`中心となる〜` または必要性説明 `` は「主要な〜 → 主要な〜」の同語変換を含む。本 PR は解説括弧欠損の補完のみ行い、書き換え列の冗長性は既存仕様として維持（スコープ外）。改善は別途検討候補。
