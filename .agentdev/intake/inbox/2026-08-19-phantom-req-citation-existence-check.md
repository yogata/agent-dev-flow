# 引用先 REQ 行の実在を検証する恒常検査が存在しない

## 観測

063 を超えるファントム REQ-010-NNN 番号の出所は git 履歴上不存在だった（REQ-028 監査起案時の暫定番号割当と推定）。同種の「未コミット草案番号の引用残存」を防ぐ検査（引用先 REQ 行の実在検証）は IR-062 等の既存ルールでは担保されていない。IR-062 等は docs/specs 側の related_req 実在性を検査対象としない。

## 今回扱わない理由

Issue 2248（OU-0031）は既存のファントム参照の処置（再配線・注記・削除）がスコープ。恒常検査の新設は対象外。

## 影響

将来の監査起案・SPEC 起案で暫定番号が未コミットのまま引用に混入しても、機械検査で検出できない。

## レビューで決めること

- 引用先 REQ 行の実在検証を新規 IR ルールとして追加するか、既存ルールの検査範囲拡張で対応するか

## 根拠

- PR 2284 本文「Findings・Capture候補」F-2（回収元: https://github.com/yogata/agent-dev-flow/pull/2284）
- 処置済みのファントム参照: PR 2284 の再配線・履歴文脈注記・削除記録
