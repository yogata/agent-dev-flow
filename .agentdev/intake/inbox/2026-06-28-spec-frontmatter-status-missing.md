# frontmatter status なし SPEC の status 付与

## 観測

PR #1188（SPEC status SSoT and draft 放置検出）で、frontmatter `status` なし SPEC の status 付与を別課題とした。本 PR では SSoT 表で `-` 表示とし、放置検出の対象外とした（IR-054 設計通り）。

## 根拠

PR #1188:

> 特になし。REQ-0154 の対象外とした「frontmatter `status` なし SPEC の status 付与」は別課題。本 PR では SSoT 表で `-` 表示とし、放置検出の対象外とした（IR-054 設計通り）。
