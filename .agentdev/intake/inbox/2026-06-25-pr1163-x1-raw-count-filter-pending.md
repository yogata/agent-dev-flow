# X-1（中黒並列）raw 213 件の許容フィルタ適用と実残存件数の確定

## 発生源

- Issue: #1162
- PR: #1163 (merged, squash e3486488)
- 発生日: 2026-06-25

## 内容

PR #1163 の inspect-docs カタログで X-1（中黒並列 `・`）を 213 件（raw）検出した。ただし `mechanical-replacement-rules.md` の許容条件（SPEC 明示例、NN 時、code block 内、YAML title）に合致するものが多く含まれる。

当カタログでは許容フィルタを適用せず raw 件数を記録した（機械判定アルゴリズム上、許容判定の一部は查読に委譲されるため）。AG-010 是正時にフィルタを適用し、許容件数を除いた実残存件数を確定する必要がある。

## 推奨対応先

AG-010（優先度順是正）での確定作業を推奨。作業候補:

- 213 件に対し `mechanical-replacement-rules.md` の許容条件（SPEC 明示例、NN 時、code block 内、YAML title）を機械的・查読的に適用し、実残存件数を確定する
- 実残存のみを X-1 是正対象とし、許容件数はカタログの注釈として残す
- X-1 は「流動的並列」と「固定複合ラベル」の境界判定が必要なため、查読工程を前提とする（mechanical-replacement-rules.md の判定アルゴリズム参照）

## 現在の追跡状態

- PR #1163 Findings セクション F-2 に記録済み
- 別 Issue 化: 未実施（本 intake が作業候補の起点。AG-010 で一括是正する想定）
