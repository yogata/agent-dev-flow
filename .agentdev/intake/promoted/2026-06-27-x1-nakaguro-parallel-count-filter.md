# X-1（中黒並列）raw 213 件の許容フィルタ適用と実残存件数の確定

## 発生源

- Issue: #1162
- PR: #1163 (merged, squash e3486488)
- 発生日: 2026-06-25

## 観測内容

PR #1163 の inspect-docs カタログで X-1（中黒並列 `・`）を 213 件（raw）検出した。ただし `mechanical-replacement-rules.md` の許容条件（SPEC 明示例、NN 時、code block 内、YAML title）に合致するものが多く含まれる。

当カタログでは許容フィルタを適用せず raw 件数を記録した（許容判定の一部は查読に委譲されるため）。

## 影響

- 実残存件数が未確定で、AG-010 是正の対象範囲が不明確
- X-1 は「流動的並列」と「固定複合ラベル」の境界判定が必要

## 課題

- 213 件に対し許容条件を機械的・查読的に適用し、実残存件数を確定する
- 実残存のみを是正対象とし、許容件数はカタログの注釈として残す

## 既存要件との関連

- `mechanical-replacement-rules.md` の X-1 許容条件・判定アルゴリズム
- 関連: X-1 表セル除外の解釈差分（algorithm SSoT vs 子Issue）の別 intake あり

## 対応方針候補

- AG-010（優先度順是正）でフィルタを適用し、実残存件数を確定して是正する。查読工程を前提とする
