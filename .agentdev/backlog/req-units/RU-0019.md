---
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-15
status: draft
depends_on: []
tentative_classification: REQ
sources:
  - .agentdev/learning/promoted/update-traceability-cross-state-qg4.md
---

# RU-0019: QG-4 traceability 完了判定の横断 durable state 前提手順（宣言ブロック確認・main 側再判定）

## Sources

- **update-traceability-cross-state-qg4（traceability 完了判定の横断 durable state 前提）**: case-close QG-4 の traceability check は、Design ヘッダの ADF-COVERS(implementation) 宣言と検証対応要否カタログという、単一 PR の差分に閉じない横断 durable state を判定対象とする。(1) design-save 工程（Design 本体へ要件反映時）で当該 Design ヘッダの既存 ADF-COVERS 宣言ブロックの更新確認が手順化されておらず、検証対応要否カタログ登録行（missing-verification は pass）の要件であっても missing-implementation fail で case-close がマージ停止する（case 2800 / PR #2804、DEL-2800-3）。(2) トレーサビリティ check の `--root` を PR HEAD worktree に向けると、ブランチ分岐後に main へ commit された検証対応要否カタログ登録（RU-0002、commit 4987ea1e）が worktree 側に存在せず、durable state 上は解消済みの対象行が unclassified と誤判定される（case 2805 Wave 1 / case 2812、PR #2812）。誤判定を本変更起因の失敗と区別する手順が未整備。ギャップ分類: fix gap / application miss（worktree-operations.md は junction 依存 checker の skip・読取専用実行を扱うが (a)(b) 未カバー。deferred pool 1783 は req-save 側のみ）。

## Source Summary

QG-4 traceability 完了判定は Design 宣言と検証対応要否カタログという横断 durable state に依存するため、保存工程の宣言確認と main 側最新状態での再判定という前提手順が必要である。判定の解釈・実行手順のみの整備であって、検査項目自体の変更を含まない。

## 統合理由

1:1。単一 learning 成果物の要件（QG-4 実行手順の整備）であり、workflow 手順という重い論点のため単独で要件化する（RU-0010 前例）。RU-0003（traceability check fail の段階解消・宣言付与層）とは対象層が異なる（実行手順・解釈 vs 宣言付与）ため依存関係は設けない。

## 要件化の方向

検証手順整備要件として要件化する:

1. design-save 工程（Design 本体へ要件反映時）に、当該 Design ヘッダの既存 ADF-COVERS 宣言ブロック更新の確認を確認対象に含める（反映先候補: .opencode/skills/agentdev-design-file-manager/ design-save 手順）
2. QG-4 / case-close の traceability check 実行手順に、worktree root 起点で unclassified 判定が出た場合の取扱を明記する: main 側 root で再実行し、カタログ登録 commit の時系列（ブランチ分岐の前後）を確認してから完了阻止を判断する（反映先候補: .opencode/skills/agentdev-quality-gates/references/qg-4-final-acceptance.md、.opencode/skills/agentdev-workflow-case-close/ QG-4 再検査）
3. 対象外: traceability check の checker 実装変更（--root の意味・検査項目の変更）、検証対応要否カタログの schema 変更、worktree で checker を skip する既定（worktree-operations.md の junction 依存 checker skip）
4. 制約: 配布 skill 本文はプロジェクト非依存の記述を維持する（具体 Issue 番号等は記載しない）。traceability check の安全側既定（カタログ不在時は全要件行を検証対応必須として扱う）を維持する

受け入れ条件（Sources より）: design-save で Design 本体へ要件を反映したケースで、当該 Design ヘッダの宣言ブロック更新が確認対象に含まれること。worktree root 起点の traceability check で unclassified 判定が出た場合に、main 側 root 再実行とカタログ登録 commit の時系列確認を行う手順が文書化されること。本手順により、カタログ登録済み要件行への missing-implementation / unclassified 誤差し戻しが防止できること。
