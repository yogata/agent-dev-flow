---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-25T08:18:02+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/skill-worktree-operational-pitfalls.md
    type: learning
---

## Sources

- **worktree 環境の運用落とし穴標準化（junction・git mv・pull block）**（学習元: L-003, L-008, L-009, L-013、PR #1036/#1099/#1128）。
  - 問題 1（junction 非伝播による偽陽性）: worktree 内で `.opencode/skills/` の junction が再作成されないため、junction 依存の整合性検査（source-projection-sync 等）が偽陽性を出す（L-003 で偽陽性27件）。
  - 問題 2（標準版スキル参照の断絶）: junction が切れた環境で `.opencode/` 経由の参照が失敗し、`src/opencode/` 直接参照が必要になる判断が都度発生（L-008）。
  - 問題 3（git mv 実行形式差異）: Windows + worktree 環境で `git -C <worktree> mv` が `fatal: renaming ... failed` で失敗し、`workdir` 指定 + 平置き `git mv` が成功する挙動差の理由が未記載（L-009）。
  - 問題 4（case-close Step 9 pull block）: 共有 main worktree で Step 1-1（重複ファイルチェック）が開始時点スナップショット検査のため、Step 9 実行までの間に並列セッションが加えた未コミット変更を検知できず pull --ff-only が拒否される（L-013）。
  - 望ましい変更: worktree 標準運用ガイド追加（SoT パス直接参照・`isInsideWorktree` 適用）、git mv workdir フォールバック手順追記、case-close Step 9 へ Step 1-1 再実行手順追加。

## Source Summary

worktree 環境固有の運用落とし穴（junction 非伝播・git mv 実行形式差異・pull block）が4件再発。`agentdev-git-worktree` references へのガードレール標準化と case-close Step 9 強化で再発を防止する方針。worktree 使用・並列セッション運用継続でほぼ確実に再発、consumer repo 配布先でも同様。

## 統合理由

1:1（特別な統合・分割不要）。単一 learning 成果物が worktree 運用ガイド4件を一括標準化するスコープとして完結。各問題は同一関心領域（worktree 環境の運用落とし穴）のため learning-promote 段階で統合済。

## 要件化の方向

req-define では以下を壁打ちする。

- `worktree-operations.md` に worktree 標準運用ガイド（src/opencode/ 直接参照・isInsideWorktree 適用）セクションを追加する。
- `git-common-procedures.md` に git mv workdir フォールバック手順、pull 失敗時フォールバックの並列セッション安全化を追記する。
- `case-close.md` Step 9 に Step 1-1 再実行手順（pull --ff-only 直前の重複ファイルチェック再実行）を追加する。
- `isInsideWorktree` 適用範囲拡張候補（checkSourceProjectionConsistency 以外の junction 依存検査）を評価対象として明記する。
- 追記内容が既存の並列実行安全ステージングプロシージャと整合する。
- 対象外: post-merge ステップ（9-11）の worktree 隔離設計変更（破壊的設計変更のため別途検討候補として記録のみ）。
- work_type 想定: enhancement（運用ガイド標準化）。推奨ラベル: enhancement, documentation。対象: git-worktree references / case-close.md Step 9。
