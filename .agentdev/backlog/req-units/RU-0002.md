---
source_type: composite
generated_by: backlog-review
generated_at: 2026-08-09
status: draft
tentative_classification: 挙動SPEC
agentdev_handoff: true
sources:
  - type: intake
    path: .agentdev/intake/promoted/2026-08-06-worktree-junction-test-fallback.md
  - type: learning
    path: .agentdev/learning/promoted/skill-worktree-structural-constraints.md
---

# RU-0002: worktree 構造的制約とテスト fallback

本 RU は AgentDevFlow 本体の改善点を扱う前工程からの引き継ぎ用 RU である（`agentdev_handoff: true`）。現在プロジェクトでは実装しない。

## Sources

### source 1: `2026-08-06-worktree-junction-test-fallback.md`（intake）

worktree junction 未設定時の構造系テストが `src/opencode/` への fallback を未実装。`commands_e2e`、`skills_structure`、`templates_structure` 等の構造系テストが worktree 環境で skip または失敗する。agentdev-git-worktree 関連、Epic #1924、PR #1933。

### source 2: `skill-worktree-structural-constraints.md`（learning）

worktree の独立 working tree に起因する構造的制約の明文化。具体的には gitignore 対象ファイルの受け渡し不可（`git add -f` が必要）、junction 依存 checker が worktree で skip される、等。agentdev-git-worktree skill references への明示を推奨。feature 分類、PR #1932。

## Source Summary

両 source とも worktree の独立 working tree に起因する構造的制約が根幹。source 1 はテスト fallback 未実装という機能欠陥、source 2 は同一制約の明文化という知見。source 1 の fallback 実装は source 2 の制約明示を前提とする（制約を理解した上で fallback を実装する）。

## 統合理由

同一根幹（worktree 環境の構造的制約）の複数成果物を N:1 統合する。制約の明示（source 2）と fallback 実装（source 1）は req-define で一括処理が自然。関連性: fallback 実装は制約明示を前提とする順序依存があり、同一 RU で取り扱うことで整合性を確保する。

## 要件化の方向

req-define は次の推奨方向を検討する:

- worktree 構造的制約の agentdev-git-worktree skill references への明文化
- 構造系テスト（`commands_e2o`、`skills_structure`、`templates_structure` 等）の worktree 環境向け fallback 実装契約の SPEC 化
- gitignore 対象ファイル受け渡し、junction 依存 checker skip 挙動の SPEC 明示
