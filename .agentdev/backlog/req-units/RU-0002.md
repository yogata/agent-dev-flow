---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-11T19:00:00+09:00
status: draft
handoff_target: agent-dev-flow
apply_in_current_project: false
sources:
  - path: .agentdev/learning/promoted/existing-skill-reflect-junction-worktree-remove-fallback.md
    type: learning
---

## Sources

### Source: Windows junction worktree 削除時のフォールバック手順追加

Windows + junction 環境で `git worktree remove` が "Not a directory" エラーで失敗する問題。`git worktree list` では既に prune 対象になっており、`git worktree prune` + ブランチ削除は正常完了するが、worktree-operations.md に junction 特有のエラーパターンとフォールバック手順が未記載。

## Source Summary

agentdev-git-worktree skill の worktree-operations.md 削除手順セクションに、Windows + junction 環境での `git worktree remove` 失敗パターン（"Not a directory"）とフォールバック手順（prune → 手動ディレクトリ削除 → ブランチ削除）の追記が必要。既存の prune 手順自体は存在するが、junction 特有の事象が明記されていない。

## 統合理由

単一 artifact につき統合・分割なし（1:1）。

## 要件化の方向

req-define では以下の方向性で要件化すること:

- **分類**: fix（既存手順のギャップ埋め）
- **対象**: `src/opencode/skills/agentdev-git-worktree/references/worktree-operations.md` の削除手順セクション
- **内容**: junction 環境での "Not a directory" エラーの発生条件とフォールバック手順を追記
- **制約**: 既存の prune 手順と整合性を保つこと。Windows 固有であることを明記
- **関連**: Issue #683, PR #687, install-consumer-opencode.ps1 の junction 使用
