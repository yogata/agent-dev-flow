---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-22T22:45:08+09:00
status: draft
agentdev_handoff: true
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/intake/promoted/2026-07-22-generate-indexes-autogen-marker.md
    type: intake
---

## Sources

### 2026-07-22-generate-indexes-autogen-marker.md (intake)

`generate_indexes.ts` が SPEC ファイル内の backtick 囲み AUTOGEN marker 記述を実 marker と誤認し、正常な AUTOGEN block を認識せず途中停止する。対象は `docs/specs/quality/spec-health-metrics.md`。説明文中の backtick 囲み marker 文字列をインラインコード文脈として判別せず、実 marker として扱うことが原因。PR #1718 で暫定対応（L26 の HTML コメント構文抽象化）済みだが、他 SPEC で同様の記述がある場合に再発する。

## Source Summary

generate_indexes.ts の AUTOGEN marker 検出ロジックが backtick 囲み（インラインコード）文脈を判別せず、説明文中の marker 文字列を実 marker として扱う。これにより索引再生成が途中停止し、REQ retire 時の AUTOGEN 再生成が自動化できない。根本解決として parser 側での backtick 囲み内 marker 無視ロジックが必要。

## 統合理由

**1:1**: 単一成果物の独立した bug 修正であり、他の SC-002 補強（RU-0001）とは対象（コード修正 vs SPEC 文面補強）が異なるため分離する。

## 要件化の方向

`generate_indexes.ts` の AUTOGEN marker 検出ロジックを改修し、backtick 囲み（インラインコード）内の marker 文字列を無視する仕組みを実装する。検出時に前後の backtick 文字をチェックし、インラインコード文脈の場合はスキップする。暫定対応（PR #1718）は残置してよい。work_type=bugfix、scale=small を想定。対象スクリプトは `repo-agentdev-integrity` skill 配下の `scripts/generate_indexes.ts`。
