---
source_type: chat
generated_by: session
generated_at: 2026-08-30T08:36:24+09:00
status: draft
sources:
  - type: chat
    path: session:2026-08-30-command-skill-prose-remediation
---

# コマンド・スキルの残存文章品質不備を全件是正する

## 背景

コマンド18ファイル、スキル50ファイルの計68ファイルについて、内容や仕様そのものは正しいものとして、文章として正しく記述されているかを一つずつ確認した。

その結果を受け、文章品質に関する横断契約、作成時の品質基準、査読時の品質基準、診断時の品質基準を追加する対応が行われた。

その後、最新 `main` を基準として同じ68ファイルを再度一つずつ確認した結果、次の状態であることを確認した。

- コマンド: 18件中18件が `fail`
- スキル: 50件中1件が `pass`、49件が `fail`
- 合計: 68件中1件が `pass`、67件が `fail`
- `blocked`: 0件

文章品質を今後保証するための横断的な規則は一部追加されている一方、既存のコマンド・スキル本文への適用はほぼ完了していない。

## 問題

既存67ファイルには、前回確認した文章品質上の不備が残存している。

主な不備は次のとおりである。

1. 完成成果物に執筆者向けのメタ指示が残っている。
2. 通常の説明文が名詞句や理由句のみで終了し、文として完結していない。
3. 日本語で自然に説明できる語が不要に英語のまま文章へ組み込まれている。
4. 「〜を正とする」「〜が正」等、規範関係を省略した表現が残っている。
5. 過剰な名詞連結によって意味の切れ目が不明瞭になっている。
6. 一文に複数の条件、例外、処置、責務が過剰に連結されている。
7. 制御文字、意図しない異言語文字、Markdown破損、コードスパン破損、参照残骸、誤字等が残っている。

特に、当初の問題提起となった `req-define` の

> 工程上の選好を肯定形の不変条件として示す:

という記述も未修正である。

また、文章品質を規定する次の4スキルには再発防止のための品質基準が追加されているが、そのスキル自身の既存文章が新しい基準を満たしていない。

- `agentdev-command-authoring`
- `agentdev-skill-authoring`
- `agentdev-doc-writing`
- `agentdev-inspect-skills`

したがって、横断契約の追加だけでは対応完了とはいえず、既存配布物への適用を完了させる必要がある。

## Source Summary

本RUの根拠は、本セッション内で合意・確認した次の内容である。

- コマンド18件とスキル50件を、一つずつ順番に文章品質の観点から確認した。
- 最初の確認では68件すべてに不備を確認した。
- 横断的な文章品質契約を追加する方針に合意し、RU化した。
- 対応後に同じ68件を再確認した。
- 再確認時点でコマンド18件すべてが `fail` であった。
- スキルは1件のみ `pass`、49件が `fail` であった。
- 再発防止規則は一部追加済みだが、既存本文への適用はほぼ未完了である。
- 残る是正は一括置換ではなく、一ファイルずつ修正、意味保持確認、文章品質判定を行う。
- 最終完了条件は68件すべてが `pass` となることである。

## 統合理由

残存不備は67ファイルに分散しているが、個別の67要件ではない。

すべて、既に定義した文章品質契約を既存配布物へ適用し切れていないことに起因する。

また、文章品質を規定する4スキル自身に不備が残っているため、これらを先に是正し、その基準を残りの配布物へ適用する必要がある。

したがって、次を一つのRequirement Unitとして扱う。

1. 文章品質基準を持つ4スキルの自己是正
2. コマンド18件の逐次是正
3. 残る通常スキルの逐次是正
4. Workflow Skillの逐次是正
5. 決定的に検査可能な破損の横断確認
6. 全68ファイルの最終逐次査読

## 要件化の方向

### 1. 文章品質基準を持つ4スキルの自己是正

次の4スキルを先行して是正する。

- `agentdev-command-authoring`
- `agentdev-skill-authoring`
- `agentdev-doc-writing`
- `agentdev-inspect-skills`

追加済みの文章品質基準を自身にも適用し、4件すべてが文章品質検査で `pass` となること。

基準を定義するファイル自身が基準違反を残した状態で、後続ファイルの是正を完了扱いにしない。

### 2. コマンド18件の逐次是正

確認対象の全18コマンドを一つずつ修正する。

各ファイルについて次を行う。

1. 前回確認済みの不備を特定する。
2. 同型の未指摘不備がないか同一ファイル内を確認する。
3. 内容を変更せず文章を是正する。
4. 修正前後で意味、責務、外部契約が変わっていないことを確認する。
5. 文章品質を再査読する。
6. `pass / fail / blocked` を記録する。
7. `pass` を確認してから次のファイルへ進む。

18件すべてが `pass` となるまでコマンド群の是正を完了扱いにしない。

### 3. 通常スキルの逐次是正

現在 `pass` の `agentdev-command-creator` を除き、Workflow Skill以外の残存不合格スキルを一つずつ是正する。

特に次を確認する。

- 未完結文
- 不要な英語混在
- 規範関係を省略した表現
- 過剰な名詞連結
- 長大な一文
- Markdown破損
- コードスパン破損
- 制御文字
- 意図しない異言語文字
- 参照残骸
- 誤字

定義済みの識別子、状態値、フィールド名、CLI、API、ファイル名等は文章品質のみを理由として変更しない。

### 4. Workflow Skillの逐次是正

Workflow Skillを一つずつ是正する。

特に、複数ファイルで共通して残る次の類型を対象とする。

- 「workflow 実装本体。」等の未完結文
- `Control Plane`
- `Resume Protocol`
- `durable state`
- `current STEP`
- `runtime artifact`
- `fan-in`
- `fan-out`
- `termination`
- `route`
- `finding`
- その他、日本語で自然に説明できるにもかかわらず説明語として英語のまま使用されている語

ただし、正式な識別子、状態名、外部契約上のリテラルは変更しない。

条件、例外、処置、責務を一文に過剰連結している箇所は、意味単位に分割する。

### 5. 決定的破損の横断検査

意味判断を必要とせず決定的に検出可能な不備について、全対象を横断確認する。

少なくとも次を対象とする。

- 制御文字
- 意図しない異言語文字
- 明らかな誤字
- Markdown強調記法の破損
- コードスパンの破損
- 壊れた参照表現
- 既知形式の参照残骸

これらについて残存0件を確認する。

### 6. 全68ファイルの最終逐次査読

残存67件の修正完了後、現在 `pass` の1件も含め、全68件を再度一つずつ査読する。

最終査読では、前回指摘事項だけでなく、横断的な文章品質基準全体を適用する。

## 主対象REQまたは変更対象候補

既存の「コマンド・スキルの文章品質横断是正と再発防止」を定めるREQへの追加または更新候補とする。

新規REQを作成するか、既存REQへ追加・更新するかは `req-define` で確定する。

主な変更対象候補は次のとおり。

### 先行対象

- `agentdev-command-authoring`
- `agentdev-skill-authoring`
- `agentdev-doc-writing`
- `agentdev-inspect-skills`

### 配布コマンド

- `src/opencode/commands/agentdev/*.md` の18ファイル

### 配布スキル

- `src/opencode/skills/agentdev-*/SKILL.md` の50ファイル

ただし `agentdev-command-creator` は現時点で `pass` であり、最終再査読のみ必須とする。

## 対象範囲

- 現在 `fail` の67ファイルの文章是正
- 現在 `pass` の1ファイルを含む全68ファイルの最終再査読
- 文章品質基準を持つ4スキルの自己是正
- 確認済み不備と同型の未指摘箇所の追加確認
- 決定的に検査可能な文字・Markdown破損の横断確認
- ファイル単位での `pass / fail / blocked` 判定
- 修正前後の意味保持確認

## 対象外

- frontmatter `description` の日本語統一
- 英語の全面禁止
- 定義済み識別子の日本語化
- 状態値の変更
- フィールド名の変更
- コマンド名の変更
- API、CLI契約の変更
- 処理順序の変更
- 状態遷移の変更
- 責務分界の変更
- 機能追加
- 無関係なリファクタリング
- 文章品質是正を理由とした仕様変更
- 一括検索置換のみでの機械的な全文修正
- 本RUで確認対象としていない `references/` 等の一括是正

## 作業仮定

- `worktree` 等、技術上の固有名称として必要な英語は保持する。
- コマンド名、Skill名、ファイル名、状態値、フィールド名、CLI/API名はリテラルとして保持する。
- 現在 `pass` の `agentdev-command-creator` は修正対象にはしないが、最終査読対象には含める。
- 文章品質の一般規範とADF固有の適用規則は、既存責務を利用し、重複する新規規則を追加しない。

## 受け入れ条件

### AC-01 基準スキルの自己適合

次の4スキルが、自身で規定する文章品質基準を満たし、すべて `pass` となること。

- `agentdev-command-authoring`
- `agentdev-skill-authoring`
- `agentdev-doc-writing`
- `agentdev-inspect-skills`

### AC-02 コマンド全件合格

コマンド18件がすべて `pass` であること。

`fail` または `blocked` が1件でも残る場合は不合格とする。

### AC-03 スキル全件合格

スキル50件がすべて `pass` であること。

現在 `pass` のファイルも最終査読で再確認する。

### AC-04 メタ指示残留なし

authoring自体が責務である箇所を除き、完成成果物に執筆者向けメタ指示が残っていないこと。

### AC-05 未完結文なし

通常の説明文に、意図しない未完結な名詞句、理由句、ラベル型表現が残っていないこと。

### AC-06 不要な英語混在なし

正式な識別子、状態値、固有名称等を除き、日本語で自然に説明できる語が不要に英語のまま本文構文へ組み込まれていないこと。

### AC-07 規範関係が一義的

「〜を正とする」「〜が正」等を含む規範関係について、定義元、参照元、所有関係のどれを意味するのかが一義的に理解できること。

必要に応じて自然な説明文へ置き換えられていること。

### AC-08 名詞連結と長文の解消

意味の切れ目が不明瞭な過剰な名詞連結がないこと。

独立して判断できる条件、例外、処置、責務を一文へ過剰連結していないこと。

### AC-09 決定的破損0件

次の残存が0件であること。

- 制御文字
- 意図しない異言語文字
- 明らかな誤字
- Markdown構文破損
- コードスパン破損
- 壊れた参照表現
- 確認済み形式の参照残骸

### AC-10 意味保持

文章修正前後で次が変更されていないこと。

- 責務
- 外部挙動
- 処理順序
- 状態遷移
- 入出力
- 安全制約
- 識別子
- API契約
- CLI契約

### AC-11 あいまいな原文の推測修正禁止

元の意味を一義的に復元できない箇所は推測で修正しないこと。

正規の定義元を確認しても解決できない場合は `blocked` として扱うこと。

### AC-12 一ファイル単位の検証

各ファイルについて、少なくとも次を確認してから次のファイルへ進むこと。

1. 修正前の指摘事項
2. 同型の追加不備
3. 修正内容
4. 意味保持
5. 文章品質再査読
6. `pass / fail / blocked`

### AC-13 最終全件査読

最終的に次を満たすこと。

- コマンド: 18 / 18 `pass`
- スキル: 50 / 50 `pass`
- 合計: 68 / 68 `pass`
- `fail`: 0
- `blocked`: 0

1件でも `fail` または `blocked` が残る場合は本RUを完了扱いにしない。

## 検証方法

検証は次の順序で行う。

1. 文章品質基準を持つ4スキルを一つずつ再査読する。
2. コマンド18件を一つずつ再査読する。
3. 通常スキルを一つずつ再査読する。
4. Workflow Skillを一つずつ再査読する。
5. 決定的破損について全対象を横断検査する。
6. 全68ファイルを最初から一つずつ最終査読する。

各ファイルおよび各受け入れ条件の結果は `pass / fail / blocked / not applicable` で記録する。

`fail` は承認済み範囲内で文章修正のみで解消できる場合に限って修正し、再検証する。

意味変更、仕様変更、責務変更等が必要な場合は是正を継続せず `blocked` とし、別途判断を求める。
