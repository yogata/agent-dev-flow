---
id: RU-0124
title: "worktree junction 系 skill scripts の main root + --root 指定実行の汎用手順整備"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-22T12:31:29Z
status: draft
agentdev_handoff: true
depends_on: []
tentative_classification: REQ
sources:
  - type: learning
    path: .agentdev/learning/promoted/measure-update-worktree-junction-skill-scripts-main-root-execution.md
---

# RU-0124: worktree junction 系 skill scripts の main root + --root 指定実行の汎用手順整備

## Sources

- learning measure-update-worktree-junction-skill-scripts-main-root-execution.md: case-close STEP-3 のトレーサビリティ独立再検査を worktree（.worktrees/3060-fix）root 起点で実行した際、agentdev-traceability の check.ts が Module not found で失敗。`.opencode/skills/agentdev-*` は junction 構造のため worktree へ未伝播。main root 側の実体から `--root <worktree root>` を明示指定して実行し 9 pass / 0 fail を確認。同種事例（2026-09-05 targeted docs guard）を含む統合クラスタ2件目。既存の正典は QG-4 final acceptance の main 側 root 再実行前提手順（読取系 check 限定・トレーサビリティ完全性ゲート限定）のみで、scripts 検査全般の汎用手順は worktree 構造的制約文書に不在。

## Source Summary

worktree で junction 系 skill scripts を実行する検査（case-run / case-close で高頻度）は構造的に worktree 内から実行不能だが、代替手順「main root 実体から `--root <worktree root>`（必要に応じて `--files` 併用）で実行する」が汎用手順として文書化されていない。scripts 実装・junction 構造・workflow 制御の変更は不要で、手順文書の追記と相互参照整備のみで解消する。

## 統合理由

単一成果物の単一論点であり、特別な統合・分割判定は不要（1:1）。

## 要件化の方向

- `.opencode/skills/agentdev-git-worktree/references/worktree-operations.md` 構造的制約節に、junction 系 skill scripts を用いる検査は「main root 実体から `--root <worktree root>` 指定で実行する」汎用手順を追記する。
- `.opencode/skills/agentdev-quality-gates/references/qg-4-final-acceptance.md` の既存 main 側 root 再実行前提手順（L389-391 相当）との相互参照を整備する。
- 制約を引き継ぐ: 本手順も「main 側 root での再実行は読取系 check の実行のみ」（書込み系操作を main root から実行しない）を遵守する。実行形態は `bun <path>` 形式（bun run は別因になり得る既知知見）。worktree 内検査結果と main root 実体からの検査結果を混在させず、`--root` で検査対象を明示する。

前工程からの引き継ぎ（upstream handoff）用 RU であり、AgentDevFlow 本体の改善を扱う。

## 受け入れ条件（素材から引き継ぎ）

- worktree-operations.md 構造的制約節に「junction 系 skill scripts の検査は main root 実体から --root 指定で実行する」手順が記載されている
- QG-4 final acceptance の既存 main 側 root 再実行手順との関係が相互参照されている
- worktree 環境で targeted docs guard・traceability check・契約テスト等の代表的検査について、main root 実体 + --root 指定での実行手順が文書化されている
