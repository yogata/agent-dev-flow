---
id: RU-0124
title: "worktree 構造的制約手順への plugins 系 junction 実体（textlint gate）適用追記"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-23T16:30:00+09:00
status: draft
work_type: docs
agentdev_handoff: true
approval_provenance: "2026-09-23T16:20:00+09:00 chat HITL 全A承認（D-1〜D-4、パス補正込み）"
depends_on: []
tentative_classification: REQ
sources:
  - type: learning
    path: .agentdev/learning/promoted/existing-countermeasure-update-worktree-textlint-gate-junction.md
---

# RU-0124: worktree 構造的制約手順への plugins 系 junction 実体（textlint gate）適用追記

## Sources

- `.agentdev/learning/promoted/existing-countermeasure-update-worktree-textlint-gate-junction.md`: PR #3074 case-run（Case #3063・Issue #3068）の worktree で textlint 最終 gate を実行しようとしたところ、worktree では `.opencode/plugins/`（agentdev-textlint-guard の gate.ts 実体）が junction 未伝播のため直接実行できず、main root 実体の gate.ts 実行 + 検査対象 root 指定で代替し完遂。worktree での junction 系実体実行不能の同族事例は 2026-09-05 以降3事例目。

## Source Summary

REQ-018-005 と `agentdev-git-worktree` worktree-operations.md L172-202「main root 実体 + --root 指定による読取系 checker 実行手順」の汎用手順は「junction 系 skill scripts」を対象とし（backlog-review 実行時に現物再確認済み）、実行例は targeted docs guard・traceability check・契約テスト（いずれも `.opencode/skills/agentdev-*` 配下 scripts）に限定される。plugins 配下の junction 実体（実体 `src/opencode/plugins/agentdev-textlint-guard/gate.ts` を投影する `.opencode/plugins/agentdev-textlint-guard/gate.ts`。実体は src 側と .opencode 投影側の両方に現物確認済み）の実行代替手順は未明文化で、gate 実行者が毎回構造的制約を自力で解決する事態が繰り返される。

## 統合理由

1:1（単一成果物を単一 RU へ対応。統合・分割なし）。他成果物と実現先・関心領域が重複しない。

## 要件化の方向

- work_type: docs 相当として req-define で要件化。変更対象は配布 skill reference の手順節追記
- 変更内容: `src/opencode/skills/agentdev-git-worktree/references/worktree-operations.md` 構造的制約節（L172-202 手順）に、plugins 配下 junction 実体（textlint gate gate.ts）への適用を追記する。main root 実体から gate.ts を起動し、検査対象 root に worktree を指定する方式を汎用手順として明記する
- REQ 判断: REQ-018（REQ-018-005）の要件行の範囲拡張（skill scripts → plugins 系 gate を含むか）要否は req-define が判断する
- 対象外: gate.ts の実装変更、textlint guard の fail-closed 挙動の変更、bun test 環境前提（node_modules junction 手順）の変更
- 制約: main root 実体からの実行は読取系の限定・環境ラベル記録・結果混在禁止など既存手順節の制約（L196-200）に従う。textlint gate の検査対象指定引数（--root 相当）の実在引数名は req-define が実装を再調査して確定する。配布 skill reference の変更は src 側原本面（src/opencode/**）経由で行う
- 決定的受け入れ条件: (1) worktree 構造的制約節に plugins 配下 junction 実体（textlint gate）の main root 実体起動 + 検査対象 root 指定の代替手順が明文化されている (2) 適用範囲（skill scripts と plugins 系 gate）が誤解なく記述されている (3) REQ-018-005 との整合（範囲拡張要否）が req-define で判断・記録されている

## 承認

- RU 構成案承認（RU 作成承認を兼ねる）: 2026-09-23T16:20:00+09:00 chat HITL 全A承認（D-1）。矛盾検出なし（STEP-6）につき単一承認で RU 生成承認として扱う
