---
source_type: mixed
generated_by: backlog-review
generated_at: 2026-06-17T00:50:11+09:00
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-17-req-define-line-count-check.md
    type: intake
  - path: .agentdev/learning/promoted/existing-skill-req-range-glob-verification.md
    type: learning
---

# req-define への定量的前提データの実証確認ステップ追加

## Sources

### intake: req-define-line-count-check

REQ-0103-116〜119（skill SKILL.md 行数是正4件）が、前提としたファイル行数データの誤りにより無効化された。Issue #786 で skill-authoring（465行）、learning-pipeline（410行）、gh-cli（296行）、req-file-manager（377行）を行数超過として是正要件を立案したが、PR #787 で実ファイル行数を再確認したところ前提データが誤りであったことが判明し、4件とも無効化された。要件定義時（req-define）に定量的前提データ（行数、ファイル数等）を実ファイルで検証するステップが存在しないことが根本原因。

対応方向の候補: req-define（要件整理）に定量的前提データの実ファイル検証ステップを追加、または req-save（要件保存）に入力値の sanity check を追加。

### learning: req-range-glob-verification

case-run で Momus review により REQ-0123 の見落としが指摘された。文書の REQ 番号レンジ表現（例: REQ-0101〜REQ-0123）を信頼し、実ファイル一覧の glob 実証確認を怠ったことが根本原因。req-define Step 3（既存REQ照合）は `agentdev-req-file-manager` の照合方法論に従うが、実ファイル一覧の glob/grep 実証確認は明示的に要求されていない。Step 4-1（関連ドキュメント更新候補抽出）は glob を探索に使うものの、番号レンジと実ファイルの一致検証は不在。ADR 番号レンジ等でも同種の問題が発生し得る。

望ましい変更: 要件定義・plan 作成ステップに「active REQ/ADR 実ファイル一覧の glob 実証確認」を必須ステップとして組み込む。

## Source Summary

両 source は「要件定義時に文書の記述・暗黙の前提を信頼し、実ファイルでの定量的検証を怠った」ことで無効要件の立案や見落としが発生したという共通パターンを共有する。行数検証（intake）と REQ 実ファイル一覧の glob 確認（learning）は、いずれも「定量的前提データの実証確認」の具体形であり、変更対象（req-define Step 3-4, agentdev-req-analysis skill）も共通する。REQ-0102「検証可能な必達要件に限定する方針」との整合も共通課題。

## 統合理由

N:1 統合。両 source は「要件定義時に実ファイルで定量的前提を検証する」同一関心領域に属し、変更対象（req-define command, agentdev-req-analysis skill）も共通する。行数検証と REQ 一覧 glob 確認は異なる検証対象だが、同一メカニズム（実証確認ステップの追加）で解決可能であり、req-define への追加という変更スコープも同一。統合により req-define の改善要件として一括して扱う。

## 要件化の方向

req-define は以下を検討する:

1. **req-define Step 3（既存REQ照合）への glob 実証確認サブステップ追加**: `glob docs/requirements/REQ-*.md` で実ファイル一覧を取得し、文書記載のレンジ（AGENTS.md 等）と照合する。乖離発見時の対応手順（文書修正 or 実ファイル確認）を明記する。ADR（`docs/adr/ADR-*.md`）でも同様の確認を副次とする。
2. **req-define への定量的前提データ検証ステップ追加**: 行数閾値超過を主張する要件に対して実ファイル行数を確認する sanity check を組み込む。ファイル数・カウント等の定量的主張も同様に実証確認の対象とする。
3. **agentdev-req-analysis SKILL.md / req-define-detailed-gates.md への追記**: REQ/ADR 実ファイル一覧確認の具体的コマンド例と、番号レンジと実ファイルの乖離発見時の対応手順を明記する。
4. **req-save への sanity check（副次候補）**: 入力値の sanity check として、行数閾値超過要件に対する実ファイル行数確認を追加するかを判断する。

work_type は maintenance を想定。既存の req-define Step 構成を大きく変えず、サブステップ追加レベルで対応する。retired REQ を含む glob と含まない glob の使い分けに AGENTS.md 優先順位を尊重する。
