---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-14T23:41:14Z
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/intake/promoted/2026-07-05-check-changed-docs-report-missing-fields.md
    type: intake
  - path: .agentdev/intake/promoted/2026-07-07-command-spec-json-output-fields-stale.md
    type: intake
---

## Sources

- **check-changed-docs-report-missing-fields（intake）**: `check_changed_docs.ts` の report オブジェクト生成部（L766 付近）で不整合。`runWorkflowChecks()` 戻り値に `docInputsCheckRequired` プロパティが存在せず report で undefined になる。`extensionsCheckRequired` は存在するが report に含まれていない可能性（PR #1477 で部分的に修正済みの可能性あり、doc_inputs_check_required は未対応の可能性）。結果として JSON 出力から2フィールドが欠落。TS-001（workflow/files_checked/failures のみ要求）には影響しないが、拡張チェック要否判定が JSON から読み取れない。
- **command-spec-json-output-fields-stale（intake）**: `docs/specs/commands/{req-save,spec-save,case-close}.md` の「JSON 出力」説明文が9フィールドのみ列挙し、`extensions_check_required` と `declared_files_check` を掲載していない。SSoT である `docs/specs/integrity/integrity-contracts.md`（TargetedDocsReport interface の 11 フィールド確定済み）と乖離。実装の健全性は保たれている（Wave 2 PR #1477 で check_changed_docs.ts が extensions_check_required を正しく含むよう修正済み）。SPEC 記載のみ陳腐化。

## Source Summary

check_changed_docs.ts の report フィールドについて、コード実装（#9）と SPEC 記載（#10）の両層で不整合がある。SSoT（integrity-contracts.md の TargetedDocsReport 11フィールド）に対し、実装は doc_inputs_check_required が欠落、SPEC は extensions_check_required / declared_files_check が未掲載。両者は同じフィールド群を対象とするが層が異なる（コード vs SPEC 記載）。

## 統合理由

2成果物は「check_changed_docs.ts report のフィールド整合性」という共通テーマで N:1 統合。コード実装と SPEC 記載は同一対象の両面であり、SSoT への一致を一括して是正すべき。

## 要件化の方向

req-define では以下を検討する方向:

1. `runWorkflowChecks()` 戻り値型に `docInputsCheckRequired` を追加し、該当判定ロジックを実装
2. report オブジェクト生成部に `extensions_check_required`, `doc_inputs_check_required` を含める（PR #1477 の対応状況を確認の上、未対応分を修正）
3. `docs/specs/commands/{req-save,spec-save,case-close}.md` の「JSON 出力」フィールド一覧へ `extensions_check_required`, `declared_files_check` を追加し SSoT と一致
4. 既存テスト（req-save/spec-save/case-close/docs-check プロファイル）で該当フィールドが欠落していないことを検証する回帰テストを追加
5. `case-run.md` の同種記載も確認・整合

route: コード実装修正 + SPEC 記載是正。REQ-0158（targeted docs guard）/ REQ-0154（SPEC status 追跡）領域。
