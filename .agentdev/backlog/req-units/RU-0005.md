---
source_type: learning
generated_by: backlog-review
generated_at: 2026-07-27T10:02:34+09:00
status: draft
tentative_classification: 挙動SPEC
agentdev_handoff: true
sources:
  - path: .agentdev/learning/promoted/existing-skill-gh-cli-title-cp932.md
    type: learning
  - path: .agentdev/learning/promoted/existing-skill-gh-cli-tempfile-lifecycle.md
    type: learning
  - path: .agentdev/learning/promoted/existing-skill-powershell-regex-matchevaluator-replace.md
    type: learning
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **gh CLI --title cp932 化け**: Windows 環境で gh CLI の `--title` / inline `--input` 引数パーサーが cp932（Windows-31J）で decode する仕様。Section 2 Step 0 のコンソールエンコーディング初期化（3行）では解消せず、Draft 6 Epic #1845 タイトルが化けた実績がある。`--body-file` は file bytes を UTF-8 として扱うため正常。title 修正は REST API PATCH（`gh api -X PATCH /repos/{owner}/{repo}/issues/{N}` + UTF-8 JSON `--input` file）で回避可能。
- **一時ファイル配置・cleanup 非一体化**: `agentdev-gh-cli` standard-procedures.md は一時ファイル配置として `$env:TEMP/agentdev/` を指定するが、Windows で `$env:TEMP` が `C:\WINDOWS\TEMP`（システム共有）へ解決し並列タスクが cp932 で同名ファイルを上書きする問題がある。cleanup は後段注記（L83/L96/L111）のみで I/O 手続きと一体化しておらず省略可能なため、サブエージェントが `.agentdev/drafts/` へ退避した際 cleanup も漏れた（case-auto run 8 draft 並列処理で23件残存）。ユーザー確定事項として「`.agentdev/tmp/` 配置、cleanup 必須」が確定済み。
- **PowerShell regex MatchEvaluator 内 -replace の罠**: case-close(#epic) QG-4 で完了条件チェックボックス7個中1個しか置換されなかった事象。`[regex]::Replace` + ScriptBlock（MatchEvaluator）内で `-replace` 演算子を使用すると全件置換が期待通り動作しない。正確なメカニズムは未特定だが ScriptBlock スコープ、置換文字列解釈、MatchEvaluator 呼び出し回数のいずれかが関与。回避策は Node.js（`String.split/join`）または PowerShell `String.Replace`（.NET メソッド、regex 非使用）。

## Source Summary

`agentdev-gh-cli` skill の Windows 環境固有問題3件。いずれも `src/opencode/skills/agentdev-gh-cli/references/standard-procedures.md` への手続き追記で対応できる共通関心。Section 2 Step 0 のコンソールエンコーディング初期化は本文 I/O で有効だが `--title` 引数 decode には影響せず、別問題として明示が必要。3件とも実運用で発現済みであり、標準手続き化で再発防止と workaround 反復を排除する。

## 統合理由

N:1 統合。3件とも同一ファイル（`src/opencode/skills/agentdev-gh-cli/references/standard-procedures.md`）への手続き追記であり、反映先スキルと文書種別が完全一致。Section 2 Step 0 との関係明示など重複する文脈説明を共有できるため、1つの RU でまとめて req-define へ渡す。

## 要件化の方向

req-define で `agentdev-gh-cli` skill へ以下を反映する spec-update + skill reference update を構築する:
- standard-procedures.md の該当セクションで Windows 環境の `--title` / inline `--input` 使用を禁止し `--body-file` / `gh api --input` を推奨
- title 修正が必要な場合の REST API PATCH 標準手続きを明記
- 一時ファイル配置を `.agentdev/tmp/`（workspace-local）へ変更、cleanup を create → gh実行 → VERIFY → cleanup の1手順ユニットに組み込み省略不可ステップ化
- 本文置換手続きへ MatchEvaluator 内 `-replace` 使用注意と回避策（Node.js / String.Replace）を追記
- 既存の backreference `$N` 対策（L31-37）と区別して記載
