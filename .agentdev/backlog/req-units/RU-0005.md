---
source_type:
  - intake
  - learning
generated_by: backlog-review
generated_at: 2026-08-15T18:35:06+09:00
status: draft
depends_on: []
tentative_classification: REQ
sources:
  - type: intake
    path: .agentdev/intake/promoted/2026-08-15-spec-candidate-full-integrity-suite-acceptance-criteria.md
  - type: learning
    path: .agentdev/learning/promoted/spec-candidate-verification-fail-origin-baseline.md
  - type: learning
    path: .agentdev/learning/promoted/spec-candidate-ir055-baseline-regeneration-contract.md
---

# 検証 fail 由来判定・受入れ基準・baseline 再生成分実行契約

## Sources

- **2026-08-15-spec-candidate-full-integrity-suite-acceptance-criteria.md（intake）**: 「full integrity suite pass」の受入れ基準が明文化されていない。OU-008a の AC-17 判定は「bun test 全 green」基準で実施したが、既知 intake 済み欠陥と環境依存欠陥が残存し運用で補完した。main repo 環境固有の fail も現れ、検証環境により fail 構成が変動することが判明した。受入れ基準（既知欠陥の扱い、環境依存の扱い、baseline 比較の要否）の品質ゲート側 SPEC または integrity 契約への明文化が求められる。
- **spec-candidate-verification-fail-origin-baseline.md（learning）**: 検証 fail の由来判定（pre-existing vs 当該変更起因）の基準 commit が不定性を持つ。PR base 相対の由来判定では同一 Epic 内の先行 Wave 起因の失敗が「元から存在」に見え、OU-008a では remediation 開始前 baseline commit（49f4db17）基準で24件が新規発生だった。worktree と main で fail 構成が一致しない問題も併存する。由来判定の baseline commit 基準と検証環境記録・fail 全件の由来分類証跡の形式規定が求められる。同主題 intake item との統合前提を明示申告している（受入れ基準の全体は intake 側が管理し、本成果物は baseline commit 基準の delta を補完する）。
- **spec-candidate-ir055-baseline-regeneration-contract.md（learning）**: IR-055 baseline（runtime reference snapshot）再生成の実行契約（いつ・どのスコープで再生成するか）が規定されていない。移設時の未登録（PR 2114）、並列 Wave の相互作用（Epic #2099 Wave 2 で delta warning 増分 +5）、docs/specs 新規参照の更新漏れ（PR #2089）の3障害が発生した。移設系 PR への再生成標準手順の組み込み、並列 Wave でのスコープ契約（Wave 境界 or 最終 merge 後）、case-close での再生成要否判定の規定が求められる。

## Source Summary

検証の完了判定（いつ pass とするか、fail の由来をどう判定するか、baseline をいつ再生成するか）の契約不在が、false-positive completion、判定揺れ、並列 Wave での delta warning を生んでいる。

## 統合理由

3件とも「検証完了判定の契約明文化」という同一関心に属する。

learning 2件（verification-fail-origin、ir055-baseline）が同主題 intake item（full-integrity-suite）との統合を明示申告している。learning 側は受入れ基準の全体枠組みを intake 側に委ね、delta（baseline commit 基準、baseline 再生成分契約）を補完する構成であり、三分割ではなく一つの協議単位として統合する。

source_type は混成（intake 1、learning 2）のため列表記 `[intake, learning]` とする。出典の正は `sources[].type` である（形式宣言、経路E review A-F2 の反映）。

## 要件化の方向

品質ゲート側 SPEC または integrity 契約へ次を明文化する。

1. 「full integrity suite pass」の受入れ基準（既知欠陥の扱い、環境依存の扱い、baseline 比較の要否）
2. 検証 fail 由来判定の基準 commit（remediation 開始前 baseline commit）。被差し戻し PR の「base から同一」表記を pre-existing 証拠として採用しない
3. 受入れ記録への検証環境明記（worktree / main、junction 伝播、node_modules 有無）と fail 全件の由来分類証跡形式
4. IR-055 baseline 再生成の実行契約（移設系 PR の標準手順、並列 Wave では Wave 境界 or 最終 merge 後のスコープ、docs/specs 変更を伴う case-close での要否判定）

baseline は ratchet（純減は健全）である性質を損なわないこと。
