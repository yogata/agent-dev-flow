---
source_type:
  - learning
generated_by: backlog-review
generated_at: 2026-09-07
status: draft
tentative_classification: 挙動Design
agentdev_handoff: true
sources:
  - .agentdev/learning/promoted/existing-measure-update-worktree-verification-dependency-premise.md
---

# RU-0005: worktree 検証の依存整備前提の完全化

AgentDevFlow 本体向け引き継ぎマーカー（`agentdev_handoff: true`）。本リポジトリは self-hosting のため通常の req/case workflow 入力として扱う。

## Sources

- `.agentdev/learning/promoted/existing-measure-update-worktree-verification-dependency-premise.md`: worktree 環境で integrity suite・契約テスト・tsc 型検証を実行するたびに依存解決失敗による環境起因 fail が反復（2026-09-01 以降で7観測: PR #2413/#2432/#2440/#2443/#2585/#2587/#2615）。既存規定（worktree-operations.md L130 付近、qg-4-final-acceptance.md「依存パッケージ前置」）は agentdev-project-extensions/scripts 1 ディレクトリのみ対象で、repo-agentdev-integrity/scripts 側の node_modules、tsc 型解決（@types/bun）、per-skill node_modules の bun test 単独実行時の扱いと junction 代替が未規定（fix gap。規定遵守後も環境依存 fail が残存）。

## Source Summary

worktree は git 管理対象のみを引き継ぐため、gitignore 対象の node_modules は依存配置の分散数だけ個別整備が必要。既存の依存前置規定は一部のみを対象としており、環境依存 fail の反復と AG-010 既知 fail 分離判定の妨害が続いている。qg-4-final-acceptance.md L244 の依存パッケージ前置も同様に1ディレクトリのみ対象であることを確認済み。

## 統合理由

1:1（単独論点。learning-promote から知識保存適否ではなく fix gap として渡された改善要求）。

## 要件化の方向

- 依存前置の対象ディレクトリ集合を `src/opencode/skills/agentdev-project-extensions/scripts` と `.opencode/skills/repo-agentdev-integrity/scripts`（worktree 実体）の両方として worktree-operations.md・qg-4-final-acceptance.md へ明示する。
- tsc 型検証を含む場合の型解決前提（当該パッケージでの bun install による `@types/bun` 等の復元）を明記する。
- bun test 単独実行の依存解決前提と、main 側 node_modules への junction 作成（検証後削除）または当該 skill ディレクトリでの bun install という代替手段を記録する。
- 環境依存 fail の分離判定は依存整備実施後の再実行後に行う手順とする。
- RU-0006（checker 実行契約）との責務境界（worktree 汎用手順 vs checker 実行契約）を影響分析で明確化すること。既存の QG-4 正規形（3 cwd 分割実行・./ prefix・環境ラベル）の実行形態契約は変更しない。
