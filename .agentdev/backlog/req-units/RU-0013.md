---
source_type: learning
generated_by: backlog-review
generated_at: 2026-07-18T15:24:32+09:00
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/learning/promoted/check-changed-docs-files-delimiter.md
    type: learning
---

## Sources

- case-close Step 3-1 で check_changed_docs.ts の --files に comma 区切り文字列（"file1,file2,file3"）を渡すと、1つのファイルパスとして解釈され fs.existsSync で除外され files_checked が空になり TARGET-EMPTY（strict severity）が発火した。
- check_changed_docs.ts の --files パーサは `while (i < args.length && !args[i].startsWith("--")) parsed.files.push(args[i])` で次トークンを順次 file として積む space 区切り仕様。case-close.md の実行コマンド例は `--files <PR 変更ファイル一覧>` と抽象表記で区切り文字が未明示。comma 区切りは仕様外。
- 影響: check_changed_docs.ts のヘルプテキスト、エラーメッセージで --files の区切り形式（space 区切り）が未明示。case-close.md Step 3-1 の実行コマンド例、SPEC `targeted-docs-guard-implementation.md` の呼出例で区切り形式が未明記。comma 区切りで渡すと1ファイル扱いになり files_checked が空になり TARGET-EMPTY strict failure が発火する。
- 根拠: inbox#4（Epic #1515 Wave 2）。case-close Step 3-1 で comma 区切りで渡し files_checked 空、TARGET-EMPTY strict failure 発火。script ソース L121-126 の --files パーサ確認で space 区切り仕様を発見。space 区切りで再実行し files_checked 3件、PASS。
- 再発条件: --files に comma 区切りで複数ファイルを渡す場合。横展開可能性: case-run、spec-save 等 --files を使用する全 workflow で発生し得る。

## Source Summary

`check_changed_docs.ts` の --files 区切り形式（space 区切り）が usage/SPEC/呼出例のいずれでも未明示。comma 区切りで渡すと1ファイル扱いとなり TARGET-EMPTY strict failure が発火する。space 区切り仕様を明示しつつ comma 受入を追加して堅牢化する必要がある。

## 統合理由

1:1。単一関心（--files 区切り形式の明示と comma 受入拡充）を扱うため統合・分割は不要。case-run、spec-save 等 --files を使用する全 workflow で横展開可能性があるため、本 RU の対象範囲を超えて関連 workflow にも展開検討を残す。

## 要件化の方向

`req-define` では下記を要件化の方向として想定する。

- `scripts/check_changed_docs.ts` の usage メッセージで --files の区切り形式（space 区切り）を明示
- `scripts/check_changed_docs.ts` の --files パーサを拡張し comma 区切りを受入（split on comma）。後方互換性担保
- `docs/specs/integrity/targeted-docs-guard-implementation.md` の呼出例に区切り形式（space 区切り推奨、comma 区切りも受入）を明記
- `src/opencode/commands/agentdev/case-close.md` Step 3-1 の実行コマンド例に区切り形式を明示
- TARGET-EMPTY strict failure の判定ロジック自体は変更しない（files_checked が正当に空でない場合の検出機能は維持）
- case-run、spec-save 等 --files を使用する他 workflow にも同様の事象が発生し得るため、関連 workflow への展開を別 case で検討
- 文書種別配置は `docs/specs/foundations/document-model.md` の文書7分類モデルに従う（暫定分類: 挙動SPEC）
