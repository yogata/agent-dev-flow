---
source_type: learning
generated_by: backlog-review
generated_at: 2026-08-09
status: draft
tentative_classification: 挙動SPEC
agentdev_handoff: true
sources:
  - type: learning
    path: .agentdev/learning/promoted/spec-phase0-commit-scope-design.md
---

# RU-0013: Phase 0 commit スコープ設計運用の明文化

本 RU は AgentDevFlow 本体の改善点を扱う前工程からの引き継ぎ用 RU である（`agentdev_handoff: true`）。現在プロジェクトでは実装しない。

## Sources

### source 1: `spec-phase0-commit-scope-design.md`（learning）

Phase 0 commit スコープ設計運用の明文化。具体例: 孫 Issue 間での SPEC スコープ交差（on_failure で SPEC 修正を許容するか明示が必要）、ドメイン state 更新と成果物変更の同一コミット混在（2分割運用の明示）。case-auto、case-run SPEC 拡張を求む。feature 分類推奨。関連: PR #1898、PR #1900。

## Source Summary

Phase 0（枝PR作成フェーズ）の commit スコープ設計運用が不明確。孫 Issue 間で SPEC スコープが交差する場合の扱い、ドメイン state 更新と成果物変更の同一コミット混在の扱いを明文化する必要がある。case-auto、case-run SPEC の拡張で対応する。

## 統合理由

特別な統合、分割不要（1:1）。

## 要件化の方向

req-define は次の推奨方向を検討する:

- case-auto、case-run SPEC への Phase 0 commit スコープ設計運用の明示
- 孫 Issue 間 SPEC スコープ交差時の扱い（on_failure で SPEC 修正を許容するか等）の SPEC 化
- ドメイン state 更新と成果物変更の同一コミット混在の禁止・許容・2分割運用の明示
