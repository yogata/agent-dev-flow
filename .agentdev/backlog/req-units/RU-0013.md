---
source_type: post_incident
generated_by: case-auto-failure-analysis
generated_at: 2026-07-27T12:45:00+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: internal://case-auto-failure-analysis/m0042
    type: post_incident
    description: case-auto @drafts Phase 0 完了報告における契約違反・未完了定義の指摘（6点）
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **Phase 0 定義適用の完全性と原子性の欠如**: case-auto Phase 0（req-save + spec-save）で以下の問題が発生した:
  1. Draft 2 の10シナリオがプレースホルダー（未定義）のまま `auto_ready: true` で処理対象となり、未確定仕様が accepted SPEC へ保存された
  2. Draft 3 の SPEC 変更が既存 SPEC（「intake/learning capture: 非関与」）と矛盾する状態で commit された
  3. search-target-area.ts が全件 `matches: []` を返した際、決定的スクリプト失敗時の手動 fallback（LLM推論で代替）が実施され、spec-save 契約（skip + follow-up）に違反した
  4. warn 結果が「Phase 0 完了」として集計され、OU 完了と区別されなかった

## Source Summary

Phase 0 の定義適用は完全性と原子性を満たす必要がある。未確定プレースホルダーは `auto_ready: true` にならない。SPEC 所有事項は case-run に先送りしない。決定的スクリプト失敗時の手動 fallback は禁止し、skip + follow-up とする。warn を「完了」と集計しない。矛盾した accepted SPEC を commit しない。Phase 0 成功と OU 完了を区別して報告する。

## 統合理由

1:1。Phase 0 適用の完全性・原子性要件を要するため独立 RU。REQ-006（case 実行契約）または REQ-008（一時成果物ライフサイクル）のいずれへ配置するかは req-define で判断する。

## 要件化の方向

REQ-006 または REQ-008 へ以下を追加する:

- 未確定プレースホルダー（「case-run工程で確定」等の記述）を含む draft は `auto_ready: true` としないこと
- SPEC 所有事項（シナリオ定義、合格基準等）を case-run 工程へ先送りしないこと
- 決定的スクリプト（search-target-area.ts 等）が空結果を返した場合、LLM推論で代替せず action を skip し follow-up として報告すること
- warn 結果を「完了」と集計せず、pass / warn / fail を区別して報告すること
- 既存 SPEC と矛盾する変更を commit しないこと（spec-save 完了前に整合性検証を実施）
- Phase 0 成功（artifact_actions 適用完了）と OU 完了（Issue化・実装・検証完了）を区別して報告すること

systemic-2（search-target-area.ts 正規化修正）と systemic-3（target_area 横断契約是正）で一部対応済み。本 RU は要件レベルでの完全性・原子性の正式化を目的とする。
