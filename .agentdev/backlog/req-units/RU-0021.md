---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-27T08:03:39+09:00
status: draft
agentdev_handoff: true
tentative_classification: guide
sources:
  - path: .agentdev/learning/promoted/command-gh-pr-merge-delete-branch-guardrail.md
    type: learning
---

## Sources

### command-gh-pr-merge-delete-branch-guardrail（learning）

case-close Step4 で `gh pr merge N --squash --delete-branch` 実行時、対象ブランチがアクティブ worktree に checkout されていると local branch 削除が「cannot delete branch used by worktree」で失敗する。`--delete-branch` は local→remote の順で削除を試みるため、local 失敗が remote 削除フェーズへの到達を阻害する。GitHub 側マージは成功するが remote branch が残存する。ギャップ分類は guardrail insufficiency（Step4/7 の手順は正しい順序だが `--delete-branch` なし運用の明示的推奨がない）。変更対象は case-close.md Step4、standard-procedures.md。根拠: PR#1143、Issue#1141。

## Source Summary

`gh pr merge --delete-branch` が worktree 占有ブランチで local 削除に失敗し、remote 削除まで到達しない。手順の順序は正しいが、`--delete-branch` を付けない運用の明示的推奨が欠けている。

## 統合理由

1:1。case-close の PR マージ時ブランチ削除ガードレールという単一関心。

## 要件化の方向

req-define は以下を確定すること。

1. case-close.md Step4 に、アクティブ worktree に checkout されたブランチでの `--delete-branch` 失敗リスクと、`--delete-branch` なし運用の明示的推奨を追記する。
2. standard-procedures.md に worktree 占有時のブランチ削除手順を追記する。
3. 受入条件（4項目）を確定し、推奨分類 chore（enhancement, documentation）を反映する。
