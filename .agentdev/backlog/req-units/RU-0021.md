---
id: RU-0021
title: "検査テストスイートの健全性是正（flaky 隔離・期待値乖離）"
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-16
status: draft
work_type: bugfix
depends_on: []
agentdev_handoff: true
tentative_classification: 対象外
sources:
  - .agentdev/intake/promoted/2026-09-16-archive-builder-staging-flaky-in-parallel-suite.md
  - .agentdev/intake/promoted/2026-09-16-check-extensions-expected-count-drift-17-vs-16.md
---

# RU-0021: 検査テストスイートの健全性是正（flaky 隔離・期待値乖離）

## Sources

- **archive-builder staging テストの並列時 flaky**: `trusted-distribution-gate/launcher-blockers.test.ts` の archive-builder staging path テスト（same-filesystem staging / parent blocker #2）が bun test フルスイート並列実行時のみ不安定 fail、単独実行では worktree・main とも green（17 pass / 0 fail）。一時ディレクトリ競合が推定される。対応候補はテスト内一時ディレクトリの分離、環境変数 TMP の個別化、または並列実行での当該テスト隔離（flaky test 分類）。単独実行 green・フルスイート時のみ再現という特性から、検証はフルスイート実行で行うこと（観測元: Case #2870 / PR #2872、main HEAD 5d2f297b でも同一故障）。
- **check_extensions テスト期待値の実態乖離（17 vs 16）**: `repo-agentdev-integrity/scripts/check_extensions.test.ts` の workflow_extensions 期待値（17、Issue #2559 由来の base known drift コメント付き）が実態（16）と乖離し、checkExtensions integration テストが main でも継続 fail。checker 本体 `check_extensions.ts` は workflow_extensions: 16・internal_workflow_extensions: 0・failures: 0 で正常であり、テスト期待値のみが 17 のまま。対応候補は期待値更新（17→16 または動的算出）または実態追従の棚卸し（16 が正か、欠落 extension の追加が必要か）（観測元: Case #2870 / PR #2872 および case-close での full integrity suite 再実行 split 1: 2556 pass / 3 fail）。

## Source Summary

いずれも trusted-distribution-gate / repo-agentdev-integrity の検査テスト群で、main でも fail が継続する恒常的なテストノイズの是正である。前者は並列実行時のリソース競合に起因する flaky、後者は期待値固定の陳腐化であり、failure mode は異なるが「検査テストスイートの健全性回復」という同一関心に属し、受け入れ検証手段も同一（bun test フルスイート実行での green 確認）である。

## 統合理由

N:1 統合。同一テストスイートの恒常 fail 是正であり、受け入れ検証が同一（フルスイート実行）。adversarial-review は統合強度を soft と判定し分割選択肢を提示したが、HITL（D-4(a)）で統合維持が確定した。req-define でスコープ分離が必要になった場合は 1:N 分割も可能。

## 要件化の方向

work_type: bugfix（テスト実装の不具合是正）。

1. staging path テストの一時ディレクトリ分離（staging path 競合の切り分け、環境変数 TMP の個別化、または並列実行時の当該テスト隔離）。検証はフルスイート実行で行う（単独実行では再現しないため）
2. check_extensions テスト期待値の更新（17→16 または動的算出）。ただし 16 が実態として正か（欠落 extension の追加需要がないか）の棚卸しを前置する

受け入れ条件: bun test フルスイート並列実行で archive-builder staging 関連テストが安定して green となること、checkExtensions integration テストが期待値と checker 実体の一致により green となること、いずれも main での再実行で再発しないこと。

## 前工程からの引き継ぎ

本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程からの引き継ぎ用 RU である（`agentdev_handoff: true`）。agent-dev-flow リポジトリ（self-hosting）では停止せず、通常の req/case workflow 入力として扱う。
