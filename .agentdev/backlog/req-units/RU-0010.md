---
source_type:
  - learning
generated_by: backlog-review
generated_at: 2026-09-08T22:28:15+09:00
status: draft
tentative_classification: 挙動Design
agentdev_handoff: true
sources:
  - .agentdev/learning/promoted/update-windows-verification-execution-preconditions.md
---

# RU-0010: Windows 検証実行・証跡取得の環境前提の契約集約（残存 gap のみ）

AgentDevFlow 本体向け引き継ぎマーカー（`agentdev_handoff: true`）。本リポジトリは self-hosting のため通常の req/case workflow 入力として扱う。

## Sources

- `.agentdev/learning/promoted/update-windows-verification-execution-preconditions.md`: Windows + PowerShell + bun 環境で checker・検証コマンドを実行する際、出力退避・依存解決・終了コード取得の環境固有の失敗モードが反復している（8 観測が蓄積）。case-run / case-close の各 gate・QG 検証で証跡欠落・gate 不能・誤判定として発覚し、blocked 再作業を生んできた。問題の型: PowerShell の `>` / `*>` リダイレクト・パイプ受信が UTF-8 出力を cp932 系で再符号化し JSON 証跡・文字列検索を破壊、bun test のレポータ出力が stderr に流れ stdout 単独の証跡退避で記録がゼロになる、Bun ランタイム API（Bun.YAML 等）に依存する checker を node 安定実行経路で実行できず fail-closed する、worktree では node_modules が伝播せず外部依存（zod 等）を持つ検証スクリプトが bun install 前置なしに実行できない、pwsh のパイプライン後は $LASTEXITCODE が最終コマンドの終了コードで上書きされる、PowerShell の一括読み書き（Get-Content → 加工 → Set-Content）が部分失敗で空書き込みに到達しうる。既存対策あり（整備不備）: `docs/knowledge/windows-powershell-bulk-io-corruption.md`（項 3 が「規定化に至らず living pool 再評価待ち」と明記）、worktree-operations.md L130・qg-4-final-acceptance.md L244（依存前置規定は agentdev-project-extensions/scripts 1 ディレクトリのみ）、AGENTS.md（一括読み書き警告）。根拠: PR #2677（Bun.YAML node 不能）、PR #2691（PS リダイレクト符号化破損）、PR #2198（PS 一括読み書き破壊）ほか deferred 6件。制約: Windows + junction + bun 環境固有の知識であり、配布物（ADF core）への直接埋め込みは技術固有知識非保持の原則に従う。

## Source Summary

検証・証跡取得の実行契約が環境固有の失敗モードを明示しておらず、Windows 環境での gate・QG 検証が反復的に失敗する。既存の知識文書・Design 注記・AGENTS.md 警告で一部は規定済みだが、checker stdout 退避の標準形式・stderr 併退避・Bun 依存 checker の実行経路区別が契約に未反映の残存 gap。対論（adversarial-review）でスコープ縮小済みであり、本 RU は残存 gap の契約集約のみを扱う。

## 統合理由

1:1。単独論点。learning-promote から fix gap / application miss として渡された改善要求であり、統合・分割不要。

## 要件化の方向

- 対論でスコープ縮小済みの残存 gap のみを要件化する。(a) bun test 証跡の stderr 併退避。(b) Bun 依存 checker の bun 経路実行の、checker 実行契約「安定実行経路」節への明記。(c) AGENTS.md のリダイレクト・パイプ面補記。
- 既存対策（worktree 依存前置対象集合・knowledge 項 3 規定化）は大部分充足済みと明記し、残余の要否を req-define で判断する。
