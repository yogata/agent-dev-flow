---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-14T09:09:45+09:00
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-14-req-tags-frontmatter-canonical-conflict.md
    type: intake
---

## Sources

### intake: req-tags-frontmatter-canonical-conflict

`required-fields` 検査が全 active REQ（21件）+ retired REQ-0111 の frontmatter に必須 `tags` が未設定と検出（NG 22件）。しかし根因は validator と正典 SPEC/REQ の矛盾: `check_integrity.ts:387` は `required = ["id", "title", "created", "updated", "tags"]` と `tags` を要求するが、REQ-0101-005 と `docs/specs/patterns.md:43-52` は REQ frontmatter を `id`, `title`, `created`, `updated` の4フィールドに限定（L52「フィールドは〜のみ」と明記）。元 item の推奨「全 REQ に tags を一括付与」を実行すると `patterns.md:52` と矛盾し新たな違反を生む。

## Source Summary

文書修正ではなく方針決定が必要な canonical conflict。validator が要求する `tags` フィールドを正典が要求していない。以下のいずれかを決定する必要がある: (A) `tags` を必須とする — REQ-0101-005 と `patterns.md` を更新して全 REQ へ `tags` 付与、(B) `tags` を必須としない — validator の `required` 配列から `tags` を削除して NG 22件を解消。

## 統合理由

**1:1**。REQ frontmatter `tags` フィールドの canonical conflict は独立した方針決定課題。RU-0005（command frontmatter 禁止 field）とは対象ファイル種別（REQ vs command）と問題性質（required vs prohibited）が異なる。RU-0002（checker FP 校正）とも異なる — 本件は heuristic の文脈認識不足ではなく、validator と SPEC の定義矛盾。

## 要件化の方向

- **方針決定が前提**: req-define で (A) `tags` 必須化 または (B) validator から `tags` 削除 を判断
- (A) の場合: REQ-0101-005 と `patterns.md` を更新 → 全 REQ へ `tags` 付与 → validator はそのまま
- (B) の場合: validator の `required` 配列から `tags` を削除 → REQ/SPEC はそのまま → NG 22件が解消
- REQ-0101-005（REQ frontmatter 必須フィールド: 4フィールド、tags なし）、`patterns.md:43-52`（REQ frontmatter 正典パターン）、REQ-0108-099（required-fields 検査）に基づく
- **route**: req-define（方針決定が前提）
- **category**: docs_chore または feature（方針次第）
- **優先度**: 高（NG 22件は全 active REQ に及ぶ）
