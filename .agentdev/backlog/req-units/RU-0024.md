---
id: RU-0024
title: "bun test 正規形の環境前提の実行契約明記（依存解決前置・worktree plugins 分割差）"
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-16
status: draft
work_type: docs_chore
depends_on: []
agentdev_handoff: true
tentative_classification: guide
sources:
  - .agentdev/intake/promoted/2026-09-16-bun-install-blocked-by-missing-root-package-json.md
  - .agentdev/intake/promoted/2026-09-16-worktree-plugins-split-not-executed.md
---

# RU-0024: bun test 正規形の環境前提の実行契約明記（依存解決前置・worktree plugins 分割差）

## Sources

- **ルート package.json 不在による bun install 不能**: bun test 3 分割正規形の依存解決前置としての bun install が、リポジトリルートに package.json が存在しないためルート実行で失敗する（Case #2846 F-002、Case #2856 で再確認、2026-09-16 再実査でルート不在を確認）。worktree 作成直後や新規 clone 環境で node_modules が無い場合、`bun install --cwd <scripts 配下>`（package.json + bun.lock のある場所ごと）を前置する必要があるが、この前提は正規形手順に明記されていない。対応候補は実行契約（QG-4 reference 等）への依存解決前置手順の明記、またはリポジトリルートへの最小 package.json 追加（配布境界への影響評価が前提）。
- **worktree で plugins 分割が junction 未伝播により不在**: worktree 環境では `.opencode/plugins` が junction 未伝播のため存在せず、plugins 分割（`bun test ./.opencode/plugins/ ./scripts/`）が `./scripts/` のみ実行となる。動作としては妥当だが、plugins 分割が実行されないことが実行ログから読み取りにくく、検証カバレッジの見落としリスクがある（Case #2831 / #2846 で観測。検出自体は main 側 full-audit で担保）。対応候補は worktree 実行時の plugins 分割代替手段（main root からの `--root` 指定等）の運用注記、または QG-4 reference への環境差補足。

## Source Summary

両成果物は bun test 3 分割正規形の環境前提（依存解決の前置手順・worktree での分割対象の環境差）が暗黙知であり、新規環境・worktree での初動失敗と検証カバレッジ見落としの温床となっている点で同一関心である。いずれも REQ-060 系（bun test 実行形態統一）・QG-4 reference を明記対象の実行契約とする。

## 統合理由

N:1 統合。両成果物が相互に統合候補を明記しており、同一ドメイン（bun test 正規形の環境前提）・同一明記先（REQ-060 系・QG-4 reference）であるため、単一の要件単位として扱う方が実行契約の一貫性が高い。

## 要件化の方向

work_type: docs_chore（構成変更分岐採用時は maintenance）。req-define で確定する事項:

1. 依存解決前置手順（scripts 配下ごとの `bun install --cwd`、ルート package.json 不在前提）の実行契約への明記
2. worktree 実行時の plugins 分割環境差（junction 未伝播）の運用注記または QG-4 reference への補足
3. 分岐: リポジトリルートへの最小 package.json 追加を取るか（distribution-boundary Design の影響評価を前提）、手順明記で完結するか

受け入れ条件: 新規環境・worktree で bun test 3 分割正規形が文書のみで実行可能であること（依存解決前置が明記済み）、worktree 実行時に plugins 分割が実行されない状態が実行ログ・注記から読み取れること。

## 前工程からの引き継ぎ

本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程からの引き継ぎ用 RU である（`agentdev_handoff: true`）。agent-dev-flow リポジトリ（self-hosting）では停止せず、通常の req/case workflow 入力として扱う。
