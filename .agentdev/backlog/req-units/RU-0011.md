---
source_type: learning
generated_by: backlog-review
generated_at: 2026-07-14T23:41:14Z
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/learning/promoted/existing-skill-gh-cli-node-e-regex-backreference.md
    type: learning
---

## Sources

- **existing-skill-gh-cli-node-e-regex-backreference（learning）**: PowerShell + Node.js `node -e` の組み合わせで regex backreference `$1`/`$2`/`$3`（`$` + 数字）を使うと、PowerShell が二重引用符内の `$identifier` を PowerShell 変数として先に補間し空文字列へ置換する。その後 Node.js が壊れた JS コードを評価し、SyntaxError または意図しない置換結果になり対象ファイルが生成されない事象。Epic #1472 Wave 3 case-close（Issue #1475 完了条件更新、PR #1478）で発生。`agentdev-gh-cli` skill の `references/standard-procedures.md` はクォート競合パターン（テンプレートリテラル禁止、`.join()` 推奨、退避策としての `.js` ファイル化等）を扱うが、regex backreference `$N` の PowerShell 変数補間による破壊を明示的に扱っていない（guardrail insufficiency）。横展開可能性は高い（PowerShell + Node.js `node -e` を使う全コマンド・全 agent が対象）。

## Source Summary

`agentdev-gh-cli` の既存クォート競合対策はバッククォート（テンプレートリテラル）とシングルクォート（JQ 式）を対象とするが、ドル記号 `$` を前置する regex backreference を明示的に禁止していない。これは別の破壊経路（PS 変数補間）であり、禁止事項の拡充が必要。文書追記のみで実装コード変更を伴わない（docs_chore）。

## 統合理由

1成果物1論点のため 1:1。agentdev-gh-cli skill の手順書追記という独立した是正単位。

## 要件化の方向

req-define では以下を検討する方向:

1. `standard-procedures.md` Section 1（禁止事項）に「`node -e "..."` の二重引用符内で regex backreference `$1`/`$2`/`$3` 等（`$` + 数字）を使用することを禁止する。PowerShell が PS 変数として先に補間し空文字列へ置換するため JS コードが破壊される」を追記
2. Section 3（READ 手続き）項目7（退避策）を拡充し、regex backreference を伴う `String.prototype.replace` が必要な場合は `.js` ファイルへの退避を必須とする旨を明記。単純な文字列置換は既存の `split().join()` 推奨（項目10）で対応
3. 追記内容が既存のテンプレートリテラル禁止（項目9）、`.join()` 推奨（項目10）と矛盾しないこと
4. ローカル版（`src/opencode-local/agentdev-gh-cli/`）は別ファイル差し替え（ADR-0131）のため対象外（適用プロジェクト側の責務）

route: docs_chore（文書追記のみ）。REQ-0155-005（学びの直接 REQ 化禁止、backlog-review → RU → req-define 経路を踏む）。
