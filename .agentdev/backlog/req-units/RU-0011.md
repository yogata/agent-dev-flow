---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-27T10:02:34+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-07-27-spec-target-area-append-fallback-formalization.md
    type: intake
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **未検出時 APPEND fallback の非公式運用**: req-define が既存 SPEC への新規セクション追加を `operation: spec-update` と未作成見出しの `target_area` で表すため、`search-target-area.ts` は `matches: []` を返す。spec-save Step 5 は未検出時の follow-up 報告と create 系 operation への切替を推奨するが、実行時には content が見出しから始まる場合の APPEND fallback が一貫して使われている。この deviation は case-auto Draft 1 〜 8 のほぼ全件で発生した。

## Source Summary

新規セクション追加の実意は満たすものの、公式手続きと実運用が一致せず、同じ deviation の報告と workaround が反復する。req-define / spec-save 双方の振る舞い契約のいずれかを公式化し、workaround 反復を排除する必要がある。work_type は maintenance、standard scale を想定。

## 統合理由

1:1。req-define / spec-save 振る舞い契約の設計判断（新規 operation 追加 or 未検出時 APPEND 公式化）を要するため独立 RU。いずれの方式を選ぶかが未決定であり、req-define で判断を確定する必要がある。

## 要件化の方向

req-define で以下のいずれかを選択し振る舞い契約へ反映する:
- 選択肢 A: req-define 側で新規セクション追加を第一級 operation として追加
- 選択肢 B: spec-save 側で未検出時の APPEND fallback を公式手続き化
- 反映先: `docs/specs/commands/spec-save.md` Step 5, `docs/specs/commands/req-define.md` Step 7-3a, `search-target-area.ts` 契約
