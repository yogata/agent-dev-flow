---
source_type: inspect
generated_by: backlog-review
generated_at: "2026-06-17T23:31:25+09:00"
status: draft
agentdev_handoff: true
sources:
  - path: .agentdev/inspect/promoted/inspect-docs-finding-req-spec-granularity.md
    type: inspect
---

## Sources

**F-04: 具体的 git 手順の残留（MOVE）**: 具体gitコマンド・フラグ・commit message形式・Step順手続きが7つのREQに残留。対象: REQ-0110-005/006/008（`git worktree prune` / `git checkout .` / 3段フォールバック）；REQ-0106-019（`git rebase origin/main`）；REQ-0108-011；REQ-0109-040〜046（`git pull --ff-only`・commit message形式・template git欄）；REQ-0131-014（rebase workflow）/015（`--force-with-lease`）；REQ-0125/0126-010〜014（同系git手順）。document-model SPEC分離基準: file pattern / 内部アルゴリズム / report format→SPEC/command reference。確信度 high、シグナル数3。

## Source Summary

具体的な git コマンド（`git worktree prune`、`git checkout .`、`git rebase origin/main`、`git pull --ff-only`、`--force-with-lease`）、commit message形式、3段フォールバック手順などの HOW 詳細が7つのREQに散在。WHAT（信頼性・安全同期）に対する HOW が要件行として残留。

## 統合理由

単一クラスタ（F-04）の独立 RU 化。共通git手順を共通skill reference / common template へ集約する横断的作業として完結する。req-define入力案なし（横断的UPDATE）。

## 要件化の方向

各REQから該当git手順行を削除。共通git手順（pull --ff-only・commit message形式・push error handling等）は共通skill reference / common template（`templates/common/git-error-messages.md` 等）へ集約。REQ固有のgit手順（worktree cleanup・rebase workflow等）は該当commandのskill reference へ移管。
