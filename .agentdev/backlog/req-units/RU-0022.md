---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-23T08:01:22+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-07-18-case-auto-capture-boundaries.md
    type: intake
---

## Sources

- 対象: `src/opencode/commands/agentdev/case-auto.md` が `capture-boundaries` 参照を持たない。
- 検出: CaptureBoundary 検査（command-capture-duty、`check_integrity.ts`）が、各 agentdev command に capture 責務記述として `capture-boundaries` への言及を期待。case-auto で検出1件（参照なし）。
- 影響: 検査スクリプトが case-auto を capture 責務不在と判定する可能性がある。ただし case-auto は統合委譲起点であり、capture 責務を個別に持たない設計である可能性があり、その場合は参照欠落は不具合ではなく検査側の期待値問題となる。
- 関連: REQ-0105（Intake / Learning / Backlog lifecycle、capture 責務）、ADR-0127（委譲契約）、ADR-0137（case-run インライン実行）。

## Source Summary

case-auto command の capture-boundaries 参照欠落が、設計上の期待（capture 責務あり）か、検査側の誤期待（capture 責務なし、統合委譲起点）かにより対応が分かれる。要件レベルでの設計意図確認が前提。

## 統合理由

1:1。対象成果物は1件のみで、独立した関心領域（case-auto command の責務境界）を持つ。

## 要件化の方向

req-define で case-auto の設計意図を要件レベルで確認した上で、以下いずれかを選択確定する:

1. **参照追加**: case-auto.md に `capture-boundaries` への参照を追加（他の委譲起点 command と同じパターン）。
2. **検査例外規則の追加**: case-auto は統合委譲起点であり capture 責務を個別に持たない設計の場合、CaptureBoundary 検査に例外規則を追加。
3. **設計見直し**: case-auto が capture 責務を持つべきか否かを要件レベルで見直す。

優先度: medium（設計意図の確認が前提、機能阻害なし、検査ノイズの継続）。

反映先候補: `src/opencode/commands/agentdev/case-auto.md`（選択肢1）、CaptureBoundary 検査実装 `check_integrity.ts`（選択肢2）、REQ-0105 関連 SPEC / ADR（選択肢3）。

## 備考

本 RU は AgentDevFlow 本体（agent-dev-flow リポジトリ）の改善を扱う前工程からの引き継ぎ RU である（`agentdev_handoff: true`）。自己ホスティング環境のため agent-dev-flow で実装される。
