---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-27T08:03:39+09:00
status: draft
agentdev_handoff: true
tentative_classification: guide
sources:
  - path: .agentdev/learning/promoted/skill-powershell-utf8-encoding.md
    type: learning
---

## Sources

### skill-powershell-utf8-encoding（learning）

Windows PowerShell（日本語ロケール）で [Console]::OutputEncoding が既定 cp932。L-005（Write 既存 UTF-8 ファイル cp932 化）は Write 経路のみカバーし、gh CLI stdout 読込・git commit -m 引数の2経路は未カバー。

1. **gh CLI stdout 読込**: `gh issue view --json body` で取り込んだ日本語本文が cp932 デコードで mojibake を起こし、`gh issue edit --body-file` で書き戻すと GitHub 本文が破損（PR#1165、Issue#1164）。
2. **git commit -m 引数**: `git commit -m「日本語タイトル」`でタイトル行が mojibake（本文は正常、commit 8ebe0e98）。

ギャップ分類は fix gap。変更対象は standard-procedures.md（[Console]::OutputEncoding=UTF8 設定）、verify.md（mojibake チェック追加）、agentdev-conventional-commits（-F utf8-file 使用、-m は ASCII-only）。横展開性 5/5。

## Source Summary

PowerShell 既定 cp932 により、gh CLI stdout 読込と git commit -m 引数の2経路で日本語 mojibake が発生。L-005 は Write 経路のみ対応し、これら2経路は未対策。

## 統合理由

1:1。PowerShell UTF-8 エンコーディング標準化という単一関心。RU-0021（gh pr merge ガードレール）とは異なるガードレール領域であり独立している。

## 要件化の方向

req-define は以下を確定すること。

1. standard-procedures.md に `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` 設定を標準手続きとして追記する。
2. verify.md に mojibake チェックを追加する。
3. agentdev-conventional-commits で `git commit -m` は ASCII-only とし、日本語を含む場合は `-F utf8-file` 使用を規定する。
4. 受入条件（4項目）を確定し、推奨分類 chore（enhancement, documentation）を反映する。
