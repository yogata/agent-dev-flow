---
id: RU-0127
title: "並行 case-open の worktree / Definition branch 境界規律の明文化"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-24T10:22:39+09:00
status: draft
agentdev_handoff: true
tentative_classification: REQ
sources:
  - type: learning
    path: .agentdev/learning/promoted/existing-countermeasure-update-parallel-delegation-worktree-boundary.md
  - type: intake
    path: .agentdev/intake/promoted/2026-09-24-definition-pr-branch-stacking-discipline.md
---

# RU-0127: 並行 case-open の worktree / Definition branch 境界規律の明文化

## Sources

- `.agentdev/learning/promoted/existing-countermeasure-update-parallel-delegation-worktree-boundary.md`: 複数の並行 case-open / 委譲が同一 worktree・checkout 状態を共有すると 1-writer 前提が崩れ、branch 混入（Case #3087 の Definition branch 親履歴に #3088 の commit cb59cb4e 混入→隔離 worktree で差分再構成）、cd 誤りによるメインリポジトリ一時汚染（Wave 5 PR #1632、5ステップ是正）、並行書込み（Case #2805、明示パスステージで混入防止）の3系統が発生。Case 専用 worktree 前置、PR 前 merge-base / diff --stat 検査、明示パスステージ、git status 検知、1-writer 早期断念基準が未明文化（fix gap）。8軸評価 29/40、既存対策の更新。
- `.agentdev/intake/promoted/2026-09-24-definition-pr-branch-stacking-discipline.md`: case-auto stage-2（Case #3089）で Definition PR #3092（branch `definition/issue-3089`）が兄弟 Case の Definition commit を含むスタック構造（origin/main + cb59cb4e + 907a081e + b9678628）であることを実観測。親の overlap 分析（宣言ファイル集合）と GitHub pr_changed_files 実報告に乖離。実際の merge 順序では実効 squash diff が自 Case 分に収まり二重取り込みは発生しなかったが、スタック底が最後 merge の場合は空 diff / 同一領域競合になり得る。REQ-083 は branch 命名のみ、REQ-032-010/011 は merge 失敗後の事後救済のみ規定で予防段階は未被覆。処分区分候補: edge_case。

## Source Summary

並行 case-open（stage-1 並行委譲〜stage-2 merge）における branch / worktree 境界の侵害が3系統の learning と intake の実観測で構造化された。作業隔離の侵害（共有 worktree・branch 混入・main 汚染・並行書込み）と、branch 作成時点のベース選択の欠陥（スタック構造・overlap 分析乖離）は同一の並行実行境界の予防規律として連続する課題であり、REQ-030/034/083 系の既存要件への規律明文化で補完する。

## 統合理由

2:1 統合（learning + intake）。統合理由: 同一対象（並行 case-open の branch/worktree 境界）で、同一の反映先ファイル群（case-open 運用手順・adapter protocol・case-auto 委譲境界）と Case 分割効率を考慮すると統合が妥当。learning 側は作業隔離の侵害事例、intake 側は branch ベース選択の予防段階であり補完的。

## 要件化の方向

並行 case-open の worktree / branch 境界規律を REQ（REQ-030/034/083 系）として明文化する: Case 専用 worktree 前置、Definition branch の origin/main HEAD 独立作成規律、PR 前 merge-base / diff --stat 検査、stage-2 overlap 突合（宣言変更ファイル集合と pr_changed_files 実報告の差分検査）、worktree 1-writer 前提の早期断念基準（in-scope 外書込み検知時は直ちに停止）。
