---
id: RU-0026
title: "check_distribution_boundary_cli.ts の誤起動防止（export-only による無出力 exit 0）"
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-16
status: draft
work_type: maintenance
depends_on: []
agentdev_handoff: true
tentative_classification: 挙動Design
sources:
  - .agentdev/intake/promoted/2026-09-16-check-distribution-boundary-cli-export-only-pitfall.md
---

# RU-0026: check_distribution_boundary_cli.ts の誤起動防止（export-only による無出力 exit 0）

## Sources

- **cli.ts が export-only モジュールで CLI 起動時無出力・exit 0**: `.opencode/skills/repo-agentdev-integrity/scripts/check_distribution_boundary_cli.ts` は `export function runCli(): void` を定義するが top-level での呼び出しを持たず、`bun check_distribution_boundary_cli.ts --profile source` の形態で直接起動しても何も出力せず exit 0 で即終了する（Case #2846 F-003、2026-09-16 再実査で runCli が export-only・top-level 呼び出し不在を確認）。CLI 起動形式を誤っても成功扱いになるため、検証を誤認するリスクがある。正規の起動が check_distribution_boundary.ts（検査本体）経由であることは checker 実行契約 Design（`docs/designs/integrity/checker-execution-contracts.md`）に記載があるが、cli.ts というファイル名が誤起動を誘発しやすい。

## Source Summary

検証担当が正規の検査を実行したつもりで無検証のまま成功扱いになるリスク（検証誤認）を、checker 実行契約への注意追記または cli.ts 自体のエントリポイント化・リネームにより解消する。

## 統合理由

1:1。単一成果物・単一論点（誤起動防止）であり、統合・分割不要。

## 要件化の方向

work_type: maintenance。軽微な docs 対応と scripts 対応の選択は req-define で確定する:

1. checker 実行契約 Design（`docs/designs/integrity/checker-execution-contracts.md`、REQ-047 系）へ、cli.ts が export-only であり直接起動では無検証になる旨の注意追記
2. または cli.ts のエントリポイント化（top-level での runCli 起動）／リネーム（命名規約の明確化）

受け入れ条件: cli.ts を直接起動する形態で無検証のまま成功扱い（無出力・exit 0）になる経路が消失するか、当該誤起動リスクが契約文書で機械的・明示的に警告されること。

## 前工程からの引き継ぎ

本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程からの引き継ぎ用 RU である（`agentdev_handoff: true`）。agent-dev-flow リポジトリ（self-hosting）では停止せず、通常の req/case workflow 入力として扱う。
