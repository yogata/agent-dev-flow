---
source_type: post_incident
generated_by: case-auto-failure-analysis
generated_at: 2026-07-27T12:45:00+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: internal://case-auto-failure-analysis/m0042
    type: post_incident
    description: case-auto @drafts 完了報告受領不可判定における agentdev_handoff 停止の契約違反指摘
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **agentdev_handoff の repository-kind-aware 扱い欠如**: case-open command が `agentdev_handoff: true` を無条件停止条件として扱い、リポジトリ種別（self-hosting / consumer）を判定しなかった。REQ-005-018〜022 は consumer repo でのみ停止することを明記し、workflow-lifecycle reference も本体リポジトリでは停止しないと明記するが、case-open command 実装がこれに違反した。結果として agent-dev-flow 本体での5草案が全て Phase 1 で誤停止し、case-auto 全体が失敗した。

## Source Summary

`agentdev_handoff: true` の停止動作はリポジトリ種別に依存すべきである。self-hosting（agent-dev-flow 本体）では provenance メタデータとして保持し通常実行を継続、consumer では Phase 0 より前に停止し本体への引き継ぎとして報告する。case-auto / case-open / case-run で同一の repository-kind 判定を使用する。停止時は consumer handoff、誤停止時は command 契約不整合として区別する。

## 統合理由

1:1。handoff routing の設計判断（repository-kind-aware gate）を要するため独立 RU。

## 要件化の方向

REQ-005 へ以下を追加する:

- REQ-005-0XX: `agentdev_handoff: true` 検出時の動作はリポジトリ種別に依存すること
  - self-hosting（`src/opencode/` 存在）: 停止条件とせず、provenance として保持し通常の req/case workflow で処理
  - consumer（`src/opencode/` 不在）: Issue を作成せず停止、agent-dev-flow repository への手動取り込み対象として報告
- REQ-005-0XX: case-auto / case-open / case-run は同一の repository-kind 判定ロジックを使用すること
- REQ-005-0XX: 誤停止（self-hosting での無条件停止）は command 契約・実装不整合として停止条件(11)へ分類すること

既存の systemic-1 修正（case-open.md Step 1, case-run.md L68, upstream-handoff.md）で一部対応済み。本 RU は要件レベルでの正式化を目的とする。
