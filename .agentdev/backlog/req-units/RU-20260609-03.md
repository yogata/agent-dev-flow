---
source_type: analysis
generated_by: codex
generated_at: 2026-06-09T00:00:00+09:00
status: draft
sources:
  - type: analysis
    path: session:2026-06-09-docs-consistency-bundle-review
---

# RU: completion-reports.md の dead contract を除去し現行完了報告契約へ統一する

## 背景

完了報告の現行契約は `docs/specs/artifact-contracts.md` と command-local template（`.opencode/commands/agentdev/templates/{command}/{variant}.md`）へ移っている。

しかし `REQ-0107`、repo-local skill、docs-check template、checker スクリプトの一部には `completion-reports.md` を current contract とみなす記述が残っている。

## 問題

`REQ-0107` 自体が `completion-reports.md` を variant 定義先として記述しつつ、別の要件行では廃止済みとしており、active REQ 内で矛盾している。

また `check_integrity.ts` には `completion-reports.md` を前提とする関数群が残り、しかも一部は main の検査経路に接続されていない。これにより、現行完了報告契約と checker 実装が乖離している。

## Source Summary

- 現行 contract は command-local template + artifact-contracts である。
- `REQ-0107` には旧 contract と新 contract が同居している。
- docs-check template / integrity skill / checker に dead contract が残っている。
- checker には未接続の検査関数があり、完了報告周辺の検査が見かけほど効いていない。

## 統合理由

本件は reporting contract の現行化と docs-check 実装整備を同時に行う必要がある。

`REQ-0107` と `REQ-0108` をまたぐ APPEND / UPDATE として整理し、command template・skill・checker の dead contract を一括除去するのが妥当である。

## 要件化の方向

以下を APPEND / UPDATE する。

- `R
EQ-0107` から `completion-reports.md` を current SSoT と読める記述を除去し、現行 contract を command-local template + artifact-contracts に一本化する。
- `docs-check` は `completion-reports.md` 依存ではなく、command-local template の存在・variant 参照・必須フィールドを検査対象とする。
- `docs-check` / integrity skill / repo-local template から `completion-reports.md` 前提の完了報告文言を除去する。
- `check_integrity.ts` の完了報告関連検査を、現行 contract に合わせて接続し直す。

## 主対象REQまたは変更対象候補

主対象REQ:

- `docs/requirements/REQ-0107.md`
- `docs/requirements/REQ-0108.md`

変更対象候補:

- `.opencode/skills/repo-agentdev-integrity/SKILL.md`
- `.opencode/commands/repo/templates/docs-check/standard.md`
- `.opencode/skills/repo-agentdev-integrity/scripts/check_integrity.ts`

必要に応じた変更候補:

- `docs/specs/artifact-contracts.md`
- `src/opencode/commands/agentdev/templates/`

## 対象外

以下は本RUの対象外とする。

- 完了報告テンプレート本文の個別リライト
- Issue / PR 本文テンプレート全般の再設計
- 新しい reporting skill の再導入
- 実出力 golden test / eval の新設

## 受け入れ条件

- `REQ-0107` から `completion-reports.md` を現行 contract と読む根拠が除去されている。
- repo-local template / skill / checker に `completion-reports.md` 前提の current rule が残っていない。
- docs-check が command-local template を現行 contract として検査できる。
- 完了報告関連の検査関数が main の検査経路に接続され、死んだ検査になっていない。
