---
id: RU-0122
title: "agentdev_gh リポジトリ解決対策（起動環境設定導線の文書化と defaultResolveRepo 堅牢化）"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-22T12:31:29Z
status: draft
agentdev_handoff: true
depends_on: []
tentative_classification: REQ
sources:
  - type: learning
    path: .agentdev/learning/promoted/measure-update-agentdev-gh-repo-resolution-env-setup.md
  - type: intake
    path: .agentdev/intake/promoted/intake-gh-tool-repo-resolution-failure.md
---

# RU-0122: agentdev_gh リポジトリ解決対策（起動環境設定導線の文書化と defaultResolveRepo 堅牢化）

## Sources

- learning measure-update-agentdev-gh-repo-resolution-env-setup.md: case-auto stage 1（case-open）で Custom Tool `agentdev_gh` の全操作が `config-uninterpretable`（cannot resolve the target repository）で失敗した事象の再発防止知見。`AGENTDEV_GH_REPO` を opencode 起動環境へ設定する導線（launcher / .env 相当）が文書化されておらず、docs/guides/ の導入手順にも不在であることを機械確認済み（docs/ 配下 grep で custom-tool-contracts.md L96 のみ）。plugin README は挙動を正典化するが起動前設定手順を記載しない。
- intake intake-gh-tool-repo-resolution-failure.md: 同一事象の修正提案。`src/opencode/plugins/agentdev-gh-tool/plugin.ts` の `defaultResolveRepo` は (1) 環境変数 `AGENTDEV_GH_REPO`、(2) `context.worktree` 起点の `gh repo view` の順で解決するが、opencode プロセス内では両者が失敗した。failure detail には worktree 解決状態・gh の stderr 等の診断情報が含まれず、`git remote get-url origin` による fallback 解決も未実装（実装確認済み）。

## Source Summary

同一事象（`AGENTDEV_GH_REPO` 未設定環境で opencode プロセス内の `gh repo view` が失敗し、agentdev_gh 全 workflow の書込み操作が blocked となる）を、learning 側は運用面（起動環境設定導線の欠落）、intake 側は実装面（解決ロジックの堅牢化不足）から記録したもの。失敗自体は Tool 契約どおりの正常な fail-closed 挙動であり、迂回（gh CLI 直接書込み）は POL-gh-io-delegation で禁止されたまま維持する。

## 統合理由

両成果物は同一関心領域（同根問題）の同一事象を補完的な2側面（運用・実装）から扱い、双方の受け入れ条件が「backlog-review で統合判定され、同根問題の重複 RU が生成されない」ことを要求する。Split Rule による前段分割の正規再合流経路として N:1 統合する（adversarial-review 合意 AC-1、Jev 統合判定 0.98 と一致）。

## 要件化の方向

実現先の主従を明記する（adversarial-review accepted finding 反映）:

- **主実現先（運用側）**: harness 導入ガイド（docs/guides/consumer-project-setup.md 等 docs/guides/ 配下の導入手順）への「opencode 起動環境（.env / launcher）への `AGENTDEV_GH_REPO=owner/name` 設定手順」追記。plugin README 設定節からの参照追記も候補。
- **判断材料（実装側）**: `defaultResolveRepo` の堅牢化候補（failure detail への worktree 解決状態・gh stderr 等の診断情報追加、`git remote get-url origin` による fallback 解決の検討）。採否・実現方法は req-define の変更影響分析で判断する。実装変更時は custom-tool-contracts.md（Tool 操作契約）との整合確認が必要。

前工程からの引き継ぎ（upstream handoff）用 RU であり、AgentDevFlow 本体の改善を扱う。req-define での要件定義後、AgentDevFlow リポジトリでの実装・文書追記として実現する。

## 受け入れ条件（素材から引き継ぎ）

- harness 導入ガイドに opencode 起動環境への AGENTDEV_GH_REPO 設定手順が記載されている
- AGENTDEV_GH_REPO 未設定環境での config-uninterpretable 失敗時、failure detail または README から解決手順（環境変数設定・gh 認証状態確認）に到達できる（実装側候補を採用する場合）
- 同根問題の重複 RU が生成されない（本統合 RU により充足）
