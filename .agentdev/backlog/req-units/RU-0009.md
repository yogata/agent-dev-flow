---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-27T10:02:34+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-07-27-capture-boundary-scope-expansion.md
    type: intake
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **capture 対象の限定**: 現状 capture pipeline は case-run phase の PR Findings を主な入力とする。req-save, spec-save, case-open, case-close の完了報告に含まれる deviation は case-auto のコンテキストで消費され、intake または learning pipeline へ流入しない。
- **高優先事項の再発**: 採番スクリプト不備、target_area fallback、GitHub CLI cp932 回避策という高優先3件が、複数 draft で再発しながら capture されなかった。各工程で検出した問題と workaround が再利用されず、同じ調査と対処が繰り返されている。

## Source Summary

工程横断の capture 境界が未整備で、各工程の deviation が intake / learning へ流入しない。work_type は maintenance、standard scale を想定。intake と learning の責務境界も分けて定義する必要がある。

## 統合理由

1:1。capture 実行主体と保存先判定の設計判断を要するため独立 RU。工程ごとに capture 候補を報告する方式と case-auto 完了時に全工程を集約する方式のいずれを採るかが未決定であり、req-define で判断を確定する必要がある。

## 要件化の方向

req-define で以下を確定し capture 境界 SPEC へ反映する:
- capture 実行主体（各工程 command vs case-auto 集約）の選択
- 保存先判定（intake または learning）の責務境界
- 影響 command: case-auto, req-save, spec-save, case-open, case-close
- 反映先 SPEC: `docs/specs/skills/agentdev-workflow-orchestration.md`, `docs/specs/skills/agentdev-case-run-execution-adapter.md`
