---
source_type:
  - learning
generated_by: backlog-review
generated_at: 2026-09-08T22:28:15+09:00
status: draft
tentative_classification: カタログDesign
agentdev_handoff: true
sources:
  - .agentdev/learning/promoted/design-gh-internal-contract-dual-runner-synchronization.md
---

# RU-0009: agentdev_gh before payload 契約と両版同時反映原則の Design 化

AgentDevFlow 本体向け引き継ぎマーカー（`agentdev_handoff: true`）。本リポジトリは self-hosting のため通常の req/case workflow 入力として扱う。

## Sources

- `.agentdev/learning/promoted/design-gh-internal-contract-dual-runner-synchronization.md`: Epic #2686（agentdev_gh 操作契約刷新）の Wave 1 実装で、issue_update / issue_reopen の追跡軸保持 VERIFY（role / kind / trackingState 完全一致・要求通常ラベル包含）の照合基準に「実行前状態」が必要であることが判明した。VERIFY は副作用後の読み戻しで判定する構造上、実行前状態を自己保持できないため、runner 応答 payload へ before を運ばせる接合を採用した。契約拡張を GitHub 版のみへ反映すると、未対応側（Local 版）の VERIFY が一律 verification-incomplete になる。問題: (1) runner 応答 payload の before 構造（state、labels、role、kind、trackingState、closeReason の正規化済み導出値を含めること、GhRunnerReply 成功側の必須化）が Design に未保存である（PR #2691 本文の Design 確定候補として記録されるに留まり、正規所有文書が存在しない）。(2) runner↔spec 内部契約を拡張する際、GitHub 版と Local 版へ同時反映する原則が文書化されていない。実装は既存（src/opencode/tools/agentdev-gh/engine.ts・runner-cli.ts、src/opencode-local/agentdev-gh/runner-local.ts。af697092〜ffaf3603 で両版反映済み）であり、Design が未追随の fix gap。before 導出値の詳細仕様（正規化規則）は実装（contracts.ts・tracking-schema.ts）と突合して確定する。Epic #2686 対応記録コメントの design-save 再実行提案 4 件の 1 つと同一内容であり、正規経路（backlog-review → RU → req-define → design-save）での確定が望ましい。制約: 追跡軸の定義そのもの（agentdev-issue-tracking Design の所有）と before payload の実装変更（実装済み）は対象外。

## Source Summary

実装が先行し Design が未追随の契約保存 gap。runner 応答 before 契約（対象操作、含める導出値、正規化規則）と、内部契約拡張の両版同時反映原則（Local 版実装完了までは最低限の型整合を維持し、VERIFY は fail-closed で verification-incomplete になることを許容）の2点を操作契約の正規所有 Design へ記載する論点。追跡軸保持 VERIFY を追加する全ケースに横展開可能な知見。

## 統合理由

1:1。単独論点。learning-promote から fix gap として渡された Design 記載要求であり、統合・分割不要。

## 要件化の方向

- `docs/designs/responsibilities/custom-tool-contracts.md`「操作契約の構成要素」節へ、runner 応答 before 契約（対象操作、導出値 state / labels / role / kind / trackingState / closeReason、正規化規則。実装 contracts.ts・tracking-schema.ts と突合）と内部契約拡張の両版同時反映原則（Local 版未完了時は型整合維持・VERIFY fail-closed 許容）を記載する。
- 同一ファイルを編集する RU-0001（移行完了後現行化）および RU-0004（91 行目散文中マーカー言い換え）と Wave 同時実行時は順次適用とする注意を要件に付記する。
