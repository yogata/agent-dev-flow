---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-21T00:00:00+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/case-run-worktree-harness-hardening.md
    type: learning
agentdev_handoff: true
---

## Sources

`case-run-worktree-harness-hardening.md`（learning）より。learning-promote 段階で4件の学習エントリ（#4 / #8 / #10 / #11）を内部統合済み。いずれも case-run の worktree・ハーネス領域に属する運用トラブル:

- **#4 Windows+junction 環境の worktree で `.opencode/` が空になる**（Issue #900 RU-3）: junction 未伝播 + `.gitignore` で `.opencode/agentdev-*` が追跙対象外のため、worktree 内の `.opencode/commands/agentdev/`・`.opencode/skills/agentdev-*/` が空ディレクトリになる。driver subagent がこの制約を知らずに検証を実行して失敗。`agentdev-workflow-orchestration` SKILL に制約記載あるが driver 引き継ぎルール不在。
- **#8 並列 Wave 実行で worktree が古い commit 基準になる**（Issue #947 / PR #950）: Wave 2 の worktree が Wave 1 の PR merge 前の commit を基準に作成され、PR #950 が DIRTY/CONFLICTING に。`case-run.md` Step 4 に `origin/main` ベース記載あるが `git fetch` 不在。
- **#10 `bunx oh-my-opencode run` が Windows でモジュール解決エラー**（Issue #955）: `Cannot find module '...oh-my-openagent\bin\oh-my-opencode.js'` エラー。bunx が Windows 版 oh-my-openagent のパッケージ構造と不一致。npx では正常動作するが references にフォールバック経路が未記載。
- **#11 oh-my-opencode ハーネスが最終検証フェーズでタイムアウト**（Issue #955）: 実装自体は完了（48ファイル変更）していたが最終検証フェーズ（grep残留確認）の途中で30分タイムアウト。ハーネスが作成した変更はコミット前にタイムアウトしたため手動コミット・PR作成が必要。タイムアウト＝即 failed とする記載はあるが、実装完了・検証未完了として扱う事後処理手順が不在。

## Source Summary

case-auto / case-run の並列 Wave 実行モデル導入後、Windows 環境・origin/main 鮮度・bunx バイナリ解決・ハーネスタイムアウトに起因する4件の運用トラブル。いずれも既存スキル・コマンドに制約記載はあるが、特定条件での適用手順が不明確だったことが原因。

## 統合理由

1:1。learning-promote 段階で4件が「case-run worktree・ハーネス手順」という共通関心で内部統合済み。外部からの再統合は二重化を招くため、本 RU は1件の learning artifact を1 RU として扱う。

## 要件化の方向

req-define への推奨方向:

- **work_type**: enhancement（既存スキル・コマンドの手順追加）
- **論点**（4本）:
  1. Windows+junction 制約の driver 引き継ぎルール化: `agentdev-workflow-orchestration/SKILL.md` に driver subagent 引き継ぎプロンプトへの制約明記を MUST として追記。`references/subagent-protocol.md` の driver 起動プロンプトテンプレートに「worktree 内 `.opencode/` は空・source/projection は手動両辺編集・同期スクリプト非依存」を必須項目として追記
  2. origin/main 鮮度確認の明示: `agentdev-git-worktree/SKILL.md` に `git fetch origin` 実行と並列 Wave 実行時の origin/main 鮮度確認を追記。`case-run.md` Step 4 に `git fetch origin` を明示
  3. bunx → npx フォールバック経路: `case-run.md` Step 5 に「bunx がモジュール解決エラーで失敗した場合は npx にフォールバック」を追記。`agentdev-case-run-execution-adapter/references/oh-my-openagent.md` の起動スクリプト例に npx フォールバック経路を追記
  4. タイムアウト事後処理手順の新設: `agentdev-case-run-execution-adapter/SKILL.md` または references に「タイムアウト＝即 failed ではなく、実装完了・検証未完了として扱う」「worktree の git status で未コミット変更を確認」「残留箇所の grep と手動修正」を規定
- **制約**: コマンドは薄く保ち、詳細はスキルへ。`agentdev-workflow-orchestration` の既存22-27行記述を置き換えず driver 引き継ぎルールを追加。source/projection 両辺編集
- **影響範囲**: `src/opencode/skills/agentdev-workflow-orchestration/SKILL.md`、`src/opencode/skills/agentdev-workflow-orchestration/references/subagent-protocol.md`、`src/opencode/skills/agentdev-git-worktree/SKILL.md`、`src/opencode/commands/agentdev/case-run.md`、`src/opencode/skills/agentdev-case-run-execution-adapter/SKILL.md`、`src/opencode/skills/agentdev-case-run-execution-adapter/references/oh-my-openagent.md`
- **完了条件候補**: 受け入れ条件9項（learning artifact 参照）。`agentdev-inspect-skills` で参照整合性エラーが検出されないこと
- **agentdev_handoff**: 本 RU は AgentDevFlow 本体の不具合・改善点であるため、現在プロジェクトでは実装しない前工程からの引き継ぎ用 RU として扱う
