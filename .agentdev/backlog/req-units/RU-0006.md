---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-22T09:12:02Z
status: draft
sources:
  - path: .agentdev/learning/promoted/existing-spec-catalog-impl-sync.md
    type: learning
  - path: .agentdev/learning/promoted/spec-docs-check-design-judgments.md
    type: learning
---

## Sources

- **SPEC catalog↔実装双方向同期運用の未文書化**: IR-044 検出関数が commit a27a8e56 で削除されたが catalog 定義（IR-044）は維持され乖離発生。今回の再実装で一致させたが、catalog定義削除時（実装も削除 or baseline_status を resolved へ）・実装削除時（catalog baseline_status を resolved へ）・実装復活時（catalog baseline_status を new へ）の双方向同期運用が integrity-rule-catalog.md に未明文化。既存 baseline_status enum・IR-020 は維持。
- **docs-check 設計判断の SPEC 未明文化**: (1) 項目役割範囲（バックエンド対象=スクリプト修正含む vs skill定義対象=含まない）、(2) 対象ファイル設計（.mdのみ・テストコード/検証スクリプト内IDパターンは正当使用例外）、(3) NG ルール間依存関係マップ、が SPEC 化されておらず checker 拡張時に毎回手動副作用評価が必要。PR #1012 で skill-category-gap NG 汚染リスクからバックエンド直接追加を回避した判断基準も未文書化。

## Source Summary

2件とも `docs/specs/integrity-rule-catalog.md` への設計判断・運用手順の追記が主対象。前者は catalog↔実装の同期運用、後者は docs-check の設計判断（役割範囲・対象ファイル・NG依存関係）。いずれも「暗黙の設計判断を SPEC レベルで明文化し checker 拡張時の手動評価コストを削減」する知見蓄積。repo-agentdev-integrity SKILL の新カテゴリ追加判定フローも補完対象。

## 統合理由

N:1 統合。2件とも同一 SPEC（integrity-rule-catalog.md）への追記であり、docs-check 設計と catalog 同期運用は integrity rule 体系の両面。個別 case 化すると SPEC 追記の整合性・重複を別 Issue で調整する手間が発生するため、1 RU で integrity rule 体系の文書化を一括扱いする。

## 要件化の方向

req-define では以下を検討する：
- integrity-rule-catalog.md へ catalog↔実装双方向同期運用手順（削除時・復活時の baseline_status 変更ルール）の追加
- 同 SPEC へ docs-check 項目役割範囲（バックエンド vs skill定義）・対象ファイル設計（.mdのみ・正当使用例外）の明文化
- 同 SPEC へ NG ルール間依存関係マップの整備
- repo-agentdev-integrity SKILL へ新カテゴリ追加判定フロー（既存 NG への副作用評価→追加可否）の追加
- 既存 baseline_status enum・IR-020 定義は維持（対象外）
- check_integrity.ts スクリプトの機能変更は含めない（文書化のみ）
- work_type 判定（feature 想定・documentation ラベル）
- 追記後の docs-check で既存カテゴリ定義が drift しないことを完了条件に設定
