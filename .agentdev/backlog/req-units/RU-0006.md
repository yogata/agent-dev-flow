---
source_type: inspect
generated_by: backlog-review
generated_at: 2026-06-23T13:13:02+09:00
status: draft
sources:
- path: .agentdev/inspect/promoted/compliance-catalog-opencode-local.md
  type: inspect
---

## Sources

- **opencode-local F-001（high）: 同一ファイル内段落完全重複**: `transform/review.md` L7-8 と L122 で「必須チェックが1つでも失敗した場合、結果は `FAIL` とする（REQ-0141-028）。部分通過（一部必須項目のみ通過）を `PASS` として扱わない。」が完全一致。
- **opencode-local F-002（high）: 同一文のファイル横断重複**: 「決定的な変換ロジックを実装した変換スクリプトは使用しない。」が `README.md` L40・`generation-flow.md` L22/L63・`transform/generate.md` L9 の 4箇所で同一表現。`generation-flow.md` 内の 2回（L22・L63）は特に近接。
- **opencode-local F-003（med）: 終端状態遷移規定の節内重複**: `case-schema/case-file.md` L70 と L95 で終端状態からの遷移不許容が重複。L70「`closed` と `cancelled` は終端状態とする。終端状態からの遷移は定義しない。」と L95「終端状態（`closed`, `cancelled`）からの遷移は定義しない。」
- **opencode-local F-005（med）: ガードレール一覧の意図的だが冗長な重複**: `transform/generate.md` L79-119 と `transform/spec.md` L43-91 で「原本保護 / 安全ゲート / 配置規則 / バックエンド制約 / GitHub 依存除去 / Case ファイル引き継ぎ」ガードレール箇条書きをほぼ同一内容で保持。両ファイルに「同一内容を保持し整合すること」と同期表明あり（意図的同期）。
- **opencode-local F-006（med）: レポートフォーマット表のファイル間重複**: `transform/generate.md` L121-138 と `transform/spec.md` L92-110 で必須項目表（12行）が同一。同期表明あり。
- **opencode-local F-007（med）: 違反判定基準表の 3箇所重複**: `transform/generate.md` L140-153・`transform/review.md` L66-79・`transform/spec.md` L111-124 で「残存 GitHub 固有参照の違反/非違反」判定表（7行）が同一。`spec.md` のみ「根拠」列追加。

## Source Summary

`src/opencode-local/` 6ファイル内で冗長構造が密集。同一ファイル内段落重複（F-001）・ファイル横断同一文重複（F-002）・節内重複（F-003）・意図的な同期表明を伴うファイル間重複（F-005/006/007）が複合的に出現。`japanese-tech-writing` スキル（冗長排除規範）に違反。F-005/006/007 は「AI エージェント参照上の設計意図」として意図的な同期表明がある点が特徴。正本を一本化し参照形式に再構成する必要があるが、AI エージェントが両ファイルを参照する運用上の利便性とのトレードオフを考慮する必要がある。

## 統合理由

1:1（1カタログ → 1 RU）。`compliance-catalog-opencode-local.md` の「後続 route 候補」が F-001〜F-007 を「まとめて1 Issue で『src/opencode-local/ 冗長排除』として整理可能」と明示。6ファイル完結の閉じたスコープであり、他ディレクトリへの影響なし。F-004（述語欠落）は RU-0004（一文一行）に含めたため本 RU からは除外。

## 要件化の方向

`req-define` では以下を確定する:

- **F-001（同一ファイル内段落重複）**: 末尾側を「再レビューでも `FAIL` の扱いとなる点は冒頭の規定通り」と短く参照させ、重複本文を削る
- **F-002（同一文のファイル横断重複）**: 正本位置（`generate.md` 目的節等）に集約し、他は「変換スクリプトは使用しない（詳細は `generate.md` 参照）」等の短い参照に。`generation-flow.md` 内 2回（L22・L63）は L63 側を削る
- **F-003（節内重複）**: L70 の一文に集約し、L95「禁止遷移」節は `blocked → closed` 直接遷移禁止のみ残す
- **F-005/006/007（意図的同期のファイル間重複）**: 正本の所在を決定（`spec.md` に寄せるか `generate.md` に寄せるか）し、他方を「`spec.md` のガードレール一覧を参照」等の短い差し替えに。AI エージェント参照上の設計意図（同期表明）は妥当性を確認の上、参照化でも運用上問題ないかを要件定義で判断
- **判定基準表（F-007）**: `spec.md` の「根拠」列を含む版を正本とし、`generate.md`・`review.md` は参照化
- **REQ-0141 整合性**: 本 RU の修正が `docs/specs/local-*.md`（正本）と矛盾しないことを確認。正本一致性は別途 inspect-docs で検証可能
- **work_type**: `docs_chore`（運用参照資料の整形・機能影響なし）。ただし F-005/006/007 の意図的同期表明の扱いは設計判断を伴うため、要件定義で方針を確定する

推奨実行順序のメモ（`depends_on` にはしない）: 本 RU は構造再編を伴うため、RU-0001/0002/0003/0004（機械的整形）の前に実行することが望ましい。
