---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-21T00:00:00+09:00
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-18-ir044-delegation-context-exemption.md
    type: intake
agentdev_handoff: true
---

## Sources

`2026-06-18-ir044-delegation-context-exemption.md`（intake）より:

- **観測**: Issue #903（RU-5）で IR-044（REQ/SPEC 境界違反検出）を既存 active REQ に稼働したところ、15件中9件が false positive。これらは SPEC 詳細ではなく「委譲先/集約先/切り出し状況の検証要件」「routing 要件の経路分類名」などで SPEC キーワード（fixture/checker 等）が現れる文脈。現行 IR-044 は否定文脈（`isNegationContext`）の exemption のみで、委譲文脈の exemption がない。
- **影響**: warning が true positive と false positive で混在し、Delta/Impact/Full Audit gate の結果判読性が低下。REQ-0136-017 の cleanup 運用で毎回9件の既知 false positive を目視除外する手間。gate 結果の信頼性低下が「通過/失敗」判断を曇らせる。
- **決定事項**: IR-044（`check_integrity.ts` の `req-spec-boundary-violation`）に「委譲/集約/切り出し/存在すること」文脈の exemption（`isDelegationContext` 等、既存 `isNegationContext` と同列）を追加するか。exemption 対象キーワードの精緻化（true positive を誤って免除しない閾値設計）。既存 exempt 9件（`.agentdev/drafts/req-spec-cleanup-plan.md`）を exemption 実装後に cleanup plan から除外できるか。

## Source Summary

IR-044 の false positive が9/15件に上り、gate 判読性と信頼性が低下。委譲/集約/切り出し文脈の exemption 拡張で true positive と false positive を分離する必要がある。

## 統合理由

1:1。integrity スクリプトの exemption ロジック拡張という独立論点。RU-0007（Step番号ガイド）と関連するが、対象が異なる（本 RU = IR-044 exemption / RU-0007 = drift 予防ガイド）。

## 要件化の方向

req-define への推奨方向:

- **work_type**: maintenance（integrity スクリプトの検出精度向上）
- **論点**:
  - IR-044 への委譲/集約/切り出し文脈 exemption 追加（`isDelegationContext` 等の述語実装）
  - exemption 対象キーワードの精緻化と true positive 保護の閾値設計
  - 既存 exempt 9件（`.agentdev/drafts/req-spec-cleanup-plan.md`）の cleanup plan 除外可否
- **影響範囲**: `.opencode/skills/repo-agentdev-integrity/scripts/check_integrity.ts`（`req-spec-boundary-violation` 実装）、`.agentdev/drafts/req-spec-cleanup-plan.md`、REQ-0136-006 / REQ-0136-017
- **完了条件候補**: 委譲文脈 exemption 実装後、既存9件の false positive が warning から消退すること。true positive が誤って免除されないことの検証（回帰テスト）
- **agentdev_handoff**: 本 RU は AgentDevFlow 本体の不具合・改善点であるため、現在プロジェクトでは実装しない前工程からの引き継ぎ用 RU として扱う
