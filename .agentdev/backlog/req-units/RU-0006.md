---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-24T00:00:00+09:00
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-24-doc-writing-mechanical-rules.md
    type: intake
---

## Sources

- Epic #1075 Wave 2（REQ-0140-026 文書品質準拠）の 4 OU（中黒 #1078、em-dash #1076、LLM 表現 #1079、一文一行 #1080）の各 PR Findings で、機械判定ルールの明文化が今後の再現性を向上させると指摘された。
- 中黒許容範囲: SPEC 明示例、時系列対比ペア（X時・Y時）、code block、YAML title の組合せで機械判定可能。これらの文脈以外の日本語並列は是正対象。
- em-dash 置換: ` — ` パターン検出で大部分をカバー。テーブルセル N/A プレースホルダ `| — |` は `| - |` への置換が自然。
- LLM 表現: 「において→で」「非常に→削除」等の表層形式置換で大部分をカバー。「最も」「重要な」等は文脈判断が必要で機械判定対象外、サンプリング査読で対応。
- 一文一行: 複数の `。` を含む prose line の改行分割で対応。括弧（`（）`）内の `。` は除外することで文境界を正確に判定。
- 既存 SPEC `document-type-responsibilities.md` 用語政策節は人間向け判定基準のみで、機械適用向けのアルゴリズム的記述がない。
- 横断是正 X-1〜X-4 および inspect-docs 自動検出の前提となるため、これらより前に整備する価値がある。

## Source Summary

文書品質の横断是正で得た 4 規範（中黒・em-dash・LLM 表現・一文一行）について、機械的に適用可能な判定アルゴリズムが未明文化のまま残っている。この状態では inspect-docs の自動検出、CI 検証、再発時の横断修正で判定基準が実行者に依存し再現性が揺らぐ。既存 SPEC は人間向け基準のみを持ち、機械適用向けの記述を欠くため、agentdev-doc-writing skill の references に機械判定ルールを明文化する必要がある。

## 統合理由

4 規範は出所が同一 Epic（#1075 Wave 2）の PR Findings で、配置先も同一スキルディレクトリ（agentdev-doc-writing references）に収束する。作業範囲が局所的で分割の利益が薄いため、1 つの RU に束ねる。LLM 表現以外は判定アルゴリズムの性質が異なるため、既存 `llm-expression-patterns.md` への集約ではなく、新規ファイル `mechanical-replacement-rules.md` に 4 規範を集約し、`llm-expression-patterns.md` は現状維持とする構成を推奨する。

## 要件化の方向

- work_type: docs_chore（skill references の新規作成・拡張）
- acceptance criteria:
  - agentdev-doc-writing skill 配下に機械的置換ルールを集約した新規参照ファイル `mechanical-replacement-rules.md` を作成。中黒許容範囲、em-dash 置換、LLM 表現機械判定、一文一行改行分割の 4 規範をアルゴリズム的に記述。
  - 機械判定対象外（「最も」「重要な」等の文脈判断が必要な表現）を明記し、サンプリング査読への委譲を明示。
  - 既存 `llm-expression-patterns.md` は LLM 表現パターンの参照として現状維持。`mechanical-replacement-rules.md` 側から相互参照を張る。
  - 明文化後、既存 OU（#1076/#1078/#1079/#1080）の修正サンプルへルールを再適用し、再現性のある判定が出ることを確認。
- priority: 中。横断是正 X-1〜X-4 および inspect-docs 自動検出の前提となる。
- dependencies/coordination: 依存 RU なし。REQ-0140-026 を根拠とする。横断是正 RU と並行または先行して着手する。
