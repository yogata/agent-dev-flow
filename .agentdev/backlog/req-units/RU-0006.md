---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-14T23:41:14Z
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/intake/promoted/2026-07-15-case-close-gh-direct-invocation.md
    type: intake
---

## Sources

- **case-close-gh-direct-invocation（intake）**: `.opencode/commands/agentdev/case-close.md` L120 および L161 に `gh pr view` の直接呼び出しが存在（2箇所）。IR-053（gh CLI 委譲）は `agentdev-gh-cli` skill への委譲を要求。REQ-0152-001（gh CLI 直接呼び出し禁止）違反。CanonicalConflict heuristic で WARNING 2件。直接呼び出しによる command 局所化リスク（エラーハンドリング、引数バリデーションの不統一）。

## Source Summary

case-close command に `gh pr view` 直接呼び出しが残留。IR-053 / REQ-0152-001 が要求する `agentdev-gh-cli` skill 委譲への変更が必要。

## 統合理由

1成果物1論点のため 1:1。case-close 固有の gh CLI 委譲問題であり、独立した是正単位。

## 要件化の方向

req-define では以下を検討する方向:

1. `case-close.md` L120, L161 の `gh pr view` 直接呼び出しを `agentdev-gh-cli` skill 委譲の表現に変更
2. 委譲後のエラーハンドリング、引数バリデーションが agentdev-gh-cli 側で統一されていることを確認

route: command fix。IR-053 / REQ-0152-001 領域。
