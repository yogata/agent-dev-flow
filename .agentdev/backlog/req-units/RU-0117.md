---
id: RU-0117
title: "case-open Definition PR 期待値宣言前の branch HEAD 実測前置（check_integrity・autogen 鮮度・traceability による期待値確定）"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-21
status: draft
work_type: docs_chore
depends_on: []
agentdev_handoff: true
tentative_classification: guide
sources:
  - .agentdev/learning/promoted/countermeasure-update-definition-pr-expectation-measured-baseline.md
---

# RU-0117: case-open Definition PR 期待値宣言前の branch HEAD 実測前置（check_integrity・autogen 鮮度・traceability による期待値確定）

## Sources

- **learning: Definition PR 期待値宣言と merge 後実測の乖離（fix gap）**: 第16段 RC fixes（Case #3042）の case-ready で、Definition PR #3043 は PR 本文期待値「check_integrity NG セット baseline 完全一致・autogen 0」を宣言して merge されたが、merge 後受入検査（@97953f3e）で NG 54→56・autogen 鮮度違反 0→1 を観測した（REQ 行追加 REQ-001-069 と Design frontmatter キー列挙文言が checker 検出パターンに掛かった）。PR 作成時検証が UTF-8 健全性・期待内容出現回数の文字列検証にとどまり、branch HEAD 全体の機械チェックが実行されていないため。`definition-pr-and-idempotency.md` に branch HEAD 実測前置は不在（check_integrity 記述なしを機械確認済み。2026-09-21 時点でも再確認: check_integrity 記述なし）。case-open deviation capture（commit c8d33c8e）由来。

## Source Summary

case-open の Definition PR 作成手順に「PR 本文へ期待値を宣言する前に branch HEAD 実測で check_integrity・check_autogen_freshness・traceability を実行し、期待値を実測値で確定する」前置を追加する。文字列検証は前提として維持し、tree 全体の機械チェックをその前に実行する順序を明示する。期待値宣言の正確性が merge 後でしか検証できない状態を解消する。

## 統合理由

1:1。単一成果物・単一配布 reference（definition-pr-and-idempotency.md）への手順追記が主体で、分割すべき複数論点を持たない。RU-0118（case-close 側 AUTOGEN gate 運用）とは関心・対象ファイルが独立（case-open の PR 作成手順 vs case-close の境界 close 検査）。同一 Case #3042 に由来するが修正対象・受け入れ条件は独立しており依存なし。

## 要件化の方向

`src/opencode/skills/agentdev-workflow-case-open/references/definition-pr-and-idempotency.md` の Definition PR 作成手順へ期待値実測前置（branch HEAD での check_integrity・check_autogen_freshness・traceability 実行と期待値の実測確定）を追記する。`root-case-and-definition-package.md` の Root Case 本文受入検査節への期待値由来（実測）明記は検討候補として含める。

制約: checker 実行は case-open の実行形態契約（bun 実行ランナー・repo root 起 cwd）に従う。baseline 比較の前提（baseline @60120646 detached worktree 等の参照先）は既存の増減理由型記録運用を維持する。期待値実測の実行により case-open の所要時間は増加するため、実行対象を REQ 行追加・Design 変更を伴う Definition PR に限定する判断は req-define が行う。

受け入れ条件: (1) PR 本文期待値の宣言前に branch HEAD 実測で期待値を確定する前置が記載されていること (2) 前置が既存の文字列検証と排他的でなく、その前に実行する順序として明示されていること。checker 実装（check_integrity・check_autogen_freshness・traceability）は既存実装を利用するのみで変更しない。
