---
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-03
status: draft
tentative_classification: 挙動Design
sources:
  - .agentdev/intake/promoted/2026-08-20-extractyamlfield-regex-yaml-dual-path.md
---

# extractYamlField の YAML 解析二重経路の解消判断

## Sources

| 源 | 要点 |
|---|---|
| `.agentdev/intake/promoted/2026-08-20-extractyamlfield-regex-yaml-dual-path.md` | check_workflow_preventive.ts の extractYamlField（正規表現による単一トップレベルフィールド抽出）が extensions YAML から kind/id を抽出。Issue #2352（OU-001）の移行対象からは意図的に除外済みだが「正規表現と Bun.YAML の2経路」と見なせる余地があり、PR #2355 では判定 ambiguous として記録のみ |

## Source Summary

Project Extensions YAML 入力からのフィールド抽出の解析経路が2種残存する解釈余地があり、RU-0002「二重経路残存なし」の解釈次第で移行要件になり得る。現状の機能動作への影響はなく、統合するか対象外として根拠明示するかの判断が保留されている。

## 統合理由

1:1。単独源。近接対として検定義 yaml × checker の二重管理（→ RU-0005）があるが、本件は解析実装経路（正規表現 vs Bun.YAML）の統合判断であり、対象・根因・解決方向（共有 lib 再利用 or 対象外明示）が異なるため分離した。

## 要件化の方向

req-define で「extractYamlField を共有 lib の resolveExtensionState（Bun.YAML 委譲実装）再利用へ統合するか、意図的な移行対象外として根拠を明示するか」を要件化する。統合する場合は checker 実装変更を、対象外とする場合は RU-0002「二重経路残存なし」の解釈に対する根拠文書（Design 記載）を artifact_actions 候補とする。
