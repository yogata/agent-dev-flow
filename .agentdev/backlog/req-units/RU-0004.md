---
source_type: learning
generated_by: backlog-save
generated_at: 2026-06-05T12:00:00+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/command-merge-retry-on-conflict.md
    type: learning
  - path: .agentdev/learning/promoted/existing-command-parallel-pr-merge-retry.md
    type: learning
---

## Sources

- **command-merge-retry-on-conflict.md**: Epic Orchestrator等で複数PRを逐次squash mergeする際、先行PRのマージがbase branchを更新し後続PRが「base branch was modified」エラーで失敗。3件の事象を確認。自動リトライ（5秒間隔・最大5回）とrebase手順の標準化が必要。
- **existing-command-parallel-pr-merge-retry.md**: 並行PR開発で同一ファイル・同一base branchを変更する複数PRの逐次マージが失敗する反復パターン。リトライ全滅時は`git rebase origin/main` → コンフリクト解消 → `git push --force-with-lease`（`--force`は禁止）→ マージ再試行のワークフローが必要。

## Source Summary

2件のlearningがともに並行PR逐次マージ時のコンフリクト・base branch更新によるマージ失敗を指摘。Gはrebase workflow中心の記述、Hは`--force-with-lease`推奨と`--force`禁止の制約を明記。HがGの制約を強化する方向で矛盾なし。統合により、自動リトライ→rebase→force-with-lease→再マージの標準リカバリワークフローを定義可能。

## 統合理由

2:1 N:1統合。G・Hはともにcase-close Step 4のマージリトライ機構の欠如を指摘。反映先が同一（case-close Step 4 + orchestration references）。Hの`--force-with-lease`制約がGのrebase workflowを安全に強化する方向であり、矛盾なし。統合で完全なリトライ→リカバリワークフローを定義可能。

## 要件化の方向

case-close Step 4に以下を追加:
- squash merge失敗時の自動リトライ（5秒間隔・最大5回）
- リトライ全滅時のrebaseワークフロー: `git rebase origin/main` → コンフリクト解消 → `git push --force-with-lease` → マージ再試行
- MUST NOT: `git push --force`（--force-with-lease以外のforce push）は禁止
- 並行Wave実行で共通変更ファイルの重複がある場合の事前警告推奨
- workflow-orchestration referencesのエラー回復マップにマージ失敗パターンを追加
