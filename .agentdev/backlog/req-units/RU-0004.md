---
source_type: chat
generated_by: session
generated_at: 2026-06-16T00:00:00+09:00
status: draft
depends_on:
  - RU-0003
sources:
  - path: chat://2026-06-16-req-reorganization-analysis
    type: chat
---

# req/case/intake/learning/backlog系command REQの分割・再配置

## Sources

調査結果:

- REQ-0102 は `req-define` と `req-save` を抱え、OU/draft schemaまで含む。
- REQ-0105 は intake / learning / backlog / RU lifecycle を包括して大きすぎる。
- REQ-0106 は `case-run` と `case-close` を同一REQで扱い、case-openとの断絶がある。
- `case-update` は command README には存在するが、明確なcommand-level REQが弱い。
- REQ-0114 は `case-auto` command REQとして妥当だが、既存command詳細の再掲が多い。

## Source Summary

req/case/intake/learning/backlog系は、command単位の責務・入力・出力・副作用境界に分割する必要がある。

## 統合理由

ユーザーが指摘した「コマンド単位のREQになっていない」問題の中核。後続の検査・索引更新より前に、command ownershipを正す必要がある。

## 要件化の方向

対象:

- REQ-0102
- REQ-0104
- REQ-0105
- REQ-0106
- REQ-0107
- REQ-0114
- `src/opencode/commands/agentdev/*.md`
- `docs/specs/workflow-contracts.md`

実施方針:

- `req-define` と `req-save` のREQ責務を分離または明確に区画する。
- `case-open` を command-level REQ として明確化し、REQ-0104内のcase-open詳細を移す。
- `case-run` と `case-close` を分割し、完了ゲート、PR/Issue、post-run capture、worktree cleanup の所有先を明確化する。
- `case-update` のcommand-level REQを追加または既存REQに明示的に追加する。
- REQ-0105を以下に分割する。
  - intake-capture / intake-from-github
  - intake-promote
  - learning-promote
  - backlog-review
  - RU lifecycle
- REQ-0114は case-auto の orchestration / stopping boundary / input resolution に縮約し、各工程詳細は各command REQへ委譲する。

受け入れ条件:

- 15公開commandのうち、`/agentdev/*` 各commandが明確なREQ所有先を持つ。
- command REQに SPEC schema や内部ファイル構造の詳細が過剰に残っていない。
- `case-open` / `case-run` / `case-close` / `case-update` の責務が断絶せず、主フロー内で接続している。
- REQ-0105 が intake/learning/backlog/RU lifecycle を一体で抱え込まない。
