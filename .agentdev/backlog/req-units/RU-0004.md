---
source_type:
  - intake
  - learning
generated_by: backlog-review
generated_at: 2026-08-15T18:35:06+09:00
status: draft
depends_on: []
tentative_classification: REQ
sources:
  - type: intake
    path: .agentdev/intake/promoted/2026-08-15-check-templates-dryrun-worktree-failures.md
  - type: intake
    path: .agentdev/intake/promoted/2026-08-15-lint-skills-src-fallback-missing.md
  - type: learning
    path: .agentdev/learning/promoted/spec-candidate-guard-scope-exclusion-rules.md
---

# worktree 環境依存解消と guard/checker 検出対象除外規定

## Sources

- **2026-08-15-check-templates-dryrun-worktree-failures.md（intake）**: check_templates.ts の --dry-run 系テスト3件が base ブランチから失敗しており、worktree 配置の templates 参照解決が環境依存になっている。worktree 環境でテストが恒常失敗し、新規失敗との判別が都度手作業になっている。パス解決の修正または環境差を吸収する test 設計への変更が求められる。
- **2026-08-15-lint-skills-src-fallback-missing.md（intake）**: lint_skills.ts は走査ルートを `.opencode/skills` に固定しているため、worktree（ジャンクション未伝播）環境では配布 Skill を走査できない。skills_structure.test.ts / commands_structure.test.ts は同種問題に src/ フォールバックで対応済みであり、同等のパス解決実装が求められる。
- **spec-candidate-guard-scope-exclusion-rules.md（learning）**: guard / checker が検出対象の除外規定を持たないため、3種の false positive・判定揺れが発生した。(1) 非 SPEC ファイル（baseline snapshot 等）が targeted docs guard に SPEC README 登録候補として誤検出される（PR #2084）。(2) IR 廃止・MERGE 時の歴史的言及が機能的残存と区別されない（PR #2089、DEC-013 AG-008 の適用範囲）。(3) Windows + junction 環境の worktree で `.opencode/skills/agentdev-*` が空洞化し checker が false positive を出す（PR #2090）。対象外節で check-templates dry-run 系 intake item との統合を明示申告している。

## Source Summary

worktree（Windows + junction）環境での検査・テストの環境依存失敗と、guard/checker の検出対象範囲規定（何を除外するか）の不在が組み合わさり、検査の自動化価値と実効性が失われている。

## 統合理由

learning 成果物自身が対象外節で worktree 系 intake item（check-templates dry-run 3件）との統合を申告している。

3件とも「検査・テストの worktree 環境依存と検出対象範囲規定」という同一関心に属する。learning 成果物の制約節（3つの単発顕現を単一の予防策族「対象外規定の明文化」に集約した構成）を維持する。

source_type は混成（intake 2、learning 1）のため列表記 `[intake, learning]` とする。出典の正は `sources[].type` であり、source_type の正規スキーマ定義は不在のため本形式を統合理由内で宣言する（経路E review A-F2 の反映）。

## 要件化の方向

1. lint_skills.ts に兄弟テストと同じ src/ フォールバック（または同等のパス解決）を実装する
2. check_templates.ts に worktree 空洞化検知時の warning/skip を実装する
3. targeted docs guard が frontmatter または配置ディレクトリに基づく SPEC 判定を行い、非 SPEC ファイルの `spec_readme_update_required` フラグを抑止する（または SPEC README 登録対象基準を明文化する）
4. 歴史記録ファイル（docs/specs/integrity/audits/、baselines/ 等）を残存参照判定の対象外とする規定を DEC-013 AG-008 適用範囲または TS-017 判定基準へ明記する

対象スクリプト check_changed_docs.ts の実行契約（モード使い分け、引数形式、起動手段）は RU-0007 が扱う。両 RU で対象が重複するため、req-define で配備先（integrity SPEC 系）の一貫性を保つこと（経路E review B-F5 の連携注記）。
