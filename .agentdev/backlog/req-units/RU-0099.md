---
id: RU-0099
title: "新規 REQ CREATE を含む req-define draft の traceability/policy.yaml 検証対応要否判断の組込み規則"
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-18
status: draft
work_type: feature
depends_on: []
agentdev_handoff: true
tentative_classification: REQ
sources:
  - .agentdev/intake/promoted/2026-09-17-req-define-catalog-registration-in-artifact-actions.md
---

# RU-0099: 新規 REQ CREATE を含む req-define draft の traceability/policy.yaml 検証対応要否判断の組込み規則

## Sources

- **intake: draft 生成規則に policy 登録判断が不在（前提現行化済み）**: Case #2898（REQ-083 CREATE）で req-define draft に verification-scope-catalog への当該 REQ 節登録が artifact_actions に含まれず、case-ready の検証対応要否ゲートで未分類と判定され ready 未遷移停止、後追い再合意により Definition Amendment PR #2900 で補完した（旧モデルの事象）。前提現行化: verification-scope-catalog は DEC-030（accepted 2026-09-17）で廃止・物理削除済み、検証対応要否は traceability/policy.yaml へ移行（未指定=required、任意行は明示登録制、Case #2936 Wave 4-1/PR #2952 で移行完了）。現行モデルでの再定式化:「新規 REQ CREATE を含む draft は traceability/policy.yaml への検証対応要否判断（optional 登録または required 許容）を artifact_actions に含める規則」。根底の failure mode は現行モデルでも反転存続する（未指定=required のため、policy 判断を含めない新規 REQ CREATE の Definition は case-ready トレーサビリティ完全性ゲートで fail し得る）。現行 req-define Design の artifact_actions 生成規則に policy 登録の規定は grep レベルで確認できない（規則追加要否の判断時に req-define Design の全文精査を要する）。

## Source Summary

新規 REQ CREATE を含む draft が policy.yaml への検証対応要否判断を含まない場合、case-ready のトレーサビリティ完全性ゲートで停止し Amendment PR による後追い補完コストが発生する構造が、旧カタログモデルから policy.yaml モデルへも引き継がれている。draft 生成規則（artifact_actions）への判断組込み要否を検討する。

## 統合理由

1:1（統合・分割不要）。単一論点（req-define draft の生成規則）。前提の陳腐化（catalog 廃止）は intake-promote review で現行化済み。

## 要件化の方向

req-define Design の全文精査（policy.yaml 登録規定の有無）を前置した上で、「新規 REQ CREATE を含む draft は policy.yaml への検証対応要否判断（optional 登録または required 許容）を artifact_actions に含める」規則の追加要否と、case-open/case-ready の冪等再実行との整合を判断する。実現先候補: req-define の draft 生成規則（REQ 系実行契約）、case-open/case-ready 品質検査。
