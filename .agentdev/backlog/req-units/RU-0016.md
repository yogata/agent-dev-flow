---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-25T08:18:02+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/skill-gh-cli-windows-write-encoding.md
    type: learning
---

## Sources

- **Windows 環境で Write ツールが既存 UTF-8 ファイルを cp932 で書き出す問題の是正**（学習元: L-005、PR #1085 / #1077 OU-005）。
  - Windows 環境で OpenCode の Write ツールが既存 UTF-8（BOM なし）ファイルを上書きする際に cp932（Shift-JIS）で書き出す事象を実証。`docs/requirements/README.md` 編集時に発生し edit ツールで回避。
  - `agentdev-gh-cli/references/standard-procedures.md` L57 は「OpenCode の Write tool も使用可能（BOMなしUTF-8で書き出す）」と記載するが、既存ファイル上書き時の cp932 化事象と矛盾し、誤った安全保証を与えている。
  - 望ましい変更: L57 を「既存 UTF-8 ファイル編集時は edit ツール（per-line string replace）を優先、Write ツール全面上書きは新規ファイル作成時に限定」へ修正。AGENTS.md 常時ルール候補（Windows 既存 UTF-8 編集時 edit 優先）の検討。
  - 制約: 新規ファイル作成時の Write は引き続き許可。edit ツールは大規模構造変更に不向き（その場合は `[System.IO.File]::WriteAllText` with `UTF8Encoding($false)` を使用）。

## Source Summary

standard-procedures.md L57 の Write ツール許可記載が事実と矛盾（既存ファイル上書きで cp932 化）。既存ファイル編集時 edit 優先・新規作成時のみ Write 許可へ修正し、AGENTS.md 常時ルール候補を検討する。

## 統合理由

1:1（特別な統合・分割不要）。単一 learning 成果物が standard-procedures.md 記載是正を扱うスコープとして完結。

## 要件化の方向

req-define では以下を壁打ちする。

- `standard-procedures.md` L57 の Write ツール許可記載を、既存ファイル編集時 edit 優先・新規ファイル作成時のみ Write 許可に修正する。
- 修正後の記載が新規ファイル作成時の Write 許可を維持する。
- AGENTS.md 常時ルール候補（Windows 環境で既存 UTF-8 ファイル編集時の edit ツール優先）を採用するか、判断根拠とともに文書化する。
- work_type 想定: fix（事実矛盾の記載是正）。推奨ラベル: bug, documentation。対象: standard-procedures.md L57 / AGENTS.md。
