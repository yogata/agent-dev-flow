---
slug: doc-granularity-model
source_type: chat
generated_by: session
generated_at: 2026-06-26
status: draft
sources:
  - type: chat
    path: session:2026-06-26-doc-granularity-model
---

# Requirements Unit: doc-granularity-model

## Sources

### セッション: 2026-06-26-doc-granularity-model

- 発端: intake/learning 由来で REQ にエッジケース要件、SPEC に細かすぎるルールが書かれすぎる懸念。SPEC を挙動仕様とカタログに分ける検討。
- 計画: `.omo/plans/doc-granularity-model.md`（15 todos, 4 waves）
- Resume point: `.omo/drafts/doc-granularity-model.md`
- 合意形成: Metis レビュー（CRITICAL2/MAJOR6/MINOR8 反映済み）、別エージェント分析統合（doc-granularity-model.handoff.md）
- 核心診断: 規範は既に成熟（document-model.md SPEC分離基準 REQ-0101-068/069、粒度判定テスト REQ-0102-054、req-health-metrics 定量閾値）。問題は (a) パイプラインが規範を強制できない、(b) SPEC 内部の挙動/カタログ双方向混入。
- ステータス: 承認済み（2026-06-26）、ユーザー決定 Q1=a/Q2=a/Q3=a（論理区分/2点必須ゲート/段階移送）

## Source Summary

セッション合意済みの要件定義文書粒度モデル改善の8項目:

1. **5区分モデル**: SPEC 内部分類を従来4区分から5区分へ拡張（挙動SPEC/カタログSPEC/横断contract SPEC/索引SPEC/詳細SPEC）。従来の workflows/ 層が横断contract SPEC に対応。
2. **粒度ゲート必須化**: req-define Step5-2 拡張（7分類化）と req-save/spec-save 保存前検証の2点で粒度を強制。intake/learning/backlog は軽量ガイドライン参照のみ。
3. **7分類**: 従来5分類に learning維持・対象外を追加（REQ/挙動SPEC/カタログSPEC/guide/learning維持/作業記録/対象外）。
4. **分類候補→確定原則**: intake/learning 由来 RU は backlog-review 時点で分類候補とし、req-define で分類を確定する。
5. **learning自動REQ化禁止**: learning の再発防止知見を恒久契約に昇華できる場合のみ REQ 化。無条件 REQ 化を禁止。
6. **設計原則（レポジトリ種別非区別）**: agentdev-* コマンド・スキル（src/*）は AgentDevFlow レポジトリと適用レポジトリ（consumer）を区別しない。5区分は各レポジトリの `document-model.md`（docs/）が定義する規範で、共通コマンドは5区分をハードコードせず document-model.md の SPEC分離基準を参照。AgentDevFlow 本体の思想（docs/）は agent-dev-flow 固有・配布対象外（ADR-0103）。唯一の例外: intake/learning のキャプチャ内容が AgentDevFlow 本体（アップストリーム還元）か consumer プロジェクトかの区別。
7. **段階移送**: 新規/更新分から適用。既存 SPEC は段階移送（IR-044 詳細と代表 command SPEC を先行）。
8. **局所物理分離許容**: *-rules.md 併設、integrity-rules/ サブディレクトリのみ許容。docs/specs/behavior|catalog/ への全面再配置は禁止。既存3層構造（commands/skills/workflows/直下）を維持。

## 統合理由

セッション由来の採用済み成果物（計画ファイル）を要件定義（req-define）へ引き継ぐため。この RU は1:1 変換（1計画→1RU）。既存の探索結果統合（handoff）と Metis レビュー、別エージェント分析を経て合意形成済みのため、統合・分割判定は不要。

## 要件化の方向

以下の8要件を req-define で要件定義（REQ/ADR/SPEC）に変換する:

1. SPEC 内部5区分モデルの定義（document-model.md への追加）
2. 粒度ゲートの2点必須化（req-define Step5-2 拡張 + req-save/spec-save 保存前検証）
3. 7分類への拡張（learning維持・対象外追加）
4. 分類候補→確定原則の明文化（backlog-review → req-define の流れ）
5. learning自動REQ化禁止の実装
6. 設計原則（レポジトリ種別非区別）の明文化と共通コマンド実装
7. 段階移送の実行計画（IR-044 詳細分離と代表 command SPEC の *-rules.md 移送を先行）
8. 局所物理分離の許容条件明確化（*-rules.md, integrity-rules/ のみ許容、全面再配置禁止）