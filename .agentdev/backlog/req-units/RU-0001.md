---
source_type: learning
generated_by: backlog-save
generated_at: 2026-06-05T12:00:00+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/skill-gh-cli-template-literal-quoting.md
    type: learning
  - path: .agentdev/learning/promoted/existing-skill-nodejs-template-literal.md
    type: learning
---

## Sources

- **skill-gh-cli-template-literal-quoting.md**: Windows環境で`node -e`にテンプレートリテラル（バッククォート）を含めるとPowerShellのクォート解釈と競合してパースエラーになる。既存Section 3項目5-8にクォート回避策はあるが、テンプレートリテラルの言及がない。インライン`node -e`内でのテンプレートリテラル禁止と配列join推奨が必要。
- **existing-skill-nodejs-template-literal.md**: テンプレートリテラル内の`${...}`式補間が中括弧パス（`{command}`, `{variant}`等）と競合し`ReferenceError`が発生する。既存MUST NOTルールはsingle quote・pipe問題をカバーするが、バックティックの`${...}` vs brace競合は未カバー。配列`join("\n")`または文字列結合への替代推奨が必要。

## Source Summary

2件のlearningがともに`node -e`インライン実行時のテンプレートリテラル（バッククォート）使用に起因する問題を指摘。AはPowerShellのクォート階層競合、BはJavaScript式補間`${...}`と中括弧パスの競合という異なる発火ポイントだが、根本原因は同一（インライン`node -e`内でのテンプレートリテラル使用禁止の欠如）。既存Section 3のクォート競合回避策にテンプレートリテラルの具体的ガイダンスを追加し、配列join推奨を明記することで両方の問題を解決可能。

## 統合理由

2:1 N:1統合。A・Bはともにagentdev-gh-cli SKILL.md Section 3のクォート競合回避策のギャップを指摘。禁止対象（`node -e`内テンプレートリテラル）と推奨替代（配列join）が同一であり、反映先も同一ファイルの同一セクション。補完関係のため統合が適切。

## 要件化の方向

agentdev-gh-cli SKILL.md Section 3（Windows回避策）に以下を追加:
- MUST NOT: `node -e`インライン内でのテンプレートリテラル（バッククォート）使用を禁止
- 推奨: 配列の`join("\n")`または`+`による文字列結合を代替として使用
- 注意: `.js`スクリプトファイル内でのテンプレートリテラル使用は禁止しない（クォート競合が発生しないため）
- 複雑な文字列処理は外部`.js`ファイルに退避する推奨を併記
