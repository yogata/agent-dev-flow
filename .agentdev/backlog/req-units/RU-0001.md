---
slug: doc-granularity-model
source_type: chat
generated_by: session
generated_at: 2026-06-26
status: draft
sources:
  - type: chat
    path: session:2026-06-26-doc-granularity-model
---

# Requirements Unit: doc-granularity-model

## Sources

### セッション: 2026-06-26-doc-granularity-model

- 発端: intake/learning 由来で REQ にエッジケース要件、SPEC に細かすぎるルールが書かれすぎる懸念。SPEC を挙動仕様とカタログに分ける検討。
- 計画: `.omo/plans/doc-granularity-model.md`（15 todos, 4 waves）
- Resume point: `.omo/drafts/doc-granularity-model.md`
- 合意形成: Metis レビュー（CRITICAL2/MAJOR6/MINOR8 反映済み）、別エージェント分析統合（doc-granularity-model.handoff.md）
- 核心診断: 規範は既に成熟（document-model.md SPEC分離基準 REQ-0101-068/069、粒度判定テスト REQ-0102-054、req-health-metrics 定量閾値）。問題は (a) パイプラインが規範を強制できない、(b) SPEC 内部の挙動/カタログ双方向混入。
- ステータス: 承認済み（2026-06-26）、ユーザー決定 Q1=a/Q2=a/Q3=a（論理区分/2点必須ゲート/段階移送）
- 二層構造（2026-06-26 更新）: RU-0001 は「AgentDevFlow共通の仕組み（第1層）」と「このリポジトリへの初回適用（第2層）」に整理。req-define へ丸投げせず、RU 段階で層を明示。第2層は恒久REQ化せず是正タスク（case-run 等）として扱う。

## Source Summary

セッション合意済みの要件定義文書粒度モデル改善。RU-0001 は二層構造で整理（AgentDevFlow共通の仕組み + このリポジトリへの初回適用）。req-define は両層を区別して要件化する。

### 第1層: AgentDevFlow共通の仕組み

1. **SPEC内部5区分（論理区分）**: SPEC は文書種別として維持し、内部を挙動SPEC/カタログSPEC/横断contract SPEC/索引SPEC/詳細SPECに論理区分。従来の workflows/ 層が横断contract SPEC に対応。
2. **粒度ゲート2点必須化（共通パイプライン）**: req-define Step5-2 拡張（7分類化）と req-save/spec-save 保存前検証の2点で粒度を強制。intake/learning/backlog は軽量ガイドライン参照のみ。
3. **7分類（共通分類モデル）**: REQ/挙動SPEC/カタログSPEC/guide/learning維持/作業記録/対象外。
4. **分類候補→確定（共通RU運用）**: intake/learning 由来 RU は backlog-review 時点で分類候補、req-define で分類確定。
5. **learning自動REQ化禁止（共通運用）**: learning 知見は恒久契約に昇華できる場合のみREQ化。無条件REQ化禁止。
6. **レポジトリ種別非区別（共通コマンド設計原則）**: agentdev-* コマンド・スキル（src/*）は AgentDevFlow レポジトリと適用レポジトリ（consumer）を区別しない。5区分は各レポジトリの `document-model.md`（docs/）が定義する規範で、共通コマンドは5区分をハードコードせず document-model.md の SPEC分離基準を参照。AgentDevFlow 本体の思想（docs/）は agent-dev-flow 固有・配布対象外（ADR-0103）。唯一の例外: intake/learning のキャプチャ内容が AgentDevFlow 本体（アップストリーム還元）か consumer かの区別。
7. **局所物理分離の許容原則（共通原則）**: *-rules.md 併設、integrity-rules/ サブディレクトリを許容。docs/specs/behavior|catalog/ への全面再配置は禁止。既存3層構造（commands/skills/workflows/直下）を維持。具体配置は各レポジトリの document-model.md 次第。

### 第2層: このリポジトリ（agent-dev-flow）への初回適用

1. **既存REQ/SPECの初回棚卸し**: agent-dev-flow 本体の既存 REQ/SPEC を棚卸し。REQ 内の SPEC 詳細混入（enum/行数上限/内部フェーズ等）を確認。
2. **integrity-rule-catalog.md の肥大化確認**: 詳細肥大化を確認し、integrity/rules/ への分離候補を特定。
3. **IR-044 詳細の先行移送候補化**: IR-044（REQ/SPEC 境界違反）の詳細を先行移送候補にする。
4. **代表command SPEC の詳細分離候補化**: 代表 command SPEC の field/schema/rule 詳細を先行移送候補にする。
5. **既存SPECの段階移送（一括禁止）**: 既存 SPEC の段階移送は一括ではなく inspect/backlog 経由で扱う。新規/更新分から適用。

## 統合理由

セッション由来の採用済み成果物（計画ファイル）を要件定義（req-define）へ引き継ぐため。この RU は1:1 変換（1計画→1RU）。既存の探索結果統合（handoff）と Metis レビュー、別エージェント分析を経て合意形成済みのため、統合・分割判定は不要。

## 要件化の方向

req-define は RU-0001 の二層構造に従い、共通仕組み（第1層）とこのリポジトリの初回是正（第2層）を区別して要件定義（REQ/ADR/SPEC）に変換する。

### 第1層: AgentDevFlow共通の仕組み（req-define で共通 REQ/SPEC/ADR に変換）

1. SPEC 内部5区分（論理区分）の定義（各レポジトリの document-model.md への参照基準として）
2. 粒度ゲート2点必須化（req-define Step5-2 拡張 + req-save/spec-save 保存前検証）
3. 7分類への拡張（learning維持・対象外追加）
4. 分類候補→確定原則の明文化（backlog-review → req-define の流れ）
5. learning自動REQ化禁止の実装
6. 設計原則（レポジトリ種別非区別）の明文化と共通コマンド実装
7. 局所物理分離の許容原則の明確化（*-rules.md, integrity-rules/ のみ許容、全面再配置禁止、具体配置は各レポジトリ次第）

### 第2層: このリポジトリへの初回適用（恒久REQ化せず、是正タスクとして扱う。case-run 等で実行）

8. 既存REQ/SPECの初回棚卸し（REQ内SPEC詳細混入の確認と是正）
9. integrity-rule-catalog.md の肥大化解消（integrity/rules/ への分離）
10. IR-044 詳細の先行移送（候補→実行）
11. 代表command SPEC の詳細分離（候補→実行）
12. 既存SPECの段階移送（inspect/backlog 経由、一括禁止）

**注意**: 第2層は agent-dev-flow 本体の既存docsへの初回是正であり、恒久要件（REQ）として残さない。REQに「IR-044詳細移送」や「代表command SPEC移送」が恒久要件として混入するのを防ぐため、第2層は是正タスク（case-open/case-run で実行する一時作業）として扱う。