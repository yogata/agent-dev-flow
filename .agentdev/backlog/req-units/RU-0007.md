---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-22T09:12:02Z
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-21-issue1000-integrity-reports-gitignore-missing.md
    type: intake
---

## Sources

- **.agentdev/integrity/reports/ が .gitignore に未記載**: `.agentdev/README.md` 状態表で `integrity/reports/*.md` の Retention を「非永続・git管理対象外（`.gitignore` で除外）」と記載しているが、リポジトリ `.gitignore` に `.agentdev/integrity/reports/` エントリが未記載。設計判断（README）と実装（.gitignore）の乖離。検証レポートが意図せず git 管理対象になるリスク。他 `.agentdev/` サブディレクトリでの同種乖離の潜在確認も必要。

## Source Summary

単独成果物。`.agentdev/README.md` の設計判断（integrity/reports は非永続・git除外）と `.gitignore` 実装の乖離を是正する。docs-check 等の整合性検査で偽陽性/偽陰性を生む原因の除去。

## 統合理由

1:1。単独の小規模是正（.gitignore 1エントリ追加）であり、他成果物との関心領域重複なし。RU-0008（case-auto/open/close 状態管理）も .agentdev/ git ライフサイクルを扱うが、適用範囲（.gitignore vs コマンド手順）とリスクプロファイル（1行修正 vs プロセス改修）が大幅に異なるため統合しない。

## 要件化の方向

req-define では以下を検討する：
- `.gitignore` へ `.agentdev/integrity/reports/` エントリ追加
- `.agentdev/README.md` の記載を維持するか、実態に合わせて表現を緩和するか
- 他 `.agentdev/` サブディレクトリでの「設計判断↔実装乖離」の潜在確認
- work_type 判定（docs_chore 想定）
- 修正後 `git check-ignore .agentdev/integrity/reports/test.md` で除外確認を完了条件に設定
