---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-14T23:41:14Z
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/intake/promoted/2026-07-05-case-close-docs-guard-base-ref-false-clean.md
    type: intake
---

## Sources

- **case-close-docs-guard-base-ref-false-clean（intake）**: case-close Step 3-1 で実行する targeted docs guard（`check_changed_docs.ts`）が、main ワークツリー上での `--base-ref` 指定で `files_checked: []` を返し、変更ファイルがあるのに検査対象ゼロの false-clean になる事象。発生メカニズム: `check_changed_docs.ts` は `git diff <base-ref>..HEAD` で変更ファイルを算出するが、case-close は main ワークツリーで実行され PR ブランチを checkout しないため HEAD == merge-base となり diff が空になる。結果として `files_checked: []`・`failures: []` が返り、検査を実施していないのに pass と見える。実例: Issue #1425 / PR #1426 の case-close で発生。`--files` 明示に切り替えて再実行し回避。

## Source Summary

case-close の docs guard が、main ワークツリー実行に起因する `--base-ref` の空 diff で false-clean になる。検査が実行されたように見えて実際にはスキップされるリスクがある。

## 統合理由

1成果物1論点のため 1:1。check_changed_docs.ts の report フィールド欠落（RU-0005）とは別問題（false-clean の予防 vs フィールド欠落）のため分離。

## 要件化の方向

req-define では以下を検討する方向（組合せ可）:

1. case-close.md Step 3-1 の起動例を `--files <PR 変更ファイル明示>` を標準とする
2. `check_changed_docs.ts` が `--base-ref` 使用時 `files_checked` が空なら warning を出す
3. case-close 手順に「`files_checked` が空でないことの確認」ステップを追加

route: command fix + スクリプト改良。REQ-0158-003（targeted docs guard）領域。
