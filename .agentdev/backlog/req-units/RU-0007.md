---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-24T00:00:00+09:00
status: draft
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-06-24-docs-check-design-gaps.md
    type: intake
---

## Sources

- (a) deprecated ADR-0113 に対する docs-check 検出の扱いが未確定。同 ADR は 2026-06-16 付で `status: deprecated`（OU-07、diagnostics → inspect 改名で現行根拠ではない）だが、REQ-0101-043/044 違反の NG が継続報告される。除外フラグ要否が決まっていない。
- (b) `case-close.md` の Epic Wave クローズ E1〜E6 が `**EN.**` lettered prefix 形式。`docs/specs/command-file-format.md` は `### Step N:` と `Step N-M` のみ定義し、`check_command_format.ts` も `**EN.**` を検査対象外にしている。許容・統合・SPEC 明記のいずれにするか未定。
- (c) 配布物整合性検査が `check_integrity.ts`（docs-check バックエンド）に未実装。inspect-* skill には実装済み。追加を見送った理由は新カテゴリで `skill-category-gap` NG が汚染されターゲットングが隠退化するため。docs-check の項目役割範囲（スクリプト修正を含める範囲 vs 含めない範囲）が SPEC に明文化されていない。
- 3 件とも docs-check または将来的な CI 自動化が NG を出し続けるか、逆に検出漏れを抱える状態に直結する。
- ADR-0113 の処理は Wave 3 の「Do NOT edit REQ/ADR files」制約で見送られた経緯があり、別 RU として ADR 整理フローに回す選択肢もある。

## Source Summary

docs-check と command file format SPEC まわりで、検査対象・検査ルール・SPEC 定義の整備に 3 つの未確定箇所が並んでいる。(a) deprecated ADR の docs-check 検出の扱い、(b) `**EN.**` lettered prefix の SPEC 上の位置づけ、(c) 配布物整合性検査の docs-check バックエンド適用範囲。いずれも docs-check 実行時の NG 持続または検出漏れに直結する設計レベルの残課題で、SPEC と実装の両面で境界を確定する必要がある。

## 統合理由

3 件とも「docs-check / SPEC 整備」で主題が共通し、個別に RU 化すると判断の整合性が取りにくい。特に (a) と (c) は docs-check の検査対象範囲という共通論点に紐づくため、1 つの RU で判断をそろえて解消する。ただし (a) の ADR-0113 ファイル編集は「Do NOT edit REQ/ADR files」制約の経緯があるため、RU スコープに含めるか別 RU（ADR 整理フロー）に切り出すかは req-define で確定する。

## 要件化の方向

- work_type: docs_chore（SPEC 整備 + チェッカー調整）。ただし (a) で ADR-0113 編集を含める場合は ADR 整理フロー扱いとなり種別要再考。
- acceptance criteria:
  - (a) deprecated ADR を docs-check 検出対象に含めるか除外するかを SPEC 上で確定。含める場合は根拠を明記、除外する場合は frontmatter 等のフラグ定義。あわせて ADR-0113 を `retired/` へ移動するか Decision セクションを結果・影響へ再分類するかを決定。
  - (b) `docs/specs/command-file-format.md` に代替フロー内サブステップ表現（`**EN.**` 形式）の許容・非許容を明記。許容なら `check_command_format.ts` の検査対象拡張、非許容なら `case-close.md` の E1〜E6 を `### Step N:` 形式へ再構成。
  - (c) docs-check の項目役割範囲を SPEC（新規または既存）へ明記。配布物整合性検査を `check_integrity.ts` に追加するか inspect-* 側のみで運用するかを決定。追加する場合は `skill-category-gap` ルールの調整もセットで。
- priority: 中。(a) の ADR-0113 NG は docs-check 実行のたびにノイズとして出続けるため早期判断が望ましい。
- dependencies/coordination: 依存 RU なし。(a) を ADR 整理フローに切り出す場合は、本 RU は (b)(c) に縮小し (a) を別 RU とする。REQ-0101-043/044、REQ-0142、REQ-0143 を関連要件とする。
