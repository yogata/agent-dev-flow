---
source_type: intake
generated_by: backlog-review
generated_at: 2026-09-15
status: draft
depends_on: []
tentative_classification: REQ
sources:
  - .agentdev/intake/promoted/2026-09-15-distribution-targets-yaml-warning-on-baseline-run.md
---

# RU-0007: check_distribution_boundary 実行時 distribution-targets.yaml 読込 warning の解消

## Sources

- **stderr warning**: case-run 提供 runner（check_distribution_boundary_cli.ts）実行時に stderr へ `fail-closed: distribution targets file is not valid YAML (.opencode\skills\repo-agentdev-integrity\data\distribution-targets.yaml)` が出力される。検査本体は正常（JSON stdout 正常・exit code 契約どおり・scanned_files 343・failures 正常）。base main 実行でも同一出力（main 由来）。

## Source Summary

検査結果に影響しないが警告ノイズとして毎回出力される、distribution-targets.yaml 読込経路起因と見られる stderr warning。原因特定（読込経路・YAML 妥当性・Windows パス区切り表示と file 解決の関係）が未実施。

## 統合理由

1:1。単独の checker 実行時挙動の解消系統。

## 要件化の方向

checker 整備要件として要件化する:

1. `.opencode/skills/repo-agentdev-integrity/data/distribution-targets.yaml` の読込経路と YAML 妥当性を確認する
2. stderr warning が検査結果（failures/stats）に影響しないことを確認した上で警告を解消する
3. Windows パス区切り（バックスラッシュ混在表示）と file 解決の関係を確認する

受け入れ条件（Sources より）: warning が出力されなくなること、検査結果（JSON stdout・exit code）が変わらないこと。
