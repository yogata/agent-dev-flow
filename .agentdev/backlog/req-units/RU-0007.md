---
source_type:
  - learning
generated_by: backlog-review
generated_at: 2026-09-08T22:28:15+09:00
status: draft
tentative_classification: 挙動Design
agentdev_handoff: true
sources:
  - .agentdev/learning/promoted/update-traceability-check-invocation-preconditions.md
---

# RU-0007: traceability check 実行前提の呼出手順明記

AgentDevFlow 本体向け引き継ぎマーカー（`agentdev_handoff: true`）。本リポジトリは self-hosting のため通常の req/case workflow 入力として扱う。

## Sources

- `.agentdev/learning/promoted/update-traceability-check-invocation-preconditions.md`: agentdev-traceability の check / coverage 実行時の引数仕様と走査対象の前提を確認せずに実行し、誤計上・手戻りを生ぐ事象が 3 観測されている。case-close のトレーサビリティ独立再検査（QG-4）で毎回利用する経路であり、再発頻度が高い。問題: (1) check.ts の `--req` に範囲構文（REQ-011-022..030 形式）を渡すと範囲文字列全体がリテラル reqId として報告される（`..` 形式を範囲展開しない。個別カンマ指定では正しく解決）。(2) worktree で scripts ディレクトリを cwd に起動した場合、`--root` の `.` が cwd 相対で解決され、ADF-COVERS 宣言 corpus が走査対象から外れる。(3) corpus の走査対象は .md と .ts のみで .agentdev/ は除外ディレクトリであり、それ以外の拡張子・除外パスへの宣言配置は計上されない。既存対策なし（scripts README は基本実行例のみで前提の注記なし）。fix gap。根拠: PR #2691（--req 範囲構文のリテラル報告）ほか deferred 2件（worktree --root・scan 対象拡張子）。反映先候補: agentdev-traceability SKILL references（check 呼出手順節）、agentdev-traceability Design（scripts README・実行例節）、agentdev-workflow-case-close / agentdev-workflow-case-run（traceability 独立再検査手順）。制約: 実装改善（--req 範囲展開）を採用するかは req-define 以降の判断であり、本成果物は手順注記の情報候補を提供する。

## Source Summary

引数仕様（--req の範囲形式非対応）、実行環境（worktree での --root 相対解決）、走査対象（拡張子・除外パス制約）の3前提がスキル手順・README のいずれにも明記されておらず、QG-4 独立再検査という高頻度経路で反復的に誤実行・誤計上を生んでいる。実装そのものの改修ではなく、実行前提の手順への集約が本論点。

## 統合理由

1:1。単独論点。learning-promote から知識保存適否ではなく fix gap として渡された改善要求であり、統合・分割不要。

## 要件化の方向

- `--req` 個別カンマ指定・worktree `--root` 明示・宣言配置可能拡張子 / 非除外パスの前提を、SKILL references・traceability Design 実行例・case-close / case-run 参照へ注記する。
- `--req` 範囲展開対応は実装改善候補として記録する（採否は req-define 以降の判断）。
