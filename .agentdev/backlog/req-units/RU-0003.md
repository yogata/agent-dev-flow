---
source_type: learning
generated_by: backlog-review
generated_at: 2026-07-06T08:28:58+09:00
status: draft
depends_on: []
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: .agentdev/learning/promoted/1-case-close-docs-guard-false-clean.md
    type: learning
---

## Sources

単一成果物 `1-case-close-docs-guard-false-clean.md`（learning パイプライン、PC-2、加重合計 28/40 で staged）。

要点:
- **観測事象**: case-close Step 3-1 の targeted docs guard（REQ-0158-003）で `check_changed_docs.ts --base-ref <merge-base>` を実行した際、case-close は main ワークツリーで実行され PR ブランチを checkout しないため HEAD が main 先端（= merge-base と同一）になり diff が空になる。PR が2ファイル変更しているのに `files_checked: []`・`failures: []` が返り false-clean になる事象が PR #1426（Issue #1425）で発生。`--files` に切り替えて再実行し `files_checked` に2ファイルが入ることを確認してからマージへ進んだ。
- **根本原因**: `--base-ref` は PR ブランチ上で実行する前提だが case-close 手順はその前提を明記していない。スクリプト側も `files_checked` 空時に warning を出さないため実行者が空結果を検知できず pass 判断してしまう。false-clean は検査見逃しリスクを生む。
- **望ましい変更**: (a) case-close Step 3-1 の起動例を `--files`（PR 変更ファイル明示）を標準とする、(b) check_changed_docs.ts が `files_checked` 空時に warning を出す、(c) case-close 手順に「`files_checked` が空でないことの確認」ステップを追加。
- **関連**: Issue #1425、PR #1426、REQ-0158-003、IR-056。横展開: check_integrity.ts 等の他の `--base-ref` を使うスクリプト（本 RU 対象外、参考記録）。

## Source Summary

`--base-ref` の前提（PR ブランチ上実行）が手順に明記されていないこと、スクリプトが `files_checked` 空時に warning を出さないことが根本原因。`--files` 標準化・`files_checked` 空確認ステップ・スクリプト warning 追加の3点で予防する方向性。guardrail insufficiency に分類される。

## 統合理由

1:1 対応。単一成果物を単一 RU に映射。成果物内の改善点（case-close.md 手順変更、check_changed_docs.ts スクリプト変更）は同一関心（docs guard false-clean 予防）で集約。SPEC とスクリプトは false-clean 予防の両輪として機能するため1 RU に集約。

## 要件化の方向

req-define は本 RU を「case-close docs guard 健全性改善（false-clean 予防）」として扱う。

- **REQ 配置判定**: 既存 REQ-0131（case-close / 完了処理）または REQ-0158（Targeted Docs Integrity Guard）への UPDATE（docs guard は false-clean を予防すべき状態要件の追加）を第一候補とする。REQ-0131 と REQ-0158 のいずれが主たる配置先かは req-define Step 5-2 で判断（docs guard の実行主体が case-close なら REQ-0131、guard 仕様一般なら REQ-0158）。
- **SPEC 配置**: `docs/specs/commands/case-close.md` Step 3-1 の起動例明確化（`--files` 標準化、`files_checked` 空確認ステップ追加、`--base-ref` の前提明記）。これは挙動SPEC として REQ の実装詳細を担う。
- **スクリプト配置**: `scripts/check_changed_docs.ts` への `files_checked` 空時 warning 出力追加。実装変更を伴う（REQ 要件行に対する SPEC/実装の具体化）。
- **横展開（参考、対象外）**: check_integrity.ts 等の他の `--base-ref` を使うスクリプトでも同パターンのリスクがあるが、本 RU の対象外。別途要件化を検討。
- **制約**: 既存の REQ-0158-003 手順との後方互換性（`--base-ref` を完全廃止せず前提を明記しつつ `--files` を推奨）。check_changed_docs.ts の既存テストが壊れないこと。
- **受け入れ条件**: case-close Step 3-1 の `--files` 標準化明記、`files_checked` 空確認ステップ追加、check_changed_docs.ts の warning 追加、`--base-ref` の前提明記（全て採用済み成果物の受け入れ条件に準拠）。
