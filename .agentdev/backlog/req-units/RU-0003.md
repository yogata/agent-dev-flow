---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-14T09:09:45+09:00
status: draft
sources:
  - path: .agentdev/intake/promoted/2026-06-14-source-projection-sync-junction-handling.md
    type: intake
---

## Sources

### intake: source-projection-sync-junction-handling

`source-projection-sync` 検査が `.opencode/skills/` 配下の agentdev-* スキル 20 件を「source にあるが projection にない」と報告するが、実体検証の結果すべて junction（reparse point）として正しく投射済み。根因は `check_integrity.ts` の `listDirs()` (L107-119) が `d.isDirectory()` を使用し、Windows junction は libuv で reparse point 扱いとなり `isDirectory()` が false を返すこと。一方 `checkBrokenJunctions` (L5561) は reparse point を正しく扱っており、check 間の実装不整合が存在する。

## Source Summary

Windows 環境で docs-check を実行するたびに 21 件の false positive が発生。根因は単一関数（`listDirs()`）の reparse point 非対応。`checkBrokenJunctions` では既に正しく扱っているため、実装パターンの共有または共通化で解決可能。

## 統合理由

**1:1**。source-projection-sync の junction/reparse point 問題は、checker heuristic の文脈認識不足（RU-0002）とは独立した filesystem API レベルの技術的根因。`isDirectory()` vs reparse point という明確で隔離された修正対象。独立 RU として扱うことで req-define が的確に修正範囲を特定できる。

## 要件化の方向

- `listDirs()` または `checkSourceProjectionConsistency` (L5459) が junction/reparse point を有効な投射エントリとして扱うよう修正
- 修正案: `fs.readdirSync` の `withFileTypes` で reparse point を directory として扱う、またはリンク先を解決して比較する
- REQ-0108-173（source-projection-sync 検査）、REQ-0108-210（junction 配下の取りこぼし禁止）、ADR-0020（repo-local 扱い）に基づく
- **route**: req-define
- **category**: integrity-rule-gap
