---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-17T00:50:11+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/existing-skill-worktree-integrity-junction.md
    type: learning
---

# worktree 環境での integrity スクリプト junction 対応手順の文書化

## Sources

### learning: worktree-integrity-junction

worktree（`.worktrees/{N}-{type}`）で integrity スクリプト（`check_integrity.ts`）を実行すると、`source-projection-sync` チェックが失敗する。worktree は独立したチェックアウトであり、`install-consumer-opencode.ps1` が作成する junction link がメインリポジトリから伝播しないため。毎回 AGENTS.md 記載の手順で junction を手動再作成する必要があり、運用上の既知制約として PR Findings に記録されていた。

REQ-0108-173 で「broken junction 検出 gate」は規定されており、`check_integrity.ts` には `resolvePathWithFallback` 関数（`/opencode/` → `src/opencode/` フォールバック、REQ-0108-189）も存在する。しかし worktree 環境特有の手順が `agentdev-workflow-orchestration` に未整備であり、case-run で worktree セットアップ後に integrity 検証を行う際のフリクションが継続していた。ギャップ分類は application miss（既存対策 partial）。

望ましい変更: case-run worktree セットアップ手順に、worktree 環境で integrity/projection 関連検証を行う際の junction 再作成ステップ（または該当チェックのスキップ/警告条件）を明記する。

## Source Summary

worktree 環境では junction link が伝播せず、integrity スクリプトの source-projection-sync チェックが失敗する。手動 junction 再作成で対応可能だが、手順化されていないため毎回のフリクションが発生。fallback logic（`resolvePathWithFallback`）と broken junction 検出 gate は存在するが、worktree 特有の手順とエラーハンドリングが未整備。Windows + junction 環境で特に顕著。

## 統合理由

1:1。単一 artifact であり、独立した論点の分割不要。

## 要件化の方向

req-define は以下を検討する:

1. **agentdev-workflow-orchestration SKILL.md への追記**: 準備フェーズ Step 4（worktree作成・ブランチ準備）周辺に、worktree 環境で integrity 検証を行う際の junction 再作成前提条件またはスキップ条件を追記する。
2. **self-healing-and-errors.md への追記**: worktree 環境で source-projection-sync が失敗した際のエラーハンドリング手順を追記する。
3. **integrity スクリプトの worktree 検知（副次候補）**: `check_integrity.ts` に worktree 環境を検知して該当チェックを警告レベルに緩和、またはメッセージに junction 再作成を案内するロジック追加を検討する。
4. **制約**: 既存の `resolvePathWithFallback` および REQ-0108-173 broken junction 検出 gate との整合性を保つ。worktree 環境での junction 再作成手順は AGENTS.md 記載の既存手順を踏襲する（新規手順の発明ではない）。Windows + junction 環境に特化した記述を明示する。まずは workflow-orchestration の手順追記で運用上のフリクションを解消することを優先する。

work_type は enhancement（documentation）を想定。PR #804 Findings, Issue #803 で実証済み。
