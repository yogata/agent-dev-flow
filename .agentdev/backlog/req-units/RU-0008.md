---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-27T10:02:34+09:00
status: draft
tentative_classification: 挙動SPEC
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-07-27-alloc-composite-id-3-digit-req-regex-bug.md
    type: intake
---

> 本 RU は AgentDevFlow 本体の不具合・改善点を扱う前工程引き継ぎ用 RU である（`agentdev_handoff: true`）。

## Sources

- **正規表現形式不一致**: `src/opencode/skills/agentdev-req-file-manager/scripts/src/alloc-composite-id.ts` の `extractAllCompositeIds` は `/REQ-(\d{4})-(\d{3})/` を使い4桁の REQ 番号だけを認識する。本リポジトリは `REQ-001-NNN` 等3桁形式を使用するため、既存 ID を認識できず `max=0` を返す。一方 `extractCompositeIdNumbers` は `\d{3,4}` を許容しており関数間で形式契約が一致していない。影響は REQ-001, REQ-003, REQ-006, REQ-008, REQ-010 を含む req-save の自動採番に及び、case-auto Draft 1 〜 8 で手動採番が必要になった。

## Source Summary

3桁 REQ 形式を一貫して認識する採番契約と検証が不足している。関数間で正規表現形式契約が不一致しており、一方の関数が3桁を許容しても他方が認識しない状態。work_type は maintenance、standard scale を想定。

## 統合理由

1:1。実装スクリプト（`alloc-composite-id.ts`）の正規表現バグ修正という単一関心。複数の REQ ファイル（REQ-001/003/006/008/010）に影響するが、根本原因は1スクリプトの形式契約不備。

## 要件化の方向

req-define で以下の修正 operation を構築する:
- `extractAllCompositeIds` の正規表現を `/REQ-(\d{3,4})-(\d{3})/` へ変更し、3桁と4桁の両形式を一貫して認識
- 3桁/4桁両形式での採番検証テストを追加
- `agentdev-req-file-manager` 実装詳細 SPEC（scripts 関連）へ形式契約の一律性を明記
