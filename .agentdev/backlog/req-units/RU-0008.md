---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-22T09:12:02Z
status: draft
sources:
  - path: .agentdev/learning/promoted/existing-command-case-workflow-state-mgmt.md
    type: learning
---

## Sources

- **case-auto/open/close 工程間状態管理・委譲契約の未整備**: 複数工程パイプライン（req-save→spec-save→case-open→case-run→case-close）で3件の重大インシデント発生。(1) Epic #979 Wave1 #982: case-auto が req-save/spec-save 成果物を main push 前に worktree 作成し sub-agent が MUST NOT DO 禁止の REQ/ADR/SPEC 編集（PR #986 スコープ違反でクローズ）、(2) PR #1012: case-auto 委譲契約 MUST NOT DO と case-close G22（SPEC昇格）が衝突、(3) PR #1014: case-open が .agentdev/ commit を push せず case-run へ引き継ぎ squash merge 後 git pull --ff-only が失敗。中間成果物の即時 push タイミング・MUST NOT DO 精密化・squash merge 後分岐ハンドリングが未整備。

## Source Summary

単独成果物（learning）。複数工程パイプラインで3件の実インシデントを踏まえた構造的改善。Form Zero（即時ステージ・コミット）は定義済みだが即時 push は未規定、MUST NOT DO が lifecycle 状態遷移まで包括禁止、git pull --ff-only のみで squash merge 分岐を想定しない、という3点の gap を埋める。

## 統合理由

1:1。3インシデントに共通する構造的 gap（中間成果物 push タイミング・委譲契約精密化・squash merge 分岐ハンドリング）を一括改修する大型プロセス変更。RU-0007（.gitignore 修正）は .agentdev/ git ライフサイクル関連だが、適用範囲・リスクプロファイルが異なり統合するとスコープが膨張するため分離。

## 要件化の方向

req-define では以下を検討する：
- case-open の draft/RU 削除 commit 後に即時 push する手順を Step 14/14-1 へ追加
- case-auto 委譲契約 MUST NOT DO を「実質的 SPEC/REQ/ADR 内容編集禁止（lifecycle 状態遷移 draft→accepted は除く）」へ精密化
- case-close Step 9（git pull --ff-only）へ squash merge 後ローカル先行 commit 検出→内容重複確認→reset 手順を追加
- git-common-procedures.md へ squash merge 後分岐ハンドリング手順（git log origin/main..HEAD → git diff --stat → git reset --hard origin/main）を追加
- 既存 Form Zero・G09・G22 は維持（手順・ガードレールの追加・精密化のみ）
- work_type 判定（fix 想定・enhancement + documentation ラベル）
- 修正後の case-auto/open/close 実行で3インシデント型が再現しないことを完了条件に設定
