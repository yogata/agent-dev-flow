---
source_type: intake
generated_by: backlog-review
generated_at: 2026-06-24T00:00:00+09:00
status: draft
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-06-24-install-script-local-mode.md
    type: intake
---

## Sources

- `scripts/install-consumer-opencode.ps1` L13-L14 は `consumer-with-agentdev`（通常版）向け実装のみ。`consumer-generated`（ローカル版）向けの agentdev-gh-cli → `src/opencode-local/agentdev-gh-cli/` リンク設定を欠く。
- 発生源は Epic #1093 Wave 3 / Issue #1096（REQ-0134 APPEND）/ PR #1100（2026-06-23 マージ）/ case-close Step 10 Capture（F-002）。Wave 3 対象外で未実装のまま残置。
- ローカル版の構成は agentdev-gh-cli のみ `src/opencode-local/agentdev-gh-cli/` へ、それ以外は `src/opencode/` へ接続（REQ-0103-158、ADR-0131 決定 #3）。
- 設計判断が未確定: 既存スクリプトへ `-LocalMode` スイッチを追加するか、別スクリプト（例: `install-consumer-opencode-local.ps1`）を新設するか。README の現行手順（`./scripts/install-consumer-opencode.ps1 -Mode apply`）との整合も考慮。
- runtime-package-boundary SPEC に「link mode 接続手順技術詳細」領域の明文化も未実施。
- 種別は feature（配布スクリプト拡張）。PowerShell 実装だが設計判断を含むため RU で要件を確定してから case 化する。
- **注意（dangling reference）**: REQ-0103-158 は REQ-0134:38 行の引用にのみ存在し、REQ-0103.md 本体には記載がない。要件化の前提となるため、本 RU の消化過程で REQ-0103.md 側へ追記するか参照元を整理するかを解決する必要がある。

## Source Summary

install-consumer-opencode.ps1 は通常版（consumer-with-agentdev）のリンク設定のみを提供し、ローカル版（consumer-generated）向けの agentdev-gh-cli 差し替え手順を欠いている。このためローカル版利用者はリンク差し替えを手作業で行う必要があり、手順の正しさが個人の文書読解に依存する。ADR-0131（通常版とローカル版を link mode に統一）の技術的実装が途中で止まった状態でもある。設計判断（スイッチ vs 別スクリプト）と SPEC 領域の明文化が未確定で、要件の根拠となる REQ-0103-158 が REQ-0103.md 本体に存在しない dangling reference も解決すべき課題として残る。

## 統合理由

単一 inbox 項目に由来する 1 件の配布スクリプト拡張。実装、SPEC 整備、dangling reference 解決が同一スクリプト・同一 SPEC 領域に収束し、分割すると ADR-0131 完結の技術的実装が一貫しなくなる。FILE 1（文書側旧用語掃除）と連動するが、本件は配布スクリプトの新規実装を含むため独立 RU が自然。FILE 1 完了後に本 RU を実行すれば、スクリプトとガイド文書の整合が同時に取れる。

## 要件化の方向

- work_type: feature（配布スクリプト拡張）
- acceptance criteria:
  - ローカル版導入がスクリプト 1 実行で完結する。
  - 生成物が REQ-0103-158 と ADR-0131 決定 #3 の構成に一致する（agentdev-gh-cli のみ `src/opencode-local/agentdev-gh-cli/` 接続、それ以外は `src/opencode/` 接続）。
  - README の導入手順と推奨 `.gitignore` 設定が local mode に対応する。
  - `./scripts/check-consumer-opencode.ps1` が local mode のリンク状態を検出できる。
  - runtime-package-boundary SPEC へ「link mode 接続手順技術詳細」領域を新設し、スクリプトの契約を明記。
  - REQ-0103-158 の dangling reference（REQ-0103.md 本体に記載がない）を解決する。REQ-0103.md へ追記するか、参照元を整理するかを case 化までに確定。
- priority: 中。ローカル版 consumer は限定的だが、ADR-0131 完結と REQ-0103-158 充足の観点で放置できない。
- dependencies/coordination: 依存 RU なし。設計判断（`-LocalMode` スイッチ vs 別スクリプト新設）を req-define で確定する。FILE 1（文書側旧用語掃除）完了後に実行すればスクリプトとガイド文書の整合が同時に取れるが、先行してもよい。REQ-0103-158、REQ-0134、ADR-0131、ADR-0130 を関連要件・決定とする。
