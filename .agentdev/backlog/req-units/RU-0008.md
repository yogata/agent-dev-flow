---
source_type:
  - learning
generated_by: backlog-review
generated_at: 2026-08-15T18:35:06+09:00
status: draft
depends_on: []
tentative_classification: 挙動SPEC
sources:
  - type: learning
    path: .agentdev/learning/promoted/spec-candidate-windows-console-encoding-initialization.md
---

# gh/git CLI エンコーディング初期化の全経路適用

## Sources

- **spec-candidate-windows-console-encoding-initialization.md（learning）**: Windows PowerShell/pwsh 環境（既定 cp932）で gh CLI / git CLI の日本語を含む操作の mojibake が4件・3経路以上で反復発生した。現行 SPEC（agentdev-gh-cli Section 2 Step 0）はコンソールエンコーディング初期化3行の必須前置を「gh CLI WRITE 手続き」のみに規定しており、次の3経路が未カバーである。(1) git CLI 直接操作（git commit -F 等の message file 読み取り経路、Issue #2054）。(2) gh pr create --title --body-file 複合呼び出し（Issue #2058）。(3) READ 系パイプライン出力（git show / Get-Content / Select-String、Issue 2100）。エントリ1（Issue #2050）は既存 Step 0 の適用省略による application miss であり、fix gap とは区別する。

## Source Summary

エンコーディング初期化の必須前置が gh CLI WRITE のみに限定され、git CLI 直接操作・READ 経路・複合呼び出しでの保護が手続き規範上存在しない。

## 統合理由

1:1（統合・分割不要）。単一 learning 成果物で完結する独立関心である。

## 要件化の方向

コンソールエンコーディング初期化の適用範囲を次のとおり拡張して規範化する。

1. git CLI 直接操作の WRITE（git commit -F、git tag -F 等）にも Step 0 相当の初期化を必須前置する
2. gh pr create / gh issue create で --title と --body-file の同時渡しを避け、ASCII 仮 title + --body-file → REST API PATCH による日本語 title 設定の2段階シーケンス、または gh api --input による統一を標準とする
3. PowerShell パイプライン経由の日本語 READ では [Console]::OutputEncoding 前置または Node.js execSync / fs.readFileSync 経路を指定する

反映先候補は agentdev-gh-cli references/standard-procedures.md、agentdev-git-worktree、および spec-save / req-define での配置判断。既存 AG-001（--title inline 禁止）・AG-002（REST API PATCH 標準）は前提として存置し、拡張のみを行う。agentdev-gh-cli はローカル版（src/opencode-local/）と通常版で参照先が分かれる構成に注意する（REQ-009、DEC-004）。
