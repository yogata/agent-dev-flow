---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-25T08:18:02+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/skill-workflow-templates-function-deletion-scope.md
    type: learning
---

## Sources

- **関数削除要求の完了条件におけるスコープ明示と全使用箇所 grep 確認の標準化**（学習元: L-014、PR #1140 / #1139 Epic #1138）。
  - Issue #1139 完了条件（AG-002/013）が「IR-044 context exemption 関数（isNegationContext 等5関数）の定義・呼出が 0 件」と関数単位で包括的削除を求めたが、isNegationContext は別 checker（checkWorkflowStatusProhibition）でも使用されており定義削除すると破壊的変更になることが判明。本来意図は「IR-044 からの context exemption 削除」だったが完了条件が関数名列挙で代用され共用関数の存在を見落とす構造だった。実装者が grep で回避。
  - 問題: 関数削除を要求する完了条件が対象スコープ（「from checkX」）を明記せず関数名列挙で完全削除を代用すると、共用関数の存在を見落とす。case-run の TS に削除対象関数の全使用箇所 grep 確認手順が標準化されていない。
  - 望ましい変更: `agentdev-workflow-templates` へ関数削除要求の完了条件書き方ガイド（スコープ明示・関数名列挙と完全削除の混同防止）追加。case-run TS へ全使用箇所 grep 確認の標準手順追加。

## Source Summary

関数削除を要求する完了条件は対象スコープ明示が前提。共用関数の包括的削除要求は他 checker を破壊する。workflow-templates への書き方ガイド追加と case-run TS の grep 確認標準化で再発を防止する方針。

## 統合理由

1:1（特別な統合・分割不要）。単一 learning 成果物が完了条件書き方ガイドと TS 標準化を扱うスコープとして完結。

## 要件化の方向

req-define では以下を壁打ちする。

- `agentdev-workflow-templates/SKILL.md` に、関数削除を要求する完了条件は対象スコープ（「from checkX」等）を明記し、関数名列挙と完全削除を混同させないガイドラインを追加する。
- ガイドラインに共用関数・cross-cutting helper 削除時の注意を明記する。
- `case-run.md` のテスト戦略（TS）に削除対象関数の全使用箇所 `grep -rn "funcName" scripts/` 確認手順を追加する（関数削除を伴う Issue に適用）。
- 追加内容が既存の完了条件 checkbox 形式と整合する。
- work_type 想定: enhancement（プロセス標準化）。推奨ラベル: enhancement, process, documentation。対象: workflow-templates SKILL.md / case-run.md。
