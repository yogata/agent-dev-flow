---
source_type: learning
generated_by: backlog-review
generated_at: 2026-07-18T15:24:32+09:00
status: draft
tentative_classification: 挙動SPEC
sources:
  - path: .agentdev/learning/promoted/harness-delegation-constraint.md
    type: learning
---

## Sources

- AgentDevFlow の case-run は `agentdev-case-run-execution-adapter` SKILL が定義する実行担当サブエージェント委譲プロトコルに基づき、Sisyphus-Junior 等の subagent へ実行を委譲する設計。Epic Wave では最大5件の子 Issue を並列委譲することで速度優位性を得る設計。
- しかし、oh-my-openagent ハーネスの `call_omo_agent` ツール schema は explore/librarian agent 型のみを許可し、custom agent 型（adapter skill が定義する実行担当サブエージェント型）を起動できない。Epic Wave 並列委譲（最大5件）も同様に機能しない。
- 本問題は PR #1068（L-004）、PR #1103（L-010）で既に観察され、`agentdev-case-run-execution-adapter` SKILL の task() 起動失敗時フォールバックパス（L131-148）が完全カバーすることが実証済み。
- ただし `call_omo_agent` schema 制約（explore/librarian のみ許可）という新側面と、Epic Wave 並列委譲の速度優位性消失という影響は adapter skill に未明文化。事前検出（probe）も未整備。
- 根拠:
  - inbox#1（Epic #1515 Wave 1）: call_omo_agent schema が explore/librarian のみを許可、Epic Wave 並列委譲不可
  - deferred L-004（PR #1068）: task() 起動失敗時フォールバック適用
  - deferred L-010（PR #1103）: orchestration + 実装を同一エージェント統合実行で完結
- 再発条件: call_omo_agent が explore/librarian のみを許可するハーネス環境で case-run を実行した場合に必ず発生。横展開可能性: 同一ハーネス環境で動作する全 case-run で発生。task() ツールを提供しない他ハーネスでも同根。

## Source Summary

ハーネス制約（`call_omo_agent` schema が explore/librarian のみ許可）で case-run 実行担当サブエージェント委譲が不可。adapter skill のフォールバックパスで運用は完結するが、`call_omo_agent` schema 制約という新側面と Epic Wave 並列委譲不可時の運用が adapter skill に未明文化。事前 probe も未整備。

## 統合理由

1:1。単一関心（adapter skill のフォールバック強化と事前 probe 整備）を扱うため統合・分割は不要。ハーネス側の schema 拡張は別プロジェクト（oh-my-openagent）のスコープであり、adapter skill 側でハーネス能力を検出して適応するアプローチを採る。

## 要件化の方向

`req-define` では下記を要件化の方向として想定する。

- `.opencode/skills/agentdev-case-run-execution-adapter/SKILL.md` に `call_omo_agent` schema 制約時の挙動を明文化
- 同 SKILL.md に事前 probe 手順（ハーネス能力検出 → 委譲可否判断 → Inability 冒頭明示）を追記
- 同 SKILL.md に Epic Wave 並列委譲不可時の運用（インライン逐次実行等）を明文化
- `.opencode/skills/agentdev-workflow-orchestration/references/capture-boundaries.md` に委譲可否 probe 手順を追記
- 既存の task() 起動失敗時フォールバックパス（L131-148）と重複せず拡張として整理
- インライン実行時も adapter protocol（証拠ベース実装・品質ゲート・PR 作成・worktree 隔離・Findings 配置）には従う（L-010 自律対応内容を踏襲）
- `docs/requirements/REQ-0149.md` の実行環境前提に `call_omo_agent` schema 制約を明記（必要に応じて別 Issue で REQ 更新）
- ハーネス側の `call_omo_agent` schema 拡張は別プロジェクト（oh-my-openagent）のスコープとし、本 RU 対象外
- 文書種別配置は `docs/specs/foundations/document-model.md` の文書7分類モデルに従う（暫定分類: 挙動SPEC）
