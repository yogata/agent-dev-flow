---
source_type: intake
generated_by: backlog-review
generated_at: 2026-07-05T11:30:14+09:00
status: draft
tentative_classification: REQ
agentdev_handoff: true
sources:
  - path: .agentdev/intake/promoted/2026-07-05-req0161-verification-self-reference.md
    type: intake
---

## Sources

PR #1417（Issue #1416, REQ-0161）で config.yaml および旧 doc-inputs 機構定義の完全削除を実施。削除対象を定義する要件文書 REQ-0161.md 自身が `docs/` 配下にあり、削除対象 ID（ADR-0133、REQ-0157、project-doc-inputs）と検証コマンドをリテラル埋め込みするため、2つの構造的課題が発生した。

- 課題1（REQ-0161.md 自己参照）: REQ-0161-004（`rg ... docs/` が 0件）は REQ-0161.md 自身がマッチするため自己削除しない限り到達不能。REQ-0161-005（check_integrity.ts exit 0）も REQ-0161.md が ADR-0133/REQ-0157 への unique ref を保持するため broken-req-ref / broken-adr-ref / adr-req-crossref の 3 NG が発生し exit 1 となる
- 課題2（README.md タイトル過剰ヒット）: `rg "config\.yaml" docs/` が `docs/requirements/README.md` の REQ-0161 エントリ行（タイトル文字列）にマッチ。要件の簡易表現でありファイルパス参照ではないが、検証コマンドの正規表現が `config.yaml` にマッチするため過剰ヒットする

check_integrity.ts は作業前から baseline-failing（exit 1、計59 NG、うち56件は既存 baseline）。strict pass 基準 REQ-0161-005 は構造的に到達不能。

## Source Summary

「削除対象を名指しする要件文書」と「削除対象を検索する検証コマンド」が同一 `docs/` 配下に共存する構造的矛盾。要件定義の必然性（何を削除するか明記）と検証の厳格性（0件到達）が両立しない。case-close QG-4 では ad-hoc 判定（自己参照除外、baseline-failing 改善評価）で PASS したが、判定基準が機械化できない。

## 統合理由

1:1 対応。単一成果物、単一関心（検証メカニズムの構造的到達不能の解消）。

## 要件化の方向

- work_type: `maintenance`（検証コマンド設計見直し）または `feature`
- 決定すべき論点（いずれか単独または組み合わせ）:
  - (a) 要件文書が削除対象 ID をリテラル含むことの例外扱いを check_integrity.ts または REQ 側で仕組み化する（自己参照の baseline 除外ロジック、要件文書の検証対象外指定など）
  - (b) REQ-0161-004/005 の完了条件記述を「自己参照を除く」等の修飾へ改める
  - (c) 検証コマンド `rg ... docs/` を要件文書に埋め込む運用を見直す（コマンドは SPEC/runner 側へ分離し、要件文書は成果物状態のみ記述）
  - (d) README.md の REQ タイトル表記を「設定ファイル削除」等の一般化表現へ改める、または検証コマンドを「ID 参照とパス参照に限定し、キーワード（config.yaml）は除外」へ運用変更する
- 影響範囲: REQ-0161-004/005、check_integrity.ts（baseline-failing 59 NG 含む）、`docs/requirements/README.md`
- 既存REQ: REQ-0161（完了条件見直し）、REQ-0108（docs-check 設計）。REQ-0161 への UPDATE を優先し、新規REQ 不要の方向
- 受け入れ条件: 決定した方向性に基づき REQ-0161-004/005 または check_integrity.ts のいずれかが更新され、自己参照が検証結果に影響しない仕組みが確立されること
