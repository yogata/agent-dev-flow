---
id: RU-0126
title: "worktree-operations.md の Windows 実行例補強（junction 処理系差分・backslash パス破損注意）"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-24T10:22:39+09:00
status: draft
agentdev_handoff: true
tentative_classification: 挙動Design
sources:
  - type: learning
    path: .agentdev/learning/promoted/existing-countermeasure-update-junction-os-command-differences.md
  - type: learning
    path: .agentdev/learning/promoted/existing-countermeasure-update-worktree-operations-backslash-path-note.md
---

# RU-0126: worktree-operations.md の Windows 実行例補強（junction 処理系差分・backslash パス破損注意）

## Sources

- `.agentdev/learning/promoted/existing-countermeasure-update-junction-os-command-differences.md`: worktree 依存整備の junction 操作は実行処理系により挙動が異なる。node fs.symlinkSync の相対 target は dest ディレクトリ基準で解決されるため誤リンク（TIM テスト 7件 fail を一時誘発）となり、Git Bash の rmdir は「Not a directory」で拒否、PowerShell Remove-Item は NonInteractive で確認プロンプトが出得る。削除は node fs.rmdirSync が確実（main 側実体不変）。既存手順例（cmd /c mklink /J の cwd 相対指定・Remove-Item）が node / bash 経由実行に転記できない（fix gap）。8軸評価 27/40、既存対策の更新。
- `.agentdev/learning/promoted/existing-countermeasure-update-worktree-operations-backslash-path-note.md`: bash は backslash を escape 文字として解釈するため、引用符なし Windows 形式パスを checker `--root` へ渡すと引数段階で破損し、空コーパス走査で fail-closed 契約どおり missing を返す（見かけ上の全件 missing、Case #3088）。forward slash 形式で pass 9 / fail 0 に解消。当該節の例示は forward slash 形式だが backslash パス破損の注意記載が欠落（fix gap）。8軸評価 25/40、既存対策の更新。

## Source Summary

worktree-operations.md の実行例は cmd / PowerShell 前提で書かれた部分があり、node / bash 経由の実行ではパス解決基準差（fs.symlinkSync の dest 基準解決）・削除コマンド差（fs.rmdirSync が確実）・bash escape 解釈（backslash パス破損→見かけ上全件 missing）により成立しない。Windows 環境の worktree 検証運用で頻出する実行形態差分を、該当節の例示と注意として補完する。

## 統合理由

2:1 統合。統合理由: 同一反映先 worktree-operations.md の Windows 実行例を対象とし、両知見とも同一文書内の実行例・注意の補完であるため、分割より単一 RU での反映先統一が Case 分割効率に優れる。

## 要件化の方向

worktree-operations.md（agentdev-git-worktree references）へ追記: junction 作成は node fs.symlinkSync の絶対パス指定（dest ディレクトリ基準解決の明記）、junction 削除は node fs.rmdirSync の正規化（Git Bash rmdir・PowerShell Remove-Item の挙動差注意）、checker `--root` への backslash パス破損（bash escape 解釈→見かけ上全件 missing）の注意と forward slash 形式統一を明記する。
