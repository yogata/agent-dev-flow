---
id: RU-0109
title: "AUTOGEN 計測日 drift 3 機構と導出基準（committer date）の Design 明示化"
source_type: learning
generated_by: backlog-review
generated_at: 2026-09-20
status: draft
work_type: docs_chore
depends_on: []
agentdev_handoff: true
tentative_classification: 挙動Design
sources:
  - .agentdev/learning/promoted/measure-update-autogen-measure-date-committer-drift.md
---

# RU-0109: AUTOGEN 計測日 drift 3 機構と導出基準（committer date）の Design 明示化

## Sources

- **learning: AUTOGEN 計測日導出と GitHub squash merge の committer date 置換による drift**: 計測日導出（generate_indexes.ts deriveMeasureDateFromLastCommit）は git log -1 --format=%cI（committer date 基準）で最終コミット日付を取るが、GitHub 側 squash merge commit の committer date は merge 実行時刻（サーバー時刻）に置き換わるため、日付境界をまたぐとドキュメント群に実変更がなくても鮮度違反となる。Case #3011 第8段（Epic #3017 Wave 1 fan-in）で発生し、計測日再生成 commit 1befae99 で解消。date rollover 起因（deferred 2026-09-01）・Phase 0 起因（deferred 2026-08-18）に続く第3の機構。現行 Design は導出基準と drift 構造を明示せず発生のたびに原因調査が繰り返される。

## Source Summary

AUTOGEN 鮮度 gate の機構は稼働中だが、計測日導出基準（committer date・author date との区別）と drift 発生 3 機構（date rollover・Phase 0・squash merge committer date 置換）が未文書。運用知見の Design 集約化が主体（gate 仕様・実装は不変）。

## 統合理由

1:1。単一成果物・単一問題クラス（AUTOGEN 計測日 drift）であり、Design 明示化と境界 close 運用の反映は同一知見の正規化先として分離しない。

## 要件化の方向

autogen-freshness-gate Design または index-auto-generation Design へ、(1) 計測日導出基準（%cI committer date・author date との区別）、(2) drift 発生機構 3 種（date rollover・Phase 0 起因・GitHub squash merge の committer date 置換）の列挙、を明示する。併せて squash merge を伴う境界 close の fan-in 検査では check_autogen_freshness を必ず実行し、drift 検出時は計測日再生成（generate_indexes）で解消してから green 判定する運用を設計書に反映する（agentdev-workflow-case-close references の fan-in・docs 検査手順への追記候補を含む）。計測日導出の author date 基準切替・merge commit 除外は恒久検討課題として記録するのみ（実装変更・gate 停止条件・exit 契約は対象外・不変）。計測日再生成は case-run 側または fan-in 時の正規経路で実施する（case-close 自身は索引を直接編集しない契約を維持）。
