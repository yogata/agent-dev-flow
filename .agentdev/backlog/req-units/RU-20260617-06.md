---
source_type: learning
generated_by: backlog-review
generated_at: 2026-06-17T00:50:11+09:00
status: draft
sources:
  - path: .agentdev/learning/promoted/existing-command-case-close-duplicate-check.md
    type: learning
---

# case-close 実行前の未コミット変更と PR 変更ファイルの重複チェック追加

## Sources

### learning: case-close-duplicate-check

case-close Step 9（`git pull --ff-only`）が、メインリポジトリの別件未コミット変更（REQ-0124 関連: docs/DOC-MAP.md, docs/README.md, docs/requirements/README.md）と PR #804 の squash commit 変更ファイルが重複したため失敗した。結果として `.agentdev/` domain state の commit/push（Step 11）が実行不能になった。

既存の `agentdev-git-worktree/references/git-common-procedures.md` の「実行前同期」手順は `git status --porcelain` で「任意のローカル変更」を検出して停止するが、これは粗すぎる。重複の有無にかかわらず全てのローカル変更で停止するため、ユーザーが別件作業を残したまま case-close を実行する通常運用で頻繁にブロックされる。ギャップ分類は fix gap（重複特化の早期警告が不在）。

望ましい変更: case-close Step 1（Issue番号解決）の直後に、メインリポジトリの `git status --short` と対象 PR の変更ファイル一覧（`gh pr view --json files`）の重複チェックを追加。重複ファイルがある場合は Step 4（merge）前にユーザーに警告し、対応（stash/commit/checkout）を促す。

## Source Summary

case-close の実行前同期が粗い粒度の「任意のローカル変更で停止」チェックに依存しており、PR 変更ファイルとの重複特化チェックが不在。重複による `git pull --ff-only` 失敗が Step 9 で初めて発覚し、Step 4（merge）後に手戻りが発生する。並行作業環境で特に発生しやすいが、その前提の早期警告がない。

## 統合理由

1:1。単一 artifact であり、独立した論点の分割不要。

## 要件化の方向

req-define は以下を検討する:

1. **case-close Step 1-2 への重複チェックサブステップ追加**: `git status --short` と `gh pr view --json files` の比較ロジックを手順化する。
2. **重複ファイル検出時の構造化警告**: ユーザーアクション候補（`git stash` / commit / `git checkout -- <file>` / worktree 利用）を構造化メッセージで提示する。
3. **ガードレール追記**: case-close の Guardrail セクションに重複チェック必須を追加する。
4. **git-common-procedures.md の拡張検討（副次）**: 既存の「任意の変更で停止」を「重複時のみ停止」に緩和する判断材料の提供。
5. **後方互換性**: 既存 Step 9（実行前同期）本体は維持し、Step 1-2 で早期警告を追加するアプローチ。`gh pr view` 実行不可時はフォールバック（Step 9 で検出）を維持する。

work_type は maintenance を想定。既存 Step 9 本体を置き換えず、後方互換性を維持する。並行作業環境で特に有効であることを明記する。
