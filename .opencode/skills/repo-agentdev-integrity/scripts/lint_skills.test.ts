/** * Subprocess tests for lint_skills.ts. * * The script resolves its scan target via findRepoRoot(import.meta.dir), which * walks up from the script file looking for .opencode/. By copying the script * into <temp>/_scripts/ and creating fixture skills under <temp>/.opencode/skills/, * findRepoRoot resolves to our temp directory and scans only fixture skills. */import { describe, it, expect, beforeAll, afterAll } from "bun:test";import * as fs from "fs";import * as path from "path";// ---------------------------------------------------------------------------// Paths// ---------------------------------------------------------------------------const SCRIPT_SRC = path.join(import.meta.dir, "lint_skills.ts");const UTILS_SRC = path.join(import.meta.dir, "cli_utils.ts");const TEMP_BASE = path.join("C:", "WINDOWS", "TEMP", "opencode");// ---------------------------------------------------------------------------// Types// ---------------------------------------------------------------------------interface RunResult {  exitCode: number;  stdout: string;  stderr: string;}interface JsonReport {  timestamp: string;  script: string;  scanned: Record<string, number>;  summary: { ok: number; ng: number; warning: number; info: number };  results: Array<{    category: string;    check: string;    level: string;    message: string;    file?: string;  }>;}interface SkillFixture {  name: string;  content: string | null; // null = no SKILL.md}// ---------------------------------------------------------------------------// Helpers// ---------------------------------------------------------------------------function createTempDir(): string {  if (!fs.existsSync(TEMP_BASE)) {    fs.mkdirSync(TEMP_BASE, { recursive: true });  }  return fs.mkdtempSync(path.join(TEMP_BASE, "lint-skills-test-"));}function setupTempEnv(  fixtures: SkillFixture[],): { tempDir: string; scriptPath: string } {  const tempDir = createTempDir();  // Copy scripts to <temp>/_scripts/ so findRepoRoot walks up to <temp>/  const scriptsDir = path.join(tempDir, "_scripts");  fs.mkdirSync(scriptsDir, { recursive: true });  fs.copyFileSync(SCRIPT_SRC, path.join(scriptsDir, "lint_skills.ts"));  fs.copyFileSync(UTILS_SRC, path.join(scriptsDir, "cli_utils.ts"));  const scriptPath = path.join(scriptsDir, "lint_skills.ts");  // Create .opencode/skills/ tree  const skillsDir = path.join(tempDir, ".opencode", "skills");  fs.mkdirSync(skillsDir, { recursive: true });  for (const fixture of fixtures) {    const skillDir = path.join(skillsDir, fixture.name);    fs.mkdirSync(skillDir, { recursive: true });    if (fixture.content !== null) {      fs.writeFileSync(path.join(skillDir, "SKILL.md"), fixture.content);    }  }  return { tempDir, scriptPath };}async function runScript(  scriptPath: string,  args: string[] = [],): Promise<RunResult> {  const proc = Bun.spawn(["bun", "run", scriptPath, ...args], {    stdout: "pipe",    stderr: "pipe",  });  const stdout = await new Response(proc.stdout).text();  const stderr = await new Response(proc.stderr).text();  const exitCode = await proc.exited;  return { exitCode, stdout, stderr };}// ---------------------------------------------------------------------------// Fixture content// ---------------------------------------------------------------------------const VALID_SKILL_MD = `\---name: agentdev-test-validdescription: A valid test skill---## USE FOR- Testing the lint script## DO NOT USE FOR- Production use## See Also`;const MISMATCH_SKILL_MD = `\---name: agentdev-wrong-namedescription: A skill with mismatched name---## USE FOR- Testing name mismatches## DO NOT USE FOR- Production use## See Also`;const NO_SECTIONS_SKILL_MD = `\---name: agentdev-test-nosectionsdescription: A skill missing required sections---Some content without required section headings.## See Also`;function makeBloatedMd(): string {  const lines: string[] = [    "---",    "name: agentdev-test-bloated",    "description: A bloated skill for testing",    "---",    "",    "## USE FOR",    "",    "- Testing bloat detection",    "",    "## DO NOT USE FOR",    "",    "- Not for production",    "",    "## See Also",    "",  ];  while (lines.length <= 510) {    lines.push(`Line ${lines.length}: padding content for bloat test.`);  }  return lines.join("\n");}const NO_PREFIX_SKILL_MD = `\---name: test-no-prefixdescription: A skill without agentdev prefix---## USE FOR- Testing prefix detection## DO NOT USE FOR- Production use## See Also`;// ---------------------------------------------------------------------------// Tests// ---------------------------------------------------------------------------describe("lint_skills.ts", () => {  const tempDirs: string[] = [];  let validEnv: { tempDir: string; scriptPath: string };  let issueEnv: { tempDir: string; scriptPath: string };  beforeAll(() => {    // Environment with a single valid skill 竊・exit 0    validEnv = setupTempEnv([      { name: "agentdev-test-valid", content: VALID_SKILL_MD },    ]);    tempDirs.push(validEnv.tempDir);    // Environment with problematic fixtures 竊・exit 1    issueEnv = setupTempEnv([      { name: "agentdev-test-no-md", content: null },      { name: "agentdev-test-mismatch", content: MISMATCH_SKILL_MD },      { name: "agentdev-test-nosections", content: NO_SECTIONS_SKILL_MD },      { name: "agentdev-test-bloated", content: makeBloatedMd() },      { name: "test-no-prefix", content: NO_PREFIX_SKILL_MD },    ]);    tempDirs.push(issueEnv.tempDir);  });  afterAll(() => {    for (const dir of tempDirs) {      fs.rmSync(dir, { recursive: true, force: true });    }  });  // ----- Flag tests -------------------------------------------------------  describe("--help flag", () => {    it("exits with code 0", async () => {      const result = await runScript(validEnv.scriptPath, ["--help"]);      expect(result.exitCode).toBe(0);    });    it("outputs help text containing script name, USAGE, and flags", async () => {      const result = await runScript(validEnv.scriptPath, ["--help"]);      expect(result.stdout).toContain("lint_skills.ts");      expect(result.stdout).toContain("USAGE");      expect(result.stdout).toContain("--help");      expect(result.stdout).toContain("--json");      expect(result.stdout).toContain("--dry-run");    });  });  describe("--dry-run flag", () => {    it("exits with code 0", async () => {      const result = await runScript(validEnv.scriptPath, ["--dry-run"]);      expect(result.exitCode).toBe(0);    });    it("lists skills that would be checked", async () => {      const result = await runScript(validEnv.scriptPath, ["--dry-run"]);      expect(result.stdout).toContain("Skills to be checked");      expect(result.stdout).toContain("agentdev-test-valid");    });  });  describe("--json flag", () => {    it("outputs valid JSON with expected structure", async () => {      const result = await runScript(validEnv.scriptPath, ["--json"]);      expect(result.exitCode).toBe(0);      const report: JsonReport = JSON.parse(result.stdout);      expect(report.script).toBe("lint_skills.ts");      expect(report.summary).toBeDefined();      expect(report.results).toBeInstanceOf(Array);      expect(typeof report.summary.ok).toBe("number");    });  });  // ----- Valid skill -------------------------------------------------------  describe("valid skill directory", () => {    it("produces all OK results and exits 0", async () => {      const result = await runScript(validEnv.scriptPath, ["--json"]);      expect(result.exitCode).toBe(0);      const report: JsonReport = JSON.parse(result.stdout);      const skillResults = report.results.filter(        (r) =>          r.file === "agentdev-test-valid" ||          r.message.includes("agentdev-test-valid"),      );      expect(skillResults.length).toBeGreaterThan(0);      for (const r of skillResults) {        expect(r.level).toBe("ok");      }    });  });  // ----- Issue detection ---------------------------------------------------  describe("missing SKILL.md", () => {    it("detects NG for SKILL.md existence", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "SKILL.md existence" &&          r.file === "agentdev-test-no-md",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("ng");    });  });  describe("name mismatch between directory and frontmatter", () => {    it("detects warning for directory vs frontmatter name", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "Directory naming vs frontmatter name" &&          r.file === "agentdev-test-mismatch",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("warning");      expect(found[0].message).toContain("does not match");    });  });  describe("missing required sections", () => {    it("detects warning for missing USE FOR section", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "USE FOR section" &&          r.file === "agentdev-test-nosections",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("warning");    });    it("detects warning for missing DO NOT USE FOR section", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "DO NOT USE FOR section" &&          r.file === "agentdev-test-nosections",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("warning");    });  });  describe("bloated SKILL.md (>500 lines)", () => {    it("detects warning for files exceeding 500 lines", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "SKILL.md bloat" &&          r.file === "agentdev-test-bloated",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("warning");      expect(found[0].message).toContain("exceeds 500");    });  });  describe("skill without agentdev- prefix", () => {    it("reports info level for unprefixed skill", async () => {      const result = await runScript(issueEnv.scriptPath, ["--json"]);      const report: JsonReport = JSON.parse(result.stdout);      const found = report.results.filter(        (r) =>          r.check === "agentdev- prefix" && r.file === "test-no-prefix",      );      expect(found.length).toBe(1);      expect(found[0].level).toBe("info");      expect(found[0].message).toContain("does not use agentdev- prefix");    });  });});