/** * Skill structure linter for AgentDevFlow. * Validates all skill directories under .opencode/skills/ for structural * correctness and naming conventions. * * Checks: *   Structure: SKILL.md existence, directory name vs frontmatter name *   Content: description, USE FOR, DO NOT USE FOR, See Also references *   References: resource subdirectories, SKILL.md bloat *   Namespace: agentdev- prefix */import {  EXIT_OK,  EXIT_ERROR,  parseArgs,  printHelp,  ok,  ng,  warn,  info,  computeSummary,  formatJsonReport,  formatMarkdownReport,  determineExitCode,  findRepoRoot,} from "./cli_utils.ts";import type {  CheckResult,  ScanSummary,  IntegrityReport,} from "./cli_utils.ts";const SCRIPT_NAME = "lint_skills.ts";const SCRIPT_DESCRIPTION = "Skill structure linter for AgentDevFlow";const SCRIPT_USAGE = "bun run .opencode/skills/agentdev-integrity/scripts/lint_skills.ts [--help] [--json] [--dry-run]";const fs = require("fs");const path = require("path");interface FrontMatter {  name: string;  description: string;}function parseFrontMatter(content: string): FrontMatter | null {  const parts = content.split("---");  if (parts.length < 3) return null;  const body = parts[1].trim();  const fields: Record<string, string> = {};  for (const line of body.split("\n")) {    const colonIndex = line.indexOf(":");    if (colonIndex === -1) continue;    const key = line.slice(0, colonIndex).trim();    const value = line.slice(colonIndex + 1).trim();    fields[key] = value;  }  if (!fields.name) return null;  return {    name: fields.name,    description: fields.description || "",  };}function stripAgentdevPrefix(name: string): string {  return name.startsWith("agentdev-") ? name.slice("agentdev-".length) : name;}function extractSeeAlsoReferences(content: string): string[] {  const refs: string[] = [];  const seeAlsoMatch = content.match(/## See Also([\s\S]*?)$/);  if (!seeAlsoMatch) return refs;  const section = seeAlsoMatch[1];  const boldPattern = /\*\*(agentdev-[a-z0-9-]+)\*\*/g;  let match: RegExpExecArray | null;  while ((match = boldPattern.exec(section)) !== null) {    refs.push(match[1]);  }  const linkPattern = /\[(agentdev-[a-z0-9-]+)\]\([^)]*\)/g;  while ((match = linkPattern.exec(section)) !== null) {    refs.push(match[1]);  }  return [...new Set(refs)];}function hasSection(content: string, heading: string): boolean {  const pattern = new RegExp(`^## .*${escapeRegex(heading)}`, "m");  return pattern.test(content);}function escapeRegex(str: string): string {  return str.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");}function lintSkill(  skillDir: string,  dirName: string,  allDirNames: Set<string>,): CheckResult[] {  const results: CheckResult[] = [];  const skillMdPath = path.join(skillDir, "SKILL.md");  // Check 1: SKILL.md existence  if (!fs.existsSync(skillMdPath)) {    results.push(ng("Structure", "SKILL.md existence", `SKILL.md not found in ${dirName}`, dirName));    // Cannot continue without SKILL.md    return results;  }  results.push(ok("Structure", "SKILL.md existence", `${dirName}/SKILL.md exists`));  const content = fs.readFileSync(skillMdPath, "utf-8");  const frontMatter = parseFrontMatter(content);  // Check 2: Directory naming vs frontmatter name  if (frontMatter) {    const fmStripped = stripAgentdevPrefix(frontMatter.name);    const dirStripped = stripAgentdevPrefix(dirName);    if (fmStripped !== dirStripped) {      results.push(warn(        "Structure",        "Directory naming vs frontmatter name",        `Directory "${dirName}" does not match frontmatter name "${frontMatter.name}"`,        dirName,      ));    } else {      results.push(ok("Structure", "Directory naming vs frontmatter name", `${dirName} matches frontmatter`));    }  } else {    results.push(warn(      "Structure",      "Directory naming vs frontmatter name",      `Could not parse frontmatter in ${dirName}/SKILL.md`,      dirName,    ));  }  // Check 3: Description presence  if (frontMatter && frontMatter.description.length > 0) {    results.push(ok("Content", "Description presence", `${dirName} has description`));  } else {    results.push(ng("Content", "Description presence", `Missing or empty description in ${dirName}/SKILL.md frontmatter`, dirName));  }  // Check 4: USE FOR section  if (hasSection(content, "USE FOR")) {    results.push(ok("Content", "USE FOR section", `${dirName} has USE FOR section`));  } else {    results.push(warn("Content", "USE FOR section", `Missing USE FOR section in ${dirName}/SKILL.md`, dirName));  }  // Check 5: DO NOT USE FOR section  if (hasSection(content, "DO NOT USE FOR")) {    results.push(ok("Content", "DO NOT USE FOR section", `${dirName} has DO NOT USE FOR section`));  } else {    results.push(warn("Content", "DO NOT USE FOR section", `Missing DO NOT USE FOR section in ${dirName}/SKILL.md`, dirName));  }  // Check 6: See Also references  const seeAlsoRefs = extractSeeAlsoReferences(content);  if (seeAlsoRefs.length > 0) {    const brokenRefs = seeAlsoRefs.filter((ref) => !allDirNames.has(ref));    if (brokenRefs.length > 0) {      for (const broken of brokenRefs) {        results.push(ng(          "Content",          "See Also references",          `Broken reference to "${broken}" in See Also section`,          dirName,        ));      }    } else {      results.push(ok("Content", "See Also references", `${dirName} See Also references all valid`));    }  } else {    results.push(ok("Content", "See Also references", `${dirName} has no See Also references to check`));  }  // Check 7: Resource references (scripts/, references/, templates/)  const resourcePatterns = ["scripts/", "references/", "templates/"];  const mentionedResources: string[] = [];  for (const res of resourcePatterns) {    if (content.includes(res)) {      mentionedResources.push(res);    }  }  if (mentionedResources.length > 0) {    let allExist = true;    for (const res of mentionedResources) {      const resPath = path.join(skillDir, res.replace("/", path.sep));      if (!fs.existsSync(resPath)) {        results.push(warn(          "References",          "Resource references",          `Referenced subdirectory "${res}" not found in ${dirName}`,          dirName,        ));        allExist = false;      }    }    if (allExist) {      results.push(ok("References", "Resource references", `${dirName} referenced resources all exist`));    }  } else {    results.push(ok("References", "Resource references", `${dirName} has no resource references to check`));  }  // Check 8: SKILL.md bloat (>500 lines)  const lineCount = content.split("\n").length;  if (lineCount > 500) {    results.push(warn(      "References",      "SKILL.md bloat",      `SKILL.md has ${lineCount} lines (exceeds 500 line threshold)`,      dirName,    ));  } else {    results.push(ok("References", "SKILL.md bloat", `${dirName}/SKILL.md has ${lineCount} lines (within 500 line limit)`));  }  // Check 9: agentdev- prefix  if (dirName.startsWith("agentdev-")) {    results.push(ok("Namespace", "agentdev- prefix", `${dirName} uses agentdev- prefix`));  } else {    results.push(info("Namespace", "agentdev- prefix", `${dirName} does not use agentdev- prefix`, dirName));  }  return results;}function main(): void {  const args = process.argv.slice(2);  const options = parseArgs(args);  if (options.help) {    printHelp(SCRIPT_NAME, SCRIPT_DESCRIPTION, SCRIPT_USAGE);    process.exit(EXIT_OK);  }  const repoRoot = findRepoRoot(import.meta.dir);  const skillsDir = path.join(repoRoot, ".opencode", "skills");  if (!fs.existsSync(skillsDir)) {    console.error(`Skills directory not found: ${skillsDir}`);    process.exit(EXIT_ERROR);  }  const entries = fs.readdirSync(skillsDir, { withFileTypes: true });  const skillDirs = entries    .filter((e: { isDirectory: () => boolean; name: string }) =>      e.isDirectory() && e.name !== "README.md"    )    .map((e: { name: string }) => ({      name: e.name,      fullPath: path.join(skillsDir, e.name),    }));  if (options.dryRun) {    console.log("Skills to be checked:");    for (const sd of skillDirs) {      console.log(`  ${sd.name}`);    }    console.log(`Total: ${skillDirs.length} skill directories`);    process.exit(EXIT_OK);  }  const allDirNames = new Set(skillDirs.map((sd) => sd.name));  const allResults: CheckResult[] = [];  for (const sd of skillDirs) {    const results = lintSkill(sd.fullPath, sd.name, allDirNames);    allResults.push(...results);  }  const summary: ScanSummary = computeSummary(allResults);  const report: IntegrityReport = {    timestamp: new Date().toISOString(),    script: SCRIPT_NAME,    scanned: { skills: skillDirs.length },    summary,    results: allResults,  };  if (options.json) {    console.log(formatJsonReport(report));  } else {    console.log(formatMarkdownReport(report));  }  process.exit(determineExitCode(summary));}main();