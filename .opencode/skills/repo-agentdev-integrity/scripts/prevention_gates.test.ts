import { describe, it, expect, beforeAll, afterAll } from "bun:test";import { mkdirSync, writeFileSync, copyFileSync, rmSync, existsSync } from "fs";import { join } from "path";const SCRIPT_DIR = import.meta.dir;const SCRIPT_FILE = join(SCRIPT_DIR, "check_integrity.ts");const CLI_UTILS_FILE = join(SCRIPT_DIR, "cli_utils.ts");const TEMP_BASE = join("C:", "WINDOWS", "TEMP", "opencode");const RUN_ID = `prevention-gate-test-${crypto.randomUUID().slice(0, 8)}`;const TEMP_ROOT = join(TEMP_BASE, RUN_ID);interface RunResult {  exitCode: number;  stdout: string;  stderr: string;}function runScript(cwd: string, args: string[]): RunResult {  const scriptPath = join(    cwd, ".opencode", "skills", "agentdev-integrity", "scripts", "check_integrity.ts"  );  const proc = Bun.spawnSync(["bun", "run", scriptPath, ...args], {    cwd,    stdout: "pipe",    stderr: "pipe",  });  return {    exitCode: proc.exitCode ?? -1,    stdout: proc.stdout?.toString("utf-8") ?? "",    stderr: proc.stderr?.toString("utf-8") ?? "",  };}function mkdirp(p: string): void {  mkdirSync(p, { recursive: true });}function writeFile(p: string, content: string): void {  mkdirp(p.split(/[\\/]/g).slice(0, -1).join("/")!);  writeFileSync(p, content, "utf-8");}function copyScripts(fixtureRoot: string): void {  const dest = join(fixtureRoot, ".opencode", "skills", "agentdev-integrity", "scripts");  mkdirp(dest);  copyFileSync(SCRIPT_FILE, join(dest, "check_integrity.ts"));  copyFileSync(CLI_UTILS_FILE, join(dest, "cli_utils.ts"));}function buildBaseFixture(root: string): void {  const reqDir = join(root, "docs", "requirements");  mkdirp(reqDir);  writeFileSync(join(reqDir, "REQ-0001.md"), [    "---",    "id: REQ-0001",    "title: Test REQ",    "created: 2025-01-01",    "updated: 2025-01-02",    "tags:",    "  - test",    "---",    "",    "## Body",    "",    "See ADR-0001.",    "",  ].join("\n"), "utf-8");  writeFileSync(join(reqDir, "README.md"), [    "# Requirements",    "",    "| ID | Title |",    "|----|-------|",    "| REQ-0001 | Test REQ |",    "",  ].join("\n"), "utf-8");  const adrDir = join(root, "docs", "adr");  mkdirp(adrDir);  writeFileSync(join(adrDir, "ADR-0001.md"), [    "---",    "id: ADR-0001",    "title: Test ADR",    "status: accepted",    "---",    "",    "Relates to REQ-0001.",    "",  ].join("\n"), "utf-8");  writeFileSync(join(adrDir, "README.md"), [    "# ADR Index",    "",    "| ADR | Title | Status |",    "|-----|-------|--------|",    "| ADR-0001 | Test ADR | accepted |",    "",  ].join("\n"), "utf-8");  const specsDir = join(root, "docs", "specs");  mkdirp(specsDir);  writeFileSync(join(specsDir, "system.md"), "# System\n\ntest-cmd\n", "utf-8");  writeFileSync(join(specsDir, "patterns.md"), "# Patterns\n", "utf-8");  const skillsDir = join(root, ".opencode", "skills");  mkdirp(join(skillsDir, "agentdev-test-skill"));  writeFileSync(join(skillsDir, "agentdev-test-skill", "SKILL.md"), [    "---",    "name: agentdev-test-skill",    "description: Test skill",    "---",    "",    "## USE FOR",    "Testing.",    "",    "## DO NOT USE FOR",    "Production.",    "",  ].join("\n"), "utf-8");  const cmdDir = join(root, ".opencode", "commands", "agentdev");  mkdirp(cmdDir);  writeFileSync(join(cmdDir, "test-cmd.md"), [    "---",    "description: Test command",    "agent: sisyphus",    "---",    "",    "## Steps",    "",    "1. Do something",    "",  ].join("\n"), "utf-8");  writeFileSync(join(cmdDir, "README.md"), [    "# Commands",    "",    "| Command | Description |",    "|---------|-------------|",    "| test-cmd | Test command |",    "",  ].join("\n"), "utf-8");  writeFileSync(join(root, "README.md"), "# Test Repo\n\ntest-cmd\n", "utf-8");  const vocabDir = join(root, ".agentdev");  mkdirp(vocabDir);  writeFileSync(join(vocabDir, "vocabulary-registry.md"), [    "# Vocabulary Registry",    "",    "| 譌ｧ隱槫ｽ・| 迴ｾ陦瑚ｪ槫ｽ・| 蛯呵・|",    "|--------|----------|------|",    "| req-backlog | 蟒・ｭ｢ | backlog-review/backlog-save |",    "",  ].join("\n"), "utf-8");}describe("Prevention gate regression tests", () => {  beforeAll(() => {    mkdirp(TEMP_ROOT);  });  afterAll(() => {    if (existsSync(TEMP_ROOT)) {      rmSync(TEMP_ROOT, { recursive: true, force: true });    }  });  describe("Pattern A/B/C/D residual detection", () => {    it("should detect Pattern A/B/C/D labels in active documents", () => {      const fixtureRoot = join(TEMP_ROOT, "pattern-residual-ng");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const specsDir = join(fixtureRoot, "docs", "specs");      writeFileSync(join(specsDir, "patterns.md"), [        "# Patterns",        "",        "## Pattern A: Bug fix",        "",        "This describes Pattern A for bug fixes.",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const patternFindings = report.results.filter(        (r: any) => r.check === "pattern-residual" && r.level !== "ok"      );      expect(patternFindings.length).toBeGreaterThan(0);    });    it("should not flag Pattern labels in retired REQ or test fixtures", () => {      const fixtureRoot = join(TEMP_ROOT, "pattern-residual-ok");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const retiredDir = join(fixtureRoot, "docs", "requirements", "retired");      mkdirp(retiredDir);      writeFileSync(join(retiredDir, "REQ-0050.md"), [        "---",        "id: REQ-0050",        "title: Old pattern REQ",        "created: 2025-01-01",        "updated: 2025-01-02",        "tags:",        "  - test",        "---",        "",        "Pattern A description in retired REQ.",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const patternFindings = report.results.filter(        (r: any) => r.check === "pattern-residual" && r.level === "ng"      );      expect(patternFindings.length).toBe(0);    });  });  describe("req-backlog residual detection", () => {    it("should detect req-backlog references in active documents", () => {      const fixtureRoot = join(TEMP_ROOT, "req-backlog-ng");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const cmdDir = join(fixtureRoot, ".opencode", "commands", "agentdev");      writeFileSync(join(cmdDir, "test-cmd.md"), [        "---",        "description: Test command",        "agent: sisyphus",        "---",        "",        "## Steps",        "",        "1. Process req-backlog items",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const backlogFindings = report.results.filter(        (r: any) => r.check === "req-backlog-residual" && r.level === "ng"      );      expect(backlogFindings.length).toBeGreaterThan(0);    });    it("should not flag req-backlog in vocabulary registry or exempt files", () => {      const fixtureRoot = join(TEMP_ROOT, "req-backlog-ok");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const backlogFindings = report.results.filter(        (r: any) => r.check === "req-backlog-residual" && r.level === "ng"      );      expect(backlogFindings.length).toBe(0);    });  });  describe("Abolished skill reference detection", () => {    it("should detect references to abolished skills", () => {      const fixtureRoot = join(TEMP_ROOT, "abolished-skill-ng");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const cmdDir = join(fixtureRoot, ".opencode", "commands", "agentdev");      writeFileSync(join(cmdDir, "test-cmd.md"), [        "---",        "description: Test command",        "agent: sisyphus",        "---",        "",        "Uses agentdev-workflow-reporting skill.",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const abolishedFindings = report.results.filter(        (r: any) => r.check === "abolished-skill-reference" && r.level === "ng"      );      expect(abolishedFindings.length).toBeGreaterThan(0);    });    it("should not flag references in test fixtures", () => {      const fixtureRoot = join(TEMP_ROOT, "abolished-skill-ok");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const abolishedFindings = report.results.filter(        (r: any) => r.check === "abolished-skill-reference" && r.level === "ng"      );      expect(abolishedFindings.length).toBe(0);    });  });  describe("REQ range staleness detection", () => {    it("should detect stale REQ range in AGENTS.md", () => {      const fixtureRoot = join(TEMP_ROOT, "req-range-stale");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      writeFileSync(join(fixtureRoot, "AGENTS.md"), [        "# AGENTS",        "",        "Active REQ: REQ-0001 through REQ-0005.",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const rangeFindings = report.results.filter(        (r: any) => r.check === "req-range-staleness" && r.level === "ng"      );      expect(rangeFindings.length).toBeGreaterThan(0);    });    it("should report OK when range is consistent", () => {      const fixtureRoot = join(TEMP_ROOT, "req-range-ok");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      writeFileSync(join(fixtureRoot, "AGENTS.md"), [        "# AGENTS",        "",        "Active REQ: REQ-0001 through REQ-0001.",        "",      ].join("\n"), "utf-8");      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const rangeFindings = report.results.filter(        (r: any) => r.check === "req-range-staleness" && r.level === "ng"      );      expect(rangeFindings.length).toBe(0);    });  });  describe("Vocabulary registry compliance", () => {    it("should report OK when vocabulary registry exists and is parseable", () => {      const fixtureRoot = join(TEMP_ROOT, "vocab-registry-ok");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const vocabFindings = report.results.filter(        (r: any) => r.check === "vocabulary-compliance" && r.level === "ok"      );      expect(vocabFindings.length).toBeGreaterThan(0);    });    it("should report info when vocabulary registry is missing", () => {      const fixtureRoot = join(TEMP_ROOT, "vocab-registry-missing");      mkdirp(fixtureRoot);      copyScripts(fixtureRoot);      buildBaseFixture(fixtureRoot);      const vocabPath = join(fixtureRoot, ".agentdev", "vocabulary-registry.md");      if (existsSync(vocabPath)) rmSync(vocabPath);      const result = runScript(fixtureRoot, ["--json"]);      const report = JSON.parse(result.stdout);      const vocabFindings = report.results.filter(        (r: any) => r.check === "vocabulary-compliance"      );      expect(vocabFindings.length).toBeGreaterThan(0);    });  });});