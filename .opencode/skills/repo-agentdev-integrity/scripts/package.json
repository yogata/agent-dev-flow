{
  "name": "agentdev-integrity-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-local integrity scripts (distribution boundary adapter, baseline, delta, IR-046/047/048 rules). The shared detector library lives in ./lib/.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.distribution-boundary.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0",
    "@types/node": "^22.0.0"
  }
}
