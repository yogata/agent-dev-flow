/** * REQ-0030-010: Test fixture validation for command E2E tests. * * Validates: * - Sample REQ file fixture produces expected parsed fields * - Sample issue body fixture matches template structure * - Sample draft fixture has valid draft-meta * - Sample intake item fixture has correct structure * - Real templates have valid variable placeholders * - Real REQ files have expected structure * - Real command definitions produce valid parsed output */import { describe, it, expect, beforeAll, afterAll } from "bun:test";import * as fs from "fs";import * as path from "path";const SCRIPT_DIR = import.meta.dir;const TEMP_BASE = path.join("C:", "WINDOWS", "TEMP", "opencode");function findRepoRoot(start: string): string {  let dir = path.resolve(start);  for (let i = 0; i < 20; i++) {    if (fs.existsSync(path.join(dir, ".opencode"))) return dir;    const parent = path.dirname(dir);    if (parent === dir) break;    dir = parent;  }  return path.resolve(start);}const REPO_ROOT = findRepoRoot(SCRIPT_DIR);const CMD_DIR = path.join(REPO_ROOT, ".opencode", "commands", "agentdev");const TEMPLATES_DIR = path.join(  REPO_ROOT,  ".opencode",  "skills",  "agentdev-workflow-templates",  "templates",);const REQ_DIR = path.join(REPO_ROOT, "docs", "requirements");// 笏笏笏 Fixture creation helpers 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏function createTempDir(): string {  if (!fs.existsSync(TEMP_BASE)) fs.mkdirSync(TEMP_BASE, { recursive: true });  return fs.mkdtempSync(path.join(TEMP_BASE, "e2e-fixtures-test-"));}const SAMPLE_REQ_MD = `\---id: REQ-9999title: "繝・せ繝育畑隕∽ｻｶ"created: "2026-01-01"updated: "2026-01-01"tags: [test, fixture]---## 逶ｮ逧・繝・せ繝医ヵ繧｣繧ｯ繧ｹ繝√Ε讀懆ｨｼ逕ｨ縺ｮ繝繝溘・隕∽ｻｶ縲・## 隕∽ｻｶ| ID | 隕∽ｻｶ ||---|---|| REQ-9999-001 | 繝・せ繝郁ｦ∽ｻｶ1・・HALL・・|| REQ-9999-002 | 繝・せ繝郁ｦ∽ｻｶ2・・HALL・・|## 驕ｩ逕ｨ遽・峇- **蟇ｾ雎｡**: 繝・せ繝医ヵ繧｣繧ｯ繧ｹ繝√Ε- **蟇ｾ雎｡螟・*: 譛ｬ逡ｪ迺ｰ蠅・`;const SAMPLE_DRAFT_MD = `\# 繝・せ繝医ラ繝ｩ繝輔ヨ## draft-meta- topic-slug: test-feature- pattern: B- scale: standard- status: draft## 逶ｮ逧・繝・せ繝育畑繝峨Λ繝輔ヨ縲・## 螳御ｺ・擅莉ｶ- [ ] 繝・せ繝医′騾壹ｋ`;const SAMPLE_INTAKE_ITEM_MD = `\# 繝・せ繝育畑 intake item## 隕ｳ貂ｬ繝・せ繝郁ｦｳ貂ｬ蜀・ｮｹ縲・## 莉雁屓謇ｱ繧上↑縺・炊逕ｱ莉悶・蜆ｪ蜈亥ｺｦ縺碁ｫ倥＞縲・## 蠖ｱ髻ｿ霆ｽ蠕ｮ縲・## 繝ｬ繝薙Η繝ｼ縺ｧ豎ｺ繧√ｋ縺薙→謗｡逕ｨ蜿ｯ蜷ｦ縲・## 譬ｹ諡・井ｻｻ諢擾ｼ・繝・せ繝育畑繝・・繧ｿ縲・`;const SAMPLE_ISSUE_BODY_FEATURE = `\## 讎りｦ・<!-- 縲仙ｿ・医・-->繝・せ繝域ｩ溯・縺ｮ讎りｦ・## 隱ｲ鬘・<!-- 縲仙ｿ・医・-->繝・せ繝郁ｪｲ鬘・## 謠先｡亥・螳ｹ<!-- 縲仙ｿ・医・-->繝・せ繝域署譯・## 螳御ｺ・擅莉ｶ<!-- 縲仙ｿ・医・-->- [ ] 繝・せ繝亥ｮ御ｺ・擅莉ｶ1- [ ] 繝・せ繝亥ｮ御ｺ・擅莉ｶ2## 繝・せ繝域姶逡･<!-- 縲仙ｿ・医・-->- [ ] 繝ｦ繝九ャ繝医ユ繧ｹ繝・ 繝・せ繝亥ｯｾ雎｡- [ ] E2E繝・せ繝・ 繝・せ繝亥ｯｾ雎｡`;const SAMPLE_ISSUE_BODY_BUG = `\## 讎りｦ・<!-- 縲仙ｿ・医・-->繝・せ繝医ヰ繧ｰ縺ｮ讎りｦ・## 蜀咲樟謇矩・<!-- 縲仙ｿ・医・-->1. 謇矩・2. 謇矩・## 譛溷ｾ・＆繧後ｋ蜍穂ｽ・<!-- 縲仙ｿ・医・-->譛溷ｾ・＆繧後ｋ蜍穂ｽ・## 螳御ｺ・擅莉ｶ<!-- 縲仙ｿ・医・-->- [ ] 繝舌げ縺御ｿｮ豁｣縺輔ｌ縺ｦ縺・ｋ`;// 笏笏笏 REQ-0030-010: Fixture validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏describe("REQ-0030-010: Test fixture existence and content validity", () => {  const tempDirs: string[] = [];  afterAll(() => {    for (const dir of tempDirs) {      fs.rmSync(dir, { recursive: true, force: true });    }  });  // 笏笏笏 REQ file fixture validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("REQ file fixture", () => {    it("parses REQ frontmatter fields correctly", () => {      const parts = SAMPLE_REQ_MD.split("---");      expect(parts.length).toBeGreaterThanOrEqual(3);      const yaml = parts[1].trim();      expect(yaml).toContain("id: REQ-9999");      expect(yaml).toContain("title:");      expect(yaml).toContain("created:");      expect(yaml).toContain("updated:");      expect(yaml).toContain("tags:");    });    it("contains required sections", () => {      expect(SAMPLE_REQ_MD).toContain("## 逶ｮ逧・);      expect(SAMPLE_REQ_MD).toContain("## 隕∽ｻｶ");      expect(SAMPLE_REQ_MD).toContain("## 驕ｩ逕ｨ遽・峇");    });    it("contains requirement table with IDs", () => {      expect(SAMPLE_REQ_MD).toContain("REQ-9999-001");      expect(SAMPLE_REQ_MD).toContain("REQ-9999-002");    });    it("writes and reads back correctly (UTF-8 round-trip)", () => {      const tempDir = createTempDir();      tempDirs.push(tempDir);      const filePath = path.join(tempDir, "REQ-9999.md");      fs.writeFileSync(filePath, SAMPLE_REQ_MD, "utf-8");      const readBack = fs.readFileSync(filePath, "utf-8");      expect(readBack).toBe(SAMPLE_REQ_MD);    });  });  // 笏笏笏 Draft fixture validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Draft fixture", () => {    it("contains draft-meta section", () => {      expect(SAMPLE_DRAFT_MD).toContain("## draft-meta");    });    it("draft-meta has required fields", () => {      const metaMatch = SAMPLE_DRAFT_MD.match(/## draft-meta\s*\n([\s\S]*?)(?=\n## |$)/);      expect(metaMatch).not.toBeNull();      const meta = metaMatch![1];      expect(meta).toContain("topic-slug:");      expect(meta).toContain("pattern:");      expect(meta).toContain("scale:");      expect(meta).toContain("status:");    });    it("draft-meta pattern is valid (A/B/C/D)", () => {      const patternMatch = SAMPLE_DRAFT_MD.match(/pattern:\s*([A-D])/);      expect(patternMatch).not.toBeNull();      expect(["A", "B", "C", "D"]).toContain(patternMatch![1]);    });    it("draft-meta scale is valid (standard/large)", () => {      const scaleMatch = SAMPLE_DRAFT_MD.match(/scale:\s*(\S+)/);      expect(scaleMatch).not.toBeNull();      expect(["standard", "large"]).toContain(scaleMatch![1]);    });    it("has completion checkboxes", () => {      expect(SAMPLE_DRAFT_MD).toMatch(/- \[ \]/);    });  });  // 笏笏笏 Intake item fixture validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Intake item fixture", () => {    it("has observation section", () => {      expect(SAMPLE_INTAKE_ITEM_MD).toContain("## 隕ｳ貂ｬ");    });    it("has reason-not-now section", () => {      expect(SAMPLE_INTAKE_ITEM_MD).toContain("## 莉雁屓謇ｱ繧上↑縺・炊逕ｱ");    });    it("has impact section", () => {      expect(SAMPLE_INTAKE_ITEM_MD).toContain("## 蠖ｱ髻ｿ");    });    it("has review-decision section", () => {      expect(SAMPLE_INTAKE_ITEM_MD).toContain("## 繝ｬ繝薙Η繝ｼ縺ｧ豎ｺ繧√ｋ縺薙→");    });    it("does not have frontmatter (per REQ-0017-033)", () => {      expect(SAMPLE_INTAKE_ITEM_MD.startsWith("---")).toBe(false);    });  });  // 笏笏笏 Issue body fixture validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Issue body fixture (feature)", () => {    it("has all required markers", () => {      const markers = SAMPLE_ISSUE_BODY_FEATURE.match(/<!--\s*縲仙ｿ・医曾s*-->/g);      expect(markers).not.toBeNull();      expect(markers!.length).toBeGreaterThanOrEqual(4);    });    it("has completion checkboxes in 螳御ｺ・擅莉ｶ section", () => {      expect(SAMPLE_ISSUE_BODY_FEATURE).toContain("## 螳御ｺ・擅莉ｶ");      expect(SAMPLE_ISSUE_BODY_FEATURE).toMatch(/- \[ \] 繝・せ繝亥ｮ御ｺ・擅莉ｶ/);    });    it("matches template structure (issue_desc_feature.md)", () => {      const templatePath = path.join(TEMPLATES_DIR, "issue_desc_feature.md");      if (!fs.existsSync(templatePath)) return;      const template = fs.readFileSync(templatePath, "utf-8");      const templateRequiredHeadings = template.match(/## [^\n]+(?=\s*\n<!--\s*縲仙ｿ・医・/g) || [];      for (const heading of templateRequiredHeadings) {        expect(SAMPLE_ISSUE_BODY_FEATURE).toContain(heading.trim());      }    });  });  describe("Issue body fixture (bug)", () => {    it("has all required markers", () => {      const markers = SAMPLE_ISSUE_BODY_BUG.match(/<!--\s*縲仙ｿ・医曾s*-->/g);      expect(markers).not.toBeNull();      expect(markers!.length).toBeGreaterThanOrEqual(3);    });    it("has 蜀咲樟謇矩・section", () => {      expect(SAMPLE_ISSUE_BODY_BUG).toContain("## 蜀咲樟謇矩・);    });  });  // 笏笏笏 Real REQ files validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Real REQ files structure", () => {    it("REQ directory exists and contains files", () => {      expect(fs.existsSync(REQ_DIR)).toBe(true);      const files = fs.readdirSync(REQ_DIR).filter((f) => f.startsWith("REQ-") && f.endsWith(".md"));      expect(files.length).toBeGreaterThan(0);    });    it("each REQ file has required frontmatter fields", () => {      const files = fs.readdirSync(REQ_DIR)        .filter((f) => f.startsWith("REQ-") && f.endsWith(".md"))        .sort();      for (const file of files) {        const content = fs.readFileSync(path.join(REQ_DIR, file), "utf-8");        const fmMatch = content.match(/^---\s*\n([\s\S]*?)\n---/);        expect(fmMatch).not.toBeNull();        const fm = fmMatch![1];        expect(fm).toContain("id:");        expect(fm).toContain("title:");        expect(fm).toContain("created:");      }    });    it("each REQ file has required sections", () => {      const files = fs.readdirSync(REQ_DIR)        .filter((f) => f.startsWith("REQ-") && f.endsWith(".md"))        .sort();      for (const file of files) {        const content = fs.readFileSync(path.join(REQ_DIR, file), "utf-8");        expect(content).toContain("## 逶ｮ逧・);        expect(content).toContain("## 隕∽ｻｶ");      }    });  });  // 笏笏笏 Real templates validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Real templates have valid structure", () => {    it("templates directory exists", () => {      expect(fs.existsSync(TEMPLATES_DIR)).toBe(true);    });    it("issue templates have variable placeholders or fixed structure", () => {      const issueTemplates = ["issue_desc_feature.md", "issue_desc_bug.md"];      for (const tmpl of issueTemplates) {        const tmplPath = path.join(TEMPLATES_DIR, tmpl);        if (!fs.existsSync(tmplPath)) continue;        const content = fs.readFileSync(tmplPath, "utf-8");        // Templates should have some structure markers        expect(content).toContain("<!-- 縲仙ｿ・医・-->");      }    });    it("comment templates have content structure", () => {      const commentTemplates = [        "issue_comment_bug_analysis.md",        "issue_comment_bug_record.md",        "issue_comment_feature_technical.md",        "issue_comment_feature_implementation.md",        "issue_comment_update.md",        "issue_comment_review_ng.md",      ];      for (const tmpl of commentTemplates) {        const tmplPath = path.join(TEMPLATES_DIR, tmpl);        if (!fs.existsSync(tmplPath)) continue;        const content = fs.readFileSync(tmplPath, "utf-8");        expect(content.trim().length).toBeGreaterThan(0);      }    });    it("pr_desc template exists and has content", () => {      const tmplPath = path.join(TEMPLATES_DIR, "pr_desc.md");      expect(fs.existsSync(tmplPath)).toBe(true);      const content = fs.readFileSync(tmplPath, "utf-8");      expect(content.trim().length).toBeGreaterThan(0);    });  });  // 笏笏笏 Command definition parse validation 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏  describe("Command definitions produce valid parsed output", () => {    const cmdFiles = fs.existsSync(CMD_DIR)      ? fs.readdirSync(CMD_DIR).filter((f) => f.endsWith(".md") && f !== "README.md").sort()      : [];    for (const file of cmdFiles) {      describe(`parsed output: ${file}`, () => {        const content = fs.readFileSync(path.join(CMD_DIR, file), "utf-8");        it("frontmatter produces non-null parse result", () => {          const parts = content.split("---");          expect(parts.length).toBeGreaterThanOrEqual(3);          const fm = parts[1].trim();          expect(fm.length).toBeGreaterThan(0);        });        it("body has at least one ## heading after frontmatter", () => {          const body = content.split("---").slice(2).join("---").trim();          const headings = body.match(/^## /gm);          expect(headings).not.toBeNull();          expect(headings!.length).toBeGreaterThan(0);        });        it("body contains at least one numbered step reference", () => {          const body = content.split("---").slice(2).join("---");          const hasNumberedStep = /(^|\n)\s*\d+\.\s/.test(body) || /Step\s+\d/.test(body);          expect(hasNumberedStep).toBe(true);        });      });    }  });});