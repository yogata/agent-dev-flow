{
  "name": "distribution-boundary-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Stage B pre-write distribution boundary guard plugin. Wires the canonical side-effect-free detector into OpenCode's tool.execute.before hook for write/edit/apply_patch. Fail-fast adapter (DEC-014 decision 3); inspection errors are gate-not-passed, NOT clean (DEC-014 decision 5).",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0",
    "@types/node": "^22.0.0"
  }
}
