---
id: ADR-0105
title: "OpenCode Source / Projection 分離"
status: accepted
created: "2026-06-08"
updated: "2026-06-08"
---

# ADR-0105: OpenCode Source / Projection 分離

## 背景

AgentDevFlowのcommand/skill/template sourceが.opencode/直下に存在し、source code（編集対象）とruntime projection（実行対象）が同一directoryに混在している。
これにより以下の問題がある:
- source変更の影響範囲が不明確
- consumer projectへの配布方法が定義できない
- integrity検査の対象がsourceとprojectionで区別できない

## 決定

1. AgentDevFlow本体のcommand/skill/template sourceをsrc/opencode/に配置する
2. 本体repoの.opencode/はself-hosting projectionとする
3. .opencode/は廃止しない（runtime入口として維持）
4. agentdev command namespaceおよびagentdev-* skill prefixを本体用予約名とする
5. 初回移行はmigration scriptで実行し、日常同期はsync scriptで実行する。
   両scriptの責務を分ける（migrationはone-time、syncは継続的）

## 結果・影響

### 正の影響

- source変更はsrc/opencode/で行い、.opencode/はprojectionとして自動同期される
- consumer projectでは.opencode/がproject-local customization入口として機能する
- integrity検査がsourceとprojectionを区別できる

### 負の影響

- symlinkが利用できない環境ではcopy-based projectionにfallbackが必要
- 開発workflowにsync手順が追加される
- 既存のpath参照を含むdocsの更新が必要

> ※symlink/copy fallbackの詳細仕様、migration手順、Windows環境リスクの詳細はREQ-0103、REQ-0108を参照。

## 関連する決定

- ADR-0019（retired — 本判断の前身）
- ADR-0102（runtime / authoring 関心分離 — runtime/authoring分離の具体化）
- ADR-0103（文書種別責務境界 — 文書種別責務境界の物理層での裏付け）
- ADR-0104（runtime 独立性 — runtime独立性のsource/projection分離による実現）

---

## 内容の圧縮

本ADRは技術判断・採否理由・影響のみを残し、REQ/SPEC相当の詳細を分離した。

- **分離内容**: symlink/copy fallbackの詳細仕様、migration手順、Windows環境リスクの詳細
- **移管先**: REQ-0103（Artifact責任分界）、REQ-0108（Integrity/Validation）
- **圧縮日**: 2026-06-08
- **根拠要件**: REQ-0112-032
