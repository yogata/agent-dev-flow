---
id: ADR-0135
title: "Project Extensions Architecture"
status: accepted
created: "2026-07-04"
updated: "2026-07-04"
---

# ADR-0135: Project Extensions Architecture

## 背景

AgentDevFlow の配布コマンド・スキル本文には、AgentDevFlow リポジトリ自身の ADR/REQ/SPEC への参照が混入していた。配布対象の consumer プロジェクトではこれらの参照は解決できず、配布物としての自己完結性を損なう。

各プロジェクトでは、AgentDevFlow が提供する標準 command/skill に対してプロジェクト固有の文脈、規約、検査、受け入れゲート、禁止事項を追加したい。この追加・拡張は、標準 command/skill 本文へ直接書き込むのではなく、プロジェクト側の設定層で与える必要がある。

単なる docs 参照リストでは、追加・拡張設定層としての機能を十分に提供しない。

## 決定

project-extensions を、標準 command/skill へのプロジェクト固有追加・拡張の設定層として採用する。.agentdev/extensions/** は context, rules, checks, acceptance_gates, must_not の5セクションを提供し、docs 参照リストの機能を包含しつつ、より広い拡張機能を提供する。

extension は標準 command/skill の上書きではなく、追加・拡張のみとする。command/skill は実行時に対応 extension のみを読み、不在時は標準動作、破損時はエラー表示+無視+標準動作とする。

command/skill 本文に ADR/REQ/SPEC の具体参照を記述しないルールを徹底する。.agentdev/extensions/** 内では当該プロジェクトの ADR/REQ/SPEC 参照を許可する。

ADR-0104（実行時独立性）に relates-to し、supersede しない。

旧 doc-inputs 機構から project-extensions への移行は資産流用を前提とした一括置換とする。schema・配置・命名の3点で非互換であるため、旧機構と extensions の段階的併存は行わず、置換完了後に旧機構残存を検出する方針とする（inspect-extensions の責務）。先の設計は accepting から短期間に全面置換されたが、schema 非互換による併存複雑化を回避するには妥当な判断であり、agent-dev-flow リポジトリの文化（ADR-0126→ADR-0131 の前例）に照らして許容範囲とする。

## 検討した代替案

### 代替案 A: 旧機構を拡張する

旧 doc-inputs 機構に context, rules 等のセクションを追加する。採用しない。旧機構は「参照リスト」としての契約が明確であり、別の関心（追加・拡張設定）を混入させると責務が曖昧になるため。

### 代替案 B: DOC-MAP に拡張情報を持たせる

採用しない。DOC-MAP は探索索引であり、command/skill 別の拡張設定を持たせると責務が混入するため。

## 結果

- 配布 command/skill 本文は ADR/REQ/SPEC 具体参照を持たなくなる。
- プロジェクトは .agentdev/extensions/** で独自の規約、検査、禁止事項を追加できる。

## 関連

- ADR-0104: 実行時独立性（relates-to、supersede しない）
