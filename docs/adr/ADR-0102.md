---
id: ADR-0102
title: "runtime / authoring 関心分離"
status: accepted
created: "2026-06-08"
updated: "2026-06-08"
---

# ADR-0102: runtime / authoring 関心分離

## 背景

AgentDevFlow の command frontmatter には implementation_pattern、secondary_pattern、load_skills が含まれていた。これらは agent-dev-flow レポジトリの開発用メタデータであり、個別プロジェクトで command を実行する際には不要である。しかし docs-check で必須検査の対象となっており、runtime と authoring の関心が混在していた。

## 決定

command frontmatter を runtime 配布物として扱い、許可フィールドを description と agent のみに制限する。implementation_pattern、secondary_pattern、load_skills は frontmatter から削除し、dev メタデータは design-principles.md や skill references など agent-dev-flow レポジトリ内の管理物に限定する。

> ※禁止フィールドの具体的一覧、integrity検査の必須→存在禁止へのreversal詳細仕様はREQ-0103、REQ-0108を参照。

runtime 配布物は自己完結し、agent-dev-flow レポジトリの dev メタデータに依存しないことを保証する。

## 代替案

- frontmatter に implementation_pattern を残しつつ optional 化する: 廃止予定のフィールドを残すと移行コストが継続する
- 別ファイルに dev メタデータを分離する: 管理対象が増え、整合性維持のコストが上がる

## 結果・影響

- 全 command frontmatter から implementation_pattern / secondary_pattern / load_skills を削除する必要がある
- docs-check の該当検査を「必須」から「存在禁止」に reversal する必要がある
- REQ-0103、REQ-0108 の該当 items を更新する必要がある
- design-principles.md に分類定義を移譲する必要がある

## 関連する決定

- ADR-0013（retired — 本判断の前身）
- ADR-0101（AgentDevFlow plugin namespace 統一 — 責務分界の具体化）
- ADR-0103（文書種別責務境界 — 文書体系への適用）

---

## 内容の圧縮

本ADRは技術判断・採否理由・影響のみを残し、REQ/SPEC相当の詳細を分離した。

- **分離内容**: 禁止フィールドの具体的一覧、integrity検査の必須→存在禁止へのreversal詳細仕様
- **移管先**: REQ-0103（Artifact責任分界）、REQ-0108（Integrity/Validation）
- **圧縮日**: 2026-06-08
- **根拠要件**: REQ-0112-032
