---
id: REQ-0106
title: "Case実行・完了"
created: "2026-05-30"
updated: "2026-06-10"
tags: [case-run, case-close, epic, reliability, completion-gate]
---

## 目的

case-run / case-close の実行信頼性、並列・Epic実行、完了ゲート、post-run回収を定義する。

## 要件

| ID | 要件 |
|---|---|
| REQ-0106-001 | `case-run` は準備・実装・提出の再開可能なフェーズを提供すること（SHALL） |
| REQ-0106-002 | `case-run` は作業用 worktree で実装し、PRを作成すること（SHALL） |
| REQ-0106-003 | 複数Caseの並列実行は依存関係を分析し、安全なWave単位で実行すること（SHALL） |
| REQ-0106-004 | Epic実行では親Issueの実行順序をSSoTとし、子Issueの実行状態と整合させること（SHALL） |
| REQ-0106-005 | `case-run` はチェックボックス更新など外部書き込みの成功を確認してから完了扱いにすること（SHALL） |
| REQ-0106-006 | `case-run` は関連ドキュメント更新候補を実装計画に組み込むこと（SHALL） |
| REQ-0106-007 | `case-run` は実装diffから関連キーワードを抽出し、SPEC等に矛盾がないか確認すること（SHOULD） |
| REQ-0106-008 | `case-run` は本筋外FindingをPR本文の `## Findings / Capture候補` セクションに `### intake` と `### learning` 小見出しで分類して記録すること（SHALL） |
| REQ-0106-009 | `case-close` は未チェック項目の達成判定を実行し、達成済み項目を更新すること（SHALL） |
| REQ-0106-010 | `case-close` は要件・成果物・SPEC・DOC-MAPの整合確認を行うこと（SHALL） |
| REQ-0106-011 | `case-close` は ADR が必要な判断に対して ADR 作成済みかを確認すること（SHALL） |
| REQ-0106-012 | `case-close` は PR merge、Issue close、domain state 永続化、branch / worktree cleanup の結果を分離して報告すること（SHALL） |
| REQ-0106-013 | `case-close` は PR本文の `## Findings / Capture候補` セクションから intake / learning を分離回収し、intake 候補を `.agentdev/intake/inbox/` に、learning 候補を `.agentdev/learning/inbox.md` に保存すること（SHALL） |
| REQ-0106-014 | branch / worktree 削除失敗時は警告して停止し、危険な自動削除を行わないこと（SHALL） |
| REQ-0106-015 | `case-close` は `git pull --ff-only` 前にローカル変更を検出した場合、自動で `git checkout --` により変更をリセットしてはならないこと（SHALL） |
| REQ-0106-016 | `case-close` は pull 前ローカル変更を検出した場合、対象ファイルと状態を表示し、構造化エラーで停止すること（SHALL） |
| REQ-0106-017 | `case-close` は pull 前ローカル変更の自動リセット対象を「PRで削除されたファイル」に限定する旧規則を廃止すること（SHALL） |
| REQ-0106-018 | Epic Orchestrator の全Wave完了後、子Issueのステータスを一括更新すること（SHALL） |
| REQ-0106-019 | 後続Wave開始前に `git rebase origin/main` を実行し、コンフリクト時は停止してユーザー判断に委ねること（SHALL） |
| REQ-0106-020 | 委譲プロンプトに Read tool による対象ファイルの事後検証（変更反映確認）を MUST ステップとして含めること（SHALL） |
| REQ-0106-021 | Step 8 乖離検出において、削除・変更対象キーワードの `docs/` 全体 grep を必須ステップとすること（SHALL） |
| REQ-0106-022 | `case-close` Step 8 で Epic を検出した際、G04 自動クローズ判定を実行する前に全子 Issue の OPEN/CLOSED 状態を事前取得し、状態一覧をログ出力すること（SHALL） |
| REQ-0106-023 | G04 自動クローズ判定のロジック自体は変更しないこと（SHALL） |
| REQ-0106-024 | `case-close` Step 4 で squash merge が失敗した場合、5秒間隔で最大5回自動リトライすること（SHALL） |
| REQ-0106-025 | リトライ全失敗後、rebase workflow（`git rebase origin/main` → force-push）へのフォールバックを提示し、ユーザー判断に委ねること（SHOULD） |
| REQ-0106-026 | push 操作において `--force` を禁止し、`--force-with-lease` のみを許可すること（SHALL） |
| REQ-0106-027 | `agentdev-workflow-orchestration` スキルの Wave scheduling 手順にファイル変更範囲の非重複チェックを追加すること（SHALL） (FPR-030) |
| REQ-0106-028 | `agentdev-workflow-orchestration/references/self-healing-and-errors.md` にマージコンフリクト予防のガイダンスを追加すること（SHALL） (FPR-031) |
| REQ-0106-029 | SKILL.md は500行以下を維持すること（SHALL）。詳細は references/ に配置 (FPR-032) |
| REQ-0106-030 | Epic 子Issue ステータスに ⏭スキップ を終了状態として定義すること（SHALL）。⏭スキップ は前提条件未達・依存関係未充足等で子Issueの実行をスキップした場合に Orchestrator が設定する。スキップ理由は Epic 本文に記録すること（SHALL）。⏭スキップ の子Issueは case-close の完了判定・Epic自動クローズ判定で完了と同等の終了状態として扱うこと（SHALL） |

## Update Notes

| 日付 | 対象 | 変更内容 |
|------|------|----------|
| 2026-06-10 | REQ-0106-008, 013 | UPDATE: セクション名 `## Findings / Capture候補` 反映・intake/learning 分離回収の明文化（Issue #701） |
| 2026-06-07 | REQ-0106-027-029 | APPEND: Wave scheduling ファイル非重複チェック・マージコンフリクト予防ガイダンス・SKILL.md 500行上限（8RU統合 / RU-0044） |
| 2026-06-09 | REQ-0106-030 | APPEND: Epic子Issue ⏭スキップ 終了状態の正式定義（Issue #697 docs-consistency-remediation） |

## 適用範囲

- **対象**: case-run、case-close、Epic/Wave実行、完了ゲート、post-run capture、worktree cleanup
- **対象外**: req-define / req-save、intake / learning の詳細処理
