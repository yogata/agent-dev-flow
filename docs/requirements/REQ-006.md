---
id: REQ-006
title: "Case実行オーケストレーション"
created: "2026-07-25"
updated: "2026-07-28"
---

## 目的

AgentDevFlow 実行フェーズにおける Issue、PR、Case の実行オーケストレーションと Epic/Wave 制御を所有する。
case-open、case-run、case-close、case-update、case-auto の各 command の責務境界と、Epic Issue を実行順序の SSoT（唯一の情報源）とするオーケストレーション全体を統括する。
工程接続のプロトコル層（マクロフェーズ構成、SSoT 遷移、work_type 分類、scale 評価）は REQ-005 が所有し、本 REQ は個別 command の実行オーケストレーションを所有する。
委譲時の判断、承認、副作用発動の境界は REQ-003 が所有し、配布成果物の責務境界と委譲プロトコルの配布物側正規所有は REQ-002 が所有する。
完了証明と成果物品質ゲートは REQ-007 が所有し、draft、RU、要件doc の構造化契約と lifecycle は REQ-008 が所有する。
external execution boundary は REQ-011-017 が、harness execution mechanism は REQ-011-018 が所有し、本 REQ の orchestration 実装はこれらの境界へ委譲する。
本 REQ が所有する安定外部契約の詳細実装（Wave 構成、execution_unit 並列モデル、委譲契約、結果状態機械、コンフリクト解消レベル、template 構造、実行安全境界の数値）は SPEC を正規所有者とし、本 REQ は境界宣言へ縮約する。

## 要件

| ID | 要件 |
|---|---|
| REQ-006-001 | case-open は合意済み要件doc を入力とし GitHub Issue 本文を生成して対象 REQ 番号を埋め込むこと |
| REQ-006-002 | case-open は入力要件doc 数、scale、operation_unit 構成に基づき Standard flow と Epic flow をルーティングすること |
| REQ-006-003 | case-open は合意済み要件doc 全体を入力とし Epic、Wave、Issue、依存関係、初期 status を自律生成すること |
| REQ-006-004 | case-open は機能要件、非機能要件、制約、対象外、受け入れ条件を新規に作成せず合意済み入力を反映すること |
| REQ-006-005 | case-open は実装順序、Issue 分解、Wave 構成をユーザー確認事項とせず自律生成すること |
| REQ-006-006 | case-open は要件が曖昧で Issue 構造を生成できない場合、停止すること |
| REQ-006-007 | case-open は依存グラフの連結成分（必須依存のみをエッジとする）を Epic 候補の出発点とすること |
| REQ-006-008 | case-open は依存強度、Epic サイズ、機能的一貫性の3軸で最終 Issue 構成を自律生成し複数 Standard、複数 Epic、混在のいずれも作成できること |
| REQ-006-009 | case-open は単独根（1 operation_unit だけの連結成分）を Epic 化せず Standard flow として扱うこと |
| REQ-006-010 | case-open は Epic サイズ上限と Wave 同時実行上限を実行安全境界として遵守すること（数値の詳細は SPEC） |
| REQ-006-011 | case-open は無関係な operation_unit 群を単一 Epic へ機械的に集約しないこと |
| REQ-006-012 | case-open は Epic 構成推論の根拠を Epic Issue 本文に記録すること |
| REQ-006-013 | case-open は Epic Issue 本文の Wave テーブルに各子 Issue の実行方法（並列、直列）を技術的依存関係に基づいて明記すること |
| REQ-006-014 | case-open は Epic 分解時に既存オープン Issue とのスコープ重複を検知し重複する子 Issue 生成をスキップまたはユーザー確認すること |
| REQ-006-015 | case-open は完了条件と事前状態の記載を識別子中心とし変動しやすい実測値スナップショットは補助値とすること |
| REQ-006-016 | case-open は完了条件を Issue 本文に展開する前に最新状態を再確認し差異がある場合は最新状態を優先すること |
| REQ-006-017 | case-open は Issue 本文をファイル経由で扱い Markdown 行構造を保持すること（詳細は SPEC） |
| REQ-006-018 | case-open は構成確定後かつ GitHub Issue 作成前に構成検証（上限、依存維持、全割当）を実行し上限超過または構成不備を検出した場合停止すること |
| REQ-006-019 | case-open は Issue 作成と VERIFY 成功後に RU を削除する唯一のコマンドであること |
| REQ-006-020 | case-open は RU ファイル削除後に main 作業ディレクトリとリモートの同期を確認し不一致を検出した場合停止すること |
| REQ-006-021 | case-open は自工程で実観測した deviation を Split Rule で分類して intake/learning のいずれかへ保存すること（case-close への capture 委譲は廃止） |
| REQ-006-022 | case-run は準備、実装、提出の再開可能なフェーズを提供すること |
| REQ-006-023 | case-run は作業用 worktree で実装し PR を作成すること |
| REQ-006-024 | case-run はチェックボックス更新など外部書き込みの成功を確認してから完了扱いとすること |
| REQ-006-025 | case-run は変更影響候補を実装計画に組み込むこと |
| REQ-006-026 | case-run の QG-3 は PR 作成直前の実装充足と乖離ゲートに限定し品質メトリクス収集や docs 全体検査を含まないこと |
| REQ-006-027 | case-run は単一 Issue または単一 Wave を処理し他 Issue の実装履歴や Epic 全体を前提としないこと |
| REQ-006-028 | case-run は実装作業を実行担当サブエージェントへ委譲し自らは実装しないこと（起動手段は harness 責務） |
| REQ-006-029 | case-run から委譲された実行担当サブエージェントは test strategy 項目ごとの検証、不合格時の処置、全項目処理までの反復を担当すること |
| REQ-006-030 | case-run は実装作業開始前に QG-3 前置の staleness check（参照ファイル現行存在確認、検査結果件数の再計測）を実行すること |
| REQ-006-031 | case-run は staleness check で差異を検出した場合でも Issue 本文を単独で書き換えず case-update へ委譲すること |
| REQ-006-032 | case-run は PR 対象ファイルに docs 変更を含む場合 docs 整合性検査を実行し結果を PR 本文に記録して case-update へ連携すること |
| REQ-006-033 | case-run は本筋外の検出事項を PR 本文の Findings セクションに intake と learning の小見出しで分類して記録すること |
| REQ-006-034 | case-run は intake ドメイン状態と learning ドメイン状態を読み取り専用とし直接変更しないこと |
| REQ-006-035 | case-run から case-close への capture 情報引き継ぎは PR 本文のみを経由し一時会話コンテキストや中間ファイル経由を排除すること |
| REQ-006-036 | case-run は Epic Issue 指定時 Epic Issue 本文から現在 Wave の実行可能子 Issue を特定し並列委譲すること（同時起動上限は SPEC） |
| REQ-006-037 | case-run は Epic Issue 再指定時 Epic Issue 本文から進行状況を判定し未完了の次 Wave を処理すること（べき等） |
| REQ-006-038 | case-run は委譲前後にタイムスタンプを記録し実行時間を結果に含めること |
| REQ-006-039 | case-close は未チェック項目の達成判定を実行し達成済み項目を更新すること |
| REQ-006-040 | case-close は要件、成果物、SPEC、DOC-MAP の整合確認と ADR 必要判断に対する ADR 作成済み確認を行うこと |
| REQ-006-041 | case-close は PR マージ、Issue クローズ、ドメイン状態永続化、branch と worktree クリーンアップの結果を分離して報告すること |
| REQ-006-042 | case-close は PR 本文の capture セクションから intake と learning を分離回収し各ドメイン状態に保存すること |
| REQ-006-043 | case-close における intake と learning 候補の入力源は PR 本文のみとし一時会話コンテキストを排除すること |
| REQ-006-044 | case-close における学びの検知はエージェント自律としユーザーへの有無問い合わせを行わないこと |
| REQ-006-045 | case-close は PR マージ前にローカル未コミット変更と対象 PR 変更ファイルの重複を検出し重複時は停止またはユーザー対応を促すこと |
| REQ-006-046 | case-close は pull 前にローカル変更を検出した場合変更を保持したまま停止し自動リセットを行わないこと |
| REQ-006-047 | case-close は branch または worktree 削除失敗時は警告して停止し危険な自動削除を行わないこと |
| REQ-006-048 | case-close は squash merge 失敗時に rebase による機械的コンフリクト解消を試みること |
| REQ-006-049 | case-close は rebase でコンフリクトが発生した場合実装変更を行わず上級オーケストレータへエスカレーションすること |
| REQ-006-050 | case-close は PR マージ前に mergeable 状態を確認し不明の場合は確定まで待機し上限超過時はマージ中止して停止すること（待機の詳細は SPEC） |
| REQ-006-051 | case-close は git main 同期時に worktree 状態、ref lock 競合、非 main ブランチ占有のリスクを事前検出し安全な代替同期手順を選択すること |
| REQ-006-052 | case-close は全 test strategy 項目が合格済みまたは Findings 記録済みであることを完了条件とし未処理項目が残る場合完了扱いしないこと |
| REQ-006-053 | case-close は Epic Issue 指定時 Epic Issue 本文から現在 Wave の PR 作成済み子 Issue を特定し PR マージと子 Issue クローズを行うこと |
| REQ-006-054 | case-close は最終 Wave 処理時 Epic Issue 自体をクローズし Epic レベルの最終処理を行うこと |
| REQ-006-055 | case-close は最終 Wave 以外の処理時残 Wave 状況を通知すること |
| REQ-006-056 | case-close は中間 Wave 終了時に当該 Wave の PR 対象範囲の完了条件のみ評価更新し最終 Wave で Epic 全体の完了条件を評価すること |
| REQ-006-057 | case-close は完了条件評価更新後に Issue 本文を再読込し未達チェックボックスが残る場合構造化エラーで停止すること |
| REQ-006-058 | case-close は乖離検出で削除、変更対象キーワードの docs 全体検索を必須ステップとすること |
| REQ-006-059 | case-close は Epic 自動クローズ判定の前に全子 Issue の open と closed 状態を事前取得すること |
| REQ-006-060 | case-update は Issue 本文更新、コメント追加、REQ ファイル更新、レビュー NG 対応のいずれかを実行すること |
| REQ-006-061 | case-update は CI/CD 修正と自律修正ループを管轄外とし case-run の責務に委譲すること |
| REQ-006-062 | case-update の Issue 本文更新は Issue 作成時に使用されたテンプレート構造を維持し必須セクションの欠落を防ぐこと |
| REQ-006-063 | case-update はフェーズを維持し Issue 番号をユーザー入力またはセッション内会話からのみ取得すること |
| REQ-006-064 | case-update は SSoT の整合性を維持し Issue 本文と要件doc の不整合を防ぐこと |
| REQ-006-065 | case-auto は要件doc または GitHub Issue を入力とし後続工程を自走する追加入口として位置づけ標準フローを置換しないこと |
| REQ-006-066 | case-auto の入力解決は引数種別（要件doc パス、セッション指定、数値、Issue URL）に応じて処理対象を特定し特定不可時は停止すること |
| REQ-006-067 | case-auto は draft 内の artifact_actions の有無に基づき工程を決定し work_type 固定分岐を採用しないこと |
| REQ-006-068 | case-auto の自走対象は repo にファイルとして残る変更に限定すること |
| REQ-006-069 | case-auto は repo 外実体変更（DB マイグレーション実行、デプロイ、cloud apply、外部 SaaS 設定変更、認証情報変更等）を自走対象外とすること |
| REQ-006-070 | case-auto はマイグレーションファイルと IaC ファイルの作成修正を自走対象としマイグレーション実行と IaC apply を自走対象外とすること |
| REQ-006-071 | case-auto は req-save、spec-save、case-open、case-close を各 command の委譲契約に従ってサブエージェントへ委譲すること |
| REQ-006-072 | case-auto は case-run をインライン実行し case-run の command 定義を権威情報源として読み込むこと（起動手段は harness 責務） |
| REQ-006-073 | case-auto は各工程の command 定義を権威情報源として委譲先に読み込ませ工程固有手続きの再実装を回避すること |
| REQ-006-074 | case-auto は operation_unit キューの管理と制御のみを担い operation_unit 本文の抽出、変換、REQ 操作解釈を行わないこと |
| REQ-006-075 | case-auto は必須依存のない execution_unit を並列実行し complete-then-next の逐次処理を行わないこと |
| REQ-006-076 | case-auto は同一 Epic 内の Wave 間を直列実行すること |
| REQ-006-077 | case-auto は depends_on（必須依存のみ）に基づいて直列化要因を判定し recommended_order を直列化ゲートとしないこと |
| REQ-006-078 | case-auto は blocked になった execution_unit だけを停止し他の ready 対象を継続すること |
| REQ-006-079 | case-auto は全対象が closed、blocked、failed になったら終了し一部 blocked 残存時は partial blocked として報告すること |
| REQ-006-080 | case-auto はクリティカルパス解析と優先順位制御を行わず実行可否を依存関係のみで判定すること |
| REQ-006-081 | case-auto は永続状態から進行と再開を行い Epic 全体の実装詳細を親コンテキストに累積しないこと |
| REQ-006-082 | case-auto は case-open が生成した Epic、Wave、Issue 構造に従って進行すること |
| REQ-006-083 | case-auto は case-open 前だけ req_draft を orchestration pre-reader として読み、case-open 成功後は invalid post-case reader として req_draft を読まないこと。case-open 成功後の停止、再開、完了処理は Issue と Epic だけで成立させること |
| REQ-006-084 | case-auto は case-open 完了後にクリーンアップ検証ゲート（ドラフト残存、RU 残存の検証）を実行し残存を検出した場合停止すること |
| REQ-006-085 | case-auto は停止時に完了済み、進行中、未実行の各委譲単位と再開に必要な次コマンドを報告すること |
| REQ-006-086 | case-auto は停止時に停止理由を分類（合意要件逸脱、スコープ拡大、repo 外実体変更、command 契約不整合等）で報告すること |
| REQ-006-087 | case-auto は実行開始時刻と完了報告生成時刻を記録し工程別の壁時計時間を完了報告に含めること |
| REQ-006-088 | case-auto は委譲工程の結果（Issue/PR 番号、保存済みファイル、pass/warn/fail）のみを受領し工程内部の調査過程や中間ログを親コンテキストに累積しないこと |
| REQ-006-089 | case-auto は orchestration stage モデル（stage 1 case-open 順次、stage 2 case-run 並列、stage 3 case-close 順次）を採用し各 orchestration stage を前 stage 完了後に開始するとともに case-run internal lifecycle（state machine、self-healing loop 等）を複製せず case-run 側の正規所有に委譲すること |
| REQ-006-090 | case-auto は orchestration stage 1 と 3 を直列集約ポイントとし main push、capture、commit を並列実行区間の外で処理すること |
| REQ-006-091 | case-auto の orchestration stage 2 における case-run 同時起動数は固定値とし実行安全境界として遵守すること（数値は SPEC） |
| REQ-006-092 | case-auto の並列実行はデフォルト挙動とし順次実行はフォールバック時にのみ許可しフォールバック理由を完了報告に含めること |
| REQ-006-093 | case-auto は background task が破棄された場合 commit 済み PR 未作成状態と未コミット変更残存状態を区別し対応する回復パターンを適用すること |
| REQ-006-094 | case-auto は case-close からのコンフリクトエスカレーション受領時コンフリクト文脈付きで case-run を再実行すること（再実行回数上限は SPEC） |
| REQ-006-095 | case-close は Epic Issue 本文の更新を単一書き手として排他制御し並列 case-run と case-auto は Epic Issue 本文を更新しないこと（case-auto が完走する場合は case-close へ委譲し case-close が書き込む） |
| REQ-006-096 | Epic 実行では親 Issue の実行順序を SSoT とし子 Issue の実行状態と整合させること |
| REQ-006-097 | Epic Orchestrator は全 Wave 完了後に子 Issue のステータスを一括更新すること |
| REQ-006-098 | 子 Issue は実行状態ライフサイクルを持ちスキップを終了状態として採用しないこと |
| REQ-006-099 | Wave 状態は永続化対象ではなく Wave 内 Issue 状態から導出すること |
| REQ-006-100 | execution_unit は standard issue または epic issue とし Wave は execution_unit に含めないこと（Wave は Epic Issue 本文から読み取る内部構造） |
| REQ-006-101 | 複数 execution_unit 並列実行時 Epic Issue 本文の単一書き手は Epic Issue 本文単位で維持されること |
| REQ-006-102 | 必須依存があっても事前契約、依存先 Epic 完了、Epic サイズ推奨範囲調整のいずれかを満たす場合は別 Epic 分割を許容すること |
| REQ-006-103 | 並列実行時に PR マージコンフリクトが発生した場合後続 PR を rebase により機械的解消すること |
| REQ-006-104 | コンフリクト解消モデルは rebase による機械的解消、文脈付き再実行、オーケストレーション級判断の段階的エスカレーションで構成すること（各レベルの詳細は SPEC） |
| REQ-006-105 | case-close は PR 本文からの回収に加え自工程で実観測した deviation を Split Rule で分類して intake/learning のいずれかへ保存すること |
| REQ-006-106 | req-save は REQ 再構成 intake に加え自工程で実観測した deviation を Split Rule で分類して intake/learning のいずれかへ保存すること |
| REQ-006-107 | spec-save は自工程で実観測した deviation を Split Rule で分類して intake/learning のいずれかへ保存すること（従来の非関与から変更） |
| REQ-006-108 | case-auto は各工程の capture 成果物の保存結果参照と件数集計のみを行い capture 本文の再分類・再保存を行わないこと |
| REQ-006-109 | 各工程 command の deviation capture は agentdev-learning-capture skill または agentdev-intake-pipeline への委譲で行い別 command を直接呼ばず完了報告に保存した capture 成果物のパス・分類・保存結果のみを含めること |

### REQ-006-110: case-auto の結果状態区別報告

case-auto は、次の4状態を区別して集約・報告すること。

1. 各工程の実行結果（pass / warn / fail）
2. 各 artifact_action の適用結果（applied / skipped / failed / no-op）
3. 定義適用工程の完了状態（req-save/spec-save の成否）
4. OU ライフサイクルの完了状態（Issue 作成 / PR 作成 / PR マージ / Issue クローズ）

warn であっても全必須 action が applied または正当な no-op なら「警告付き工程完了」とできる。必須 action に skipped または
failed が1件以上ある場合は「定義適用完了」と報告しないこと。warn を pass へ変換して集約しないこと。
定義適用工程が成功していても OU ライフサイクルが完了していなければ OU 完了と報告しないこと。Phase 0 成功（artifact_actions
適用完了）と OU 完了（Issue/PR/Case 完了）は別々に報告すること。

## 適用範囲

- **対象**:
  - case-open（Issue 本文生成、REQ 番号埋め込み、Standard と Epic flow ルーティング、Epic/Wave/Issue 構成の自律生成、連結成分と3軸判断、スコープ重複検知、完了条件記載、構成検証、RU 削除と同期確認）
  - case-run（準備と実装と提出の再開可能フェーズ、worktree 実装と PR 作成、QG-3 の実装充足と乖離ゲート限定、単位 Issue と Wave スコープ、実行担当サブエージェント委譲、staleness check、docs 整合性検査、Findings 記録、capture 責務境界、Epic Wave 並列実行とべき等再実行、実行時間計測）
  - case-close（達成判定、整合確認、ADR 確認、PR マージと Issue クローズと永続化とクリーンアップの分離報告、intake と learning 回収、ローカル変更安全停止、branch と worktree 削除安全、rebase による機械的コンフリクト解消とエスカレーション、mergeable 待機、main 同期安全、test strategy 完了条件、Epic Wave クローズと完了条件評価と Epic Issue 本文単一書き手、docs 全体検索、Epic 自動クローズ判定の事前状態取得）
  - case-update（4モード、CI/CD と自律修正の case-run 委譲、テンプレート構造維持、フェーズ維持、Issue 番号取得制約、SSoT 整合性）
  - case-auto（追加入口、入力解決、artifact_actions 工程決定、自走対象と対象外、構成工程委譲と case-run インライン実行、operation_unit キュー管理、execution_unit 並列実行、Wave 間直列、blocked 部分停止、永続状態進行と再開、SSoT 遷移、クリーンアップ検証ゲート、停止理由分類と再開コマンド報告、壁時計時間計測、orchestration stage モデル、background task 回復、コンフリクトエスカレーション、Epic Issue 本文読み取りのみ）
  - Epic と Wave モデル（Epic Issue を実行順序 SSoT とするオーケストレーション、子 Issue 実行状態ライフサイクル、Wave 状態の Issue 状態導出、execution_unit 定義、単一書き手排他制御、別 Epic 分割許容、コンフリクト解消モデル）
  - 工程横断 capture 責務（req-save, spec-save, case-open, case-close の自工程 deviation capture と case-auto の保存結果参照・件数集計のみ。deviation は capture-boundaries.md の Split Rule で intake/learning へ振り分け、agentdev-learning-capture skill または agentdev-intake-pipeline へ委譲して保存する）
- **対象外**:
  - ワークフローのマクロフェーズ構成、SSoT 遷移、work_type 分類、scale 評価、公開 command 分類（REQ-005）
  - 委譲時の最終判断、ユーザー承認、永続化と外部更新、制御された副作用の発動契約（REQ-003）
  - 配布成果物の責務境界、subagent 委譲プロトコルの配布物側正規所有、委譲契約と結果状態機械の詳細実装（REQ-002）
  - 機械横断是正の完了証明と成果物品質ゲート（REQ-007）
  - draft、RU、要件doc の構造化契約と lifecycle（REQ-008）
  - REQ と SPEC の責務分離の一般原則と文書種別配置基準（REQ-001）
  - Wave 構成の詳細、execution_unit 並列モデルの内部パラメータ、委譲契約の field 定義、template 構造、実行安全境界の数値、コンフリクト解消レベルの詳細手順（SPEC）
  - 実行担当サブエージェントの起動 API、起動引数、並列度、retry 回数、timeout、context 管理方式（harness 責務）
  - harness 固有詳細の配布物からの除去一般原則（REQ-002）
