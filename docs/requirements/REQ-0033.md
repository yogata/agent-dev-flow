---
id: REQ-0033
title: "AgentDevFlow command / skill 定義の二次整合性是正"
created: "2026-05-24"
updated: "2026-05-30"
tags: [agentdev, consistency, guardrails, command-definitions, skill-definitions]
---

> **⚠️ Superseded**
> 
> この要件は全面置き換えられました。現行仕様として読むことは推奨されません。
> 履歴・参照用として保持されています。
> 
> **置き換え理由**: command / skill 定義の整合性是正は、REQ-0041 の新基準REQ群（REQ-C: Command/Skill/Template/Script責任分界、REQ-D: AgentDevFlow command protocol）で現行仕様として再定義される。59件の個別是正項目は新基準REQの要件行として再構成される。
> **現行仕様として読む範囲**: 該当なし（全面置き換え）
> **履歴として残す理由**: R1-R36一次是正後の残存不整合59件の記録。Issue #340 / PR #341 の後続対応の設計経緯。

## 目的

AgentDevFlow の全 command / skill 定義におけるガードレール不整合のうち、Issue #340 / PR #341 による一次整合性是正（R1-R36）後も残存する、または同対応後に新たに見つかった不整合を解消する。

対象は、実行時に到達不能・非永続・誤更新・責務混線を起こす実務上の不整合であり、AGENTS.md の「最小変更」「変更行は依頼に直結」「検証可能な成功条件」に従い、実装変更ではなく command / skill 定義の整合性是正に限定する。

## 要件

### A. 新規要件

#### case-close

| ID | 要件 |
|---|---|
| REQ-0033-001 | `case-close` の PR merge / Issue close / `.agentdev` 永続化は、結果状態を分離して報告すること（SHALL）。GitHub 側完了後に `.agentdev` push が失敗した場合は「GitHub完了 / domain state永続化失敗」として扱う |
| REQ-0033-002 | `case-close` の branch / worktree 削除失敗時の扱いは「警告して継続」ではなく「警告して停止」に統一すること（SHALL） |

#### req-save

| ID | 要件 |
|---|---|
| REQ-0033-003 | `req-save` は draft status を `saved` に更新してから commit / push すること（SHALL）。現状の push 後 status 更新により status が永続化されない構造を解消する |
| REQ-0033-004 | `req-save` は SPLIT 検出時に生成する `.sisyphus/drafts/requirements-review-finding-{topic-slug}.md` を Output に明記すること（SHALL） |

#### git pull / hash 検証

| ID | 要件 |
|---|---|
| REQ-0033-005 | 読込・評価・ユーザー承認後に `git pull --ff-only` を実行する設計を残す場合は、読込時 hash と pull 後 hash の一致検証を必須とすること（SHALL）。一致しない場合は評価・承認をやり直す |

#### req-define

| ID | 要件 |
|---|---|
| REQ-0033-006 | `req-define` は、明示入力ファイルを source 種別に依存せず `Requirement Source` として記録すること（SHALL）。learning staging stub のみを特別扱いしない |
| REQ-0033-007 | `req-define` は `.agentdev/intake/promoted/*.md` および `.agentdev/learning/promoted/*.md` artifact を要件doc提示直後に削除しないこと（SHALL）。削除は `req-save` または `case-open` 成功後の imported 処理として扱う |

#### case-open

| ID | 要件 |
|---|---|
| REQ-0033-008 | `case-open` は Epic flow / Standard flow の Step 番号を分離すること（SHALL）。Standard flow が Epic flow 手順へ誤流入しない構造にする |
| REQ-0033-009 | `case-open` は Epic 子Issue最大件数を Issue 作成前に検査すること（SHALL）。上限超過時は Epic / 子Issue を作成せず停止する |
| REQ-0033-010 | `case-open` は draft を削除する場合、削除直前の `issued` status 更新を不要な操作として削除すること（SHOULD）。履歴が必要な場合は archive 方式を別途定義する |

#### case-run

| ID | 要件 |
|---|---|
| REQ-0033-011 | `case-run` は「多重Issue入力上限」と「Epic Wave 内同時実行上限」を別ルールとして定義すること（SHALL）。多重Issueのユーザー入力上限は最大5件、Epic Wave は5件超過時に自動分割する |
| REQ-0033-012 | `case-run` は Pattern B の specs 更新必須条件と、全パターン共通の関連ドキュメント整合性条件を分離すること（SHALL） |

#### case-update

| ID | 要件 |
|---|---|
| REQ-0033-013 | `case-update --req` は REQ 更新後の commit / push 方針を明記すること（SHALL）。直接 commit / push するか、`req-save` 系に委譲するかを曖昧にしない |
| REQ-0033-014 | `case-update --review-ng` が REQ UPDATE を行う場合、`agentdev-spec-compliance` の乖離報告とユーザー承認済み判断を入力条件にすること（SHALL） |

#### intake-capture / intake-review / intake-promote

| ID | 要件 |
|---|---|
| REQ-0033-015 | `intake-capture` は、ユーザー入力から明示できる内容のみ整理すること（SHALL）。推測不能なセクションは省略し、過度な補完を禁止する |
| REQ-0033-016 | `intake-review` は directory 移動を判定結果の表現として使用することを明確化すること（SHALL）。「ディレクトリ移動を必須の状態表現として扱わない」という矛盾表現は削除または修正する |
| REQ-0033-017 | `intake-promote` は `intake-open` 用 promoted artifact に、Issue 化に必要な最小情報を含めること（SHALL）。最低限、タイトル、概要、対象範囲、完了条件、Issue構成を含める。※ intake-open は REQ-0039 により廃止。本要件は req-backlog 経由の RU 化に読み替える |

#### intake-open

| ID | 要件 |
|---|---|
| REQ-0033-018 | `intake-open` の「データ取得は gh CLI のみ」は、GitHub Issue/PR の読み書きに限定して記述すること（SHOULD）。ローカル artifact 読込には適用しない。※ intake-open は REQ-0039 により廃止 |

#### integrity-check

| ID | 要件 |
|---|---|
| REQ-0033-019 | `integrity-check` の read-only 制約は「検査対象 artifact を変更しない」の意味として再定義すること（SHALL）。report / intake item の生成は例外として明記する |
| REQ-0033-020 | `integrity-check` は git 操作を行わないことを維持し、完了報告に「git永続化なし。必要なら別途 commit」を明示すること（SHALL） |
| REQ-0033-021 | `integrity-check` が intake item を作成する場合、同名ファイル衝突時は `intake-capture` と同じ連番付与ルールを使うこと（SHALL） |

#### agentdev-gh-cli

| ID | 要件 |
|---|---|
| REQ-0033-022 | `agentdev-gh-cli` は Windows 固有の文字化け対策と、全環境共通の `--body-file` / VERIFY 制約を分離して記述すること（SHALL） |

#### agentdev-git-worktree

| ID | 要件 |
|---|---|
| REQ-0033-023 | `agentdev-git-worktree` は検出した base branch を `origin/{base_branch}` として一貫使用すること（SHALL）。`origin/main` 固定は削除する |
| REQ-0033-024 | `agentdev-git-worktree` は worktree lifecycle に伴う branch cleanup を DO NOT USE FOR の例外として明記すること（SHOULD） |
| REQ-0033-025 | `agentdev-git-worktree` は Windows / POSIX の `.sisyphus/` cleanup 手順を分けて記述すること（SHOULD） |

#### agentdev-req-analysis

| ID | 要件 |
|---|---|
| REQ-0033-026 | `agentdev-req-analysis` は ADR が必要な場合でも ADR ファイル作成を扱わず、ADR候補記録までを責務とすること（SHALL）。作成は `req-save` + `agentdev-adr-file-manager` に委譲する |

#### agentdev-req-file-manager

| ID | 要件 |
|---|---|
| REQ-0033-027 | `agentdev-req-file-manager` は CREATE/APPEND/UPDATE の影響対象に `docs/README.md` を明記すること（SHALL） |
| REQ-0033-028 | `agentdev-req-file-manager` は要件行ID `REQ-{NNNN}-{MMM}` を正式定義するか、APPEND 条件から削除すること（SHALL） |
| REQ-0033-029 | `agentdev-req-file-manager` は `case-close` で整合性自動修正を行うように読める記述を修正すること（SHALL）。`case-close` は verify-only、修正は `req-save` または `case-update` に限定する |

#### agentdev-adr-guidelines / agentdev-adr-file-manager

| ID | 要件 |
|---|---|
| REQ-0033-030 | `agentdev-adr-guidelines` は ADR 要否判断と内容観点に限定し、保存先・採番・ファイル管理は `agentdev-adr-file-manager` に委譲すること（SHALL） |
| REQ-0033-031 | ADR status 表記は `agentdev-adr-guidelines` と `agentdev-adr-file-manager` で統一すること（SHALL）。特に `Superseded` と `superseded-by:[ADR-MMMM]` の差異を解消する |
| REQ-0033-032 | `agentdev-adr-file-manager` は Accepted ADR の不変性と UPDATE 許容範囲を整合させること（SHALL）。許容する場合は非意味的 typo 修正に限定する |
| REQ-0033-033 | `agentdev-adr-file-manager` の適用先から `case-run` による ADR 作成を削除すること（SHALL）。ADR 作成は `req-save` に限定する |

#### agentdev-workflow-reporting

| ID | 要件 |
|---|---|
| REQ-0033-034 | `agentdev-workflow-reporting` は `intake-open` の説明から backlog draft 表現を削除し、`promoted artifact` に統一すること（SHOULD）。※ intake-open は REQ-0039 により廃止 |
| REQ-0033-035 | `agentdev-workflow-reporting` は git 操作を行わない command の `git 永続化` 欄を `該当なし` として扱うこと（SHALL） |

#### agentdev-workflow-orchestration

| ID | 要件 |
|---|---|
| REQ-0033-036 | `agentdev-workflow-orchestration` は多重Issue入力上限と Epic Wave 分割上限を別ルールとして定義すること（SHALL） |
| REQ-0033-037 | `agentdev-workflow-orchestration` は CI polling が実行環境上できない場合のフォールバックを定義すること（SHALL）。その場合は現在状態を報告して停止する |

#### agentdev-spec-compliance

| ID | 要件 |
|---|---|
| REQ-0033-038 | `agentdev-spec-compliance` は乖離分類と推奨アクションまでを責務とし、REQ更新の最終判断は `case-update` 側のユーザー承認済み入力に委ねること（SHALL） |

#### agentdev-workflow-templates

| ID | 要件 |
|---|---|
| REQ-0033-039 | `agentdev-workflow-templates` は「必須セクションは `該当なし` 可、任意セクションは省略可」と明確に分けること（SHALL） |

#### agentdev-epic-tracker

| ID | 要件 |
|---|---|
| REQ-0033-040 | `agentdev-epic-tracker` は `⏭ スキップ` を正式状態に追加するか、Orchestrator が設定しない方針に統一すること（SHALL） |

#### agentdev-learning-capture / agentdev-learning-pipeline

| ID | 要件 |
|---|---|
| REQ-0033-041 | `agentdev-learning-capture` は inbox/archive 作成可否と git 永続化責務を分離すること（SHALL）。skill は作成・追記可、永続化は呼び出し command の責務と明記する |
| REQ-0033-042 | `agentdev-learning-capture` は同一観測から learning と intake の両方が発生する場合、`capture-boundaries.md` の split rule に従って成果物を分離すること（SHALL） |
| REQ-0033-043 | `agentdev-learning-pipeline` の Requirement Source 反映先例は `docs/requirements/REQ-{NNNN}.md` に修正すること（SHALL）。`REQ-{NNNN}-{name}.md` は REQ ファイル規約と衝突するため使用しない |
| REQ-0033-044 | `agentdev-learning-pipeline` の imported 判定は循環しない条件に修正すること（SHALL）。`case-close` 正常完了ではなく「GitHub完了処理成功後、archive 処理に入った時点」と定義する |

#### agentdev-integrity

| ID | 要件 |
|---|---|
| REQ-0033-045 | `agentdev-integrity` と `integrity-check` は report 出力形式を一致させること（SHALL）。`.md` のみ、または `.md + optional .json` のいずれかに統一する |
| REQ-0033-046 | `agentdev-integrity` は検査・レポート schema までを責務とし、intake item 化の最終判断は `integrity-check` command に限定すること（SHALL） |

#### 責務分離原則

| ID | 要件 |
|---|---|
| REQ-0033-047 | command / skill の責務記述は、command を実行手順の一次参照、skill を判定基準・共通知識の一次参照として分離すること（SHALL） |

### B. 既存REQの反映漏れ

| ID | 要件 |
|---|---|
| REQ-0033-048 | `agentdev-workflow-lifecycle` は `intake-from-github` の説明を「draft保存」ではなく「intake item として inbox 保存」に修正すること（SHOULD）。Issue #340 で namespace 統一済みだが、lifecycle 参照内の旧表記が未更新の可能性がある |
| REQ-0033-049 | command / skill 内の旧語彙 `issue-*`, `tips`, `backlog draft`, `draft保存` は、履歴説明を除き現行語彙へ統一すること（SHOULD）。Issue #340 / PR #370 で旧 namespace・旧 terminology の検出・修正を実施済みだが、残存箇所の再確認を含む |
| REQ-0033-052 | `agentdev-integrity` の検査範囲は以下の8カテゴリを対象とすること（SHALL）: REQ frontmatter 検証、ADR ↔ REQ 相互参照整合性、Skill ↔ load_skills 参照整合性、Command-map 整合性、旧 namespace 残存検出、完了報告フォーマット検証（REQ-0024-017/018）、Template 必須セクション検証、Skill 構造 lint。検査カテゴリの追加・変更は skill 定義の更新を経ること |
| REQ-0033-053 | `integrity-check` command は `agentdev-integrity` skill の検査定義に基づいて検証を実行すること（SHALL）。検証ロジックの追加・変更は skill 側で行い、command 定義には検証ロジックを直接記述しない。report 出力形式は REQ-0033-045 に従い `.md` 形式とする |

### C. 既存REQとの矛盾解消

| ID | 要件 |
|---|---|
| REQ-0033-050 | `intake-promote` が生成する promoted artifact は frontmatter を持たず、route / status はディレクトリ配置で表現すること（SHALL）。Issue #340 では minimal frontmatter（route/status）で統一する方針だったが、後続設計で frontmatter 不要の方針に変更された可能性があるため、現行方針を確定してから是正する |
| REQ-0033-051 | `agentdev-workflow-templates` は `intake-open` promoted artifact に `route/status` frontmatter を要求してはならない（SHALL）。REQ-0033-050 と同じく、#340 の frontmatter 方針と現行定義の衝突を解消する。※ intake-open は REQ-0039 により廃止 |
| REQ-0033-054 | `promoted/*.md` artifact の imported 処理は、後続ルート（`req-save` または `case-open`）の成功後に実行すること（SHALL）。`req-define` 完了直後には削除しない。imported 処理は該当 artifact ファイルの削除で行う |
| REQ-0033-055 | imported 判定条件は以下のいずれかが成立した時点とすること（SHALL）: (a) `req-save` が REQ ファイルの保存に成功した場合、(b) `case-open` が GitHub Issue の作成と VERIFY に成功した場合。失敗時は artifact を残し、再実行可能な状態を維持すること |

### D. case-close手順整合性

| ID | 要件 |
|---|---|
| REQ-0033-056 | `case-close` の手順は system.md、各コマンド定義、および REQ-0032 と整合すること（SHALL）。手順に矛盾がある場合は REQ を正として扱う |
| REQ-0033-057 | `case-close` は `promoted/*.md` に artifact が残存する場合、imported 処理（REQ-0033-054）を実行してから完了報告を出力すること（SHALL） |

### E. case-run / git-worktree整合性

| ID | 要件 |
|---|---|
| REQ-0033-058 | `case-run` の git-worktree 使用手順は `agentdev-git-worktree` skill 定義と整合すること（SHALL）。worktree作成（base branch検出、命名規則）、worktreeプレフィクス必須、Windows/POSIX分離の各要件を case-run が遵守すること |

### F. integrity reports永続化方針

| ID | 要件 |
|---|---|
| REQ-0033-059 | `integrity-check` の report 出力先は `.sisyphus/integrity-report.md` とすること（SHALL）。report は git 管理対象外とし、`.gitignore` に追加することを推奨する（SHOULD）。過去 report の蓄積・保持期間は定義せず、都度上書きとする |

## 既存REQで対応済みのため新規要件化しない項目

以下は今回の検討で再確認されたが、既存REQに同一内容が定義済みのため、本REQの新規要件には含めない。

| 検討項目 | 既存REQ | 扱い |
|---|---|---|
| case-close の未チェック即停止禁止 | REQ-0032-001〜004 | 既存REQ参照 |
| case-close の達成判定後完了ゲート再評価 | REQ-0032-010, REQ-0032-012 | 既存REQ参照 |
| case-close Step 再構成 | REQ-0032-003, REQ-0032-004 | 既存REQ参照 |
| .agentdev 更新前 git pull | REQ-0025 / REQ-0022 | 既存REQ参照 |
| intake-open の情報不足時質問禁止 | REQ-0029-015〜017 | 既存REQ参照 |
| intake-open 完了報告3区分 | REQ-0029-019〜022 | 既存REQ参照 |
| case-run Guardrail 連番化 | Issue #340 / PR #341 | 既に対応済み |
| intake-from-github 旧 marker 処理削除 | Issue #340 / PR #341 | 既に対応済み |
| intake-open Guardrail 連番化 | Issue #340 / PR #341 | 既に対応済み |

実装時に現行ファイルへ未反映であることが見つかった場合は、新規要件ではなく「既存REQの反映漏れ」として扱う。

## 適用範囲

- **対象**:
  - 対象 command: `/agentdev/req-define`, `/agentdev/req-save`, `/agentdev/case-open`, `/agentdev/case-run`, `/agentdev/case-update`, `/agentdev/case-close`, `/agentdev/learning-refine`, `/agentdev/learning-promote`, `/agentdev/intake-capture`, `/agentdev/intake-from-github`, `/agentdev/intake-review`, `/agentdev/intake-promote`, `/agentdev/intake-open`, `/agentdev/integrity-check`
  - 対象 skill: command の `load_skills` で参照される全 `agentdev-*` skill
  - 修正範囲: command / skill / reference / template の文書定義に限定

- **対象外**:
  - コード実装、テスト実装
  - GitHub Issue 作成、PR 作成
  - 既存 `.agentdev` artifact の実修正
  - 実装計画の作成
  - 自動修正スクリプト実装

## 受け入れ条件

| ID | 条件 |
|---|---|
| AC-001 | 全 command の Guardrails に、同一 command 内で到達不能な Step が存在しない |
| AC-002 | push 後に永続化対象ファイルを更新する command が存在しない |
| AC-003 | `.agentdev` 読込・評価・移動・削除を行う command は、pull 順序または hash 検証により処理対象の同一性を保証している |
| AC-004 | `intake-promote` と `agentdev-workflow-templates` の promoted artifact 前提が一致している |
| AC-005 | `case-close` はチェック更新、最終完了ゲート、merge/close、domain state 永続化の順序が一意に読める（REQ-0032 と本REQの両方を反映した状態で） |
| AC-006 | `integrity-check` の read-only の意味が「検査対象 artifact を変更しない」に統一されている |
| AC-007 | README / lifecycle / reporting / command / skill の用語が `agentdev`, `learning`, `intake item`, `promoted artifact` に統一されている |
| AC-008 | `agentdev-integrity` の機械検査対象に、本要件で直す表面的矛盾のうち検出可能なものが含まれている |
| AC-009 | すべての修正は command / skill / reference / template の文書定義に限定され、コード実装・Issue作成・PR作成を含まない |

## 関連情報

- **前提**: Issue #340 / PR #341（R1-R36 横断的整合性是正）は対応済みとする
- **関連**: REQ-0032（case-close 未チェック項目達成判定）
- **関連**: REQ-0025（intake 系コマンドの git 永続化）
- **関連**: REQ-0022（.agentdev domain state 更新後の git 永続化）
- **関連**: REQ-0029（intake-open promoted artifact 一括処理）
- **関連**: REQ-0021（ガードレールのスクリプト化）
- **関連**: REQ-0016（Command/Skill/Template/Script責任分界）
