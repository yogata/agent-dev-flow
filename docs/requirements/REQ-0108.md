---
id: REQ-0108
title: "Integrity / Validation / Tests"
created: "2026-05-30"
updated: "2026-05-30"
tags: [integrity, validation, tests, guardrails, git, doc-map, guides, retired, canonical, link, namespace, report]
---

## 目的

AgentDevFlow 管理下の全文書 artifact（REQ/ADR/SPEC/DOC-MAP/guides/command/skill/template）の「存在・参照・索引・境界・旧構造残存」を機械的に検査し、finding を分類・route付きで報告する read-only な整合性検査として、文書体系の健全性を担保する。ガードレールとテストで品質を担保する。

## 要件

| ID | 要件 |
|---|---|
| REQ-0108-001 | `integrity-check` は以下の17集合を検査対象とすること（SHALL）: active REQ (`docs/requirements/REQ-*.md`), retired REQ (`docs/requirements/retired/REQ-*.md`), REQ index (`docs/requirements/README.md`), REQ migration table (`docs/requirements/mapping-table.md`), ADR (`docs/adr/ADR-*.md`), ADR index (`docs/adr/README.md`), SPEC (`docs/specs/*.md`), SPEC index (`docs/specs/README.md` — 存在する場合), DOC-MAP (`docs/DOC-MAP.md`), docs README (`docs/README.md` — 存在する場合), guides (`docs/guides/*.md`), commands (`.opencode/commands/**/*.md`), skills (`.opencode/skills/*/SKILL.md`), skill reference (`.opencode/skills/**/reference/*.md`), skill references (`.opencode/skills/**/references/*.md`), templates (`.opencode/skills/agentdev-workflow-templates/templates/*.md`), integrity scripts (`.opencode/skills/agentdev-integrity/scripts/*.ts`) |
| REQ-0108-002 | `integrity-check` は active REQ と retired REQ を別集合として扱うこと（SHALL） |
| REQ-0108-003 | `integrity-check` は以下の inventory 整合性を検査すること（SHALL）: REQ index ↔ active REQ, REQ index ↔ retired REQ 導線, DOC-MAP ↔ active REQ, DOC-MAP ↔ SPEC, DOC-MAP ↔ guides, ADR index ↔ ADR, SPEC index ↔ SPEC（存在する場合）, command README ↔ command file, root README / system.md ↔ command inventory |
| REQ-0108-004 | `integrity-check` は retired REQ が現行要件判断に使われる導線を検出すること（SHOULD） |
| REQ-0108-005 | `integrity-check` は ADR↔REQ 相互参照、Skill↔load_skills 参照、Command-map↔実体、Template 必須セクションの整合性を検査対象に含めること（SHALL） |
| REQ-0108-006 | 検出結果は finding ごとに level (`ng`/`warning`/`info`), category, route, file, line (省略可), evidence, expected, message を持つこと（SHALL） |
| REQ-0108-007 | `integrity-check` は基準文書を直接自動更新しないこと（SHALL） |
| REQ-0108-008 | `integrity-check` は learning item を直接作成しないこと（MUST NOT）。learning 候補として報告のみ行うこと |
| REQ-0108-009 | 要件・責務・ワークフロー変更が必要な finding は req-define 入力候補として扱うこと（SHALL） |
| REQ-0108-010 | `.agentdev` domain state 更新後は必要に応じて git 永続化すること（SHALL） |
| REQ-0108-011 | 読込・評価・ユーザー承認後に `git pull --ff-only` を行う設計では、読込時hashとpull後hashの一致を検証すること（SHALL） |
| REQ-0108-012 | agentdev command群は command / skill / template / epic の各層をカバーする体系的テストで品質を担保すること（SHALL） |
| REQ-0108-013 | `integrity-check` は以下の link 整合性を検査すること（SHALL）: (a) Markdown リンク先ファイルが存在しない, (b) Markdown リンク先セクションが存在しない, (c) `REQ-{NNNN}` 参照先が存在しない, (d) `ADR-{NNNN}` 参照先が存在しない, (e) retired REQ を現行要件のように参照している, (f) DOC-MAP / README / guides から存在しない docs パスへのリンク |
| REQ-0108-014 | `integrity-check` は canonical 境界違反を構造的に検出すること（SHOULD）: DOC-MAP が要件本文を代替している疑い, guides が REQ/ADR/SPEC の内容を代替している疑い, README が基準文書の本文を持ちすぎている疑い。構造的検出（要件行ID大量列挙、SHALL/MUST 相当の要件定義表、ADR判断本文相当の記述等）でよい |
| REQ-0108-015 | `integrity-check` は以下の lifecycle 境界を検査すること（SHALL）: (a) active REQ と retired REQ の ID 重複, (b) active REQ 一覧に retired REQ が混入, (c) retired REQ が現行要件判断の第一参照として案内されている, (d) mapping-table.md に存在しない旧REQ参照がある, (e) retired REQ の移行有無が mapping table で追えない |
| REQ-0108-016 | `integrity-check` の旧 namespace / terminology 検査は `docs/DOC-MAP.md`, `docs/README.md`, `docs/guides/*.md`, `docs/specs/*.md`, `.opencode/skills/**/reference/*.md`, `.opencode/skills/**/references/*.md` も対象に含めること（SHALL）。検出対象は少なくとも旧 command 名, 旧 command path, 旧 skill 名, `docs/tips/`, `tips` terminology, `views` 構造参照, bare slash command form を含む |
| REQ-0108-017 | finding の category は `document-drift` / `broken-reference` / `obsolete-structure` / `canonical-conflict` / `workflow-gap` / `integrity-rule-gap` のいずれかであること（SHALL） |
| REQ-0108-018 | finding の route は以下の判定に従うこと（SHALL）: 単発の壊れリンク → `intake`, 単発の README/実体不一致 → `intake`, 旧構造・旧namespace残存 → `intake`, 同種 finding が複数箇所で反復 → `intake+learning`, 検査ルール自体の不足 → `req-define`, 文書体系・責務境界の定義不足 → `req-define`, 再発防止知見として有用 → `learning`, 履歴記述として妥当 → `none` |
| REQ-0108-019 | `check_integrity.ts` は検査結果を `.agentdev/integrity/reports/YYYY-MM-DD-integrity-report.md` に保存すること（SHALL）。保存先ディレクトリがない場合は作成すること |
| REQ-0108-020 | 同日複数回実行時、同名ファイルが存在する場合は連番（`-2`, `-3`...）を付与して衝突を回避すること（SHALL） |
| REQ-0108-021 | JSON出力指定時は標準出力を JSON にし、Markdown レポート保存は維持すること（SHALL）。既存 CLI 契約と矛盾する場合は既存 CLI 契約を優先し、コマンド定義側に明記すること |

## 適用範囲

- **対象**: integrity-check の検査対象拡張（存在・参照・索引・境界・旧構造残存）、finding 分類・route、レポート出力、read-only 制約、validation、guardrail scripts、体系的テスト、git永続化
- **対象外**: `.sisyphus/`、個別commandの詳細手順、ドキュメント本文の意味的レビュー、guides の文章品質、個別 finding の修正実装、GitHub Issue 化、コミット作成
