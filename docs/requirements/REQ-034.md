---
id: REQ-034
title: "case-auto 実行契約（自走オーケストレーション）"
created: "2026-08-15"
updated: "2026-09-19"
---

## 目的

case-auto の実行契約を所有する。
要件doc または GitHub Issue を入力とする標準実行コマンドとしての自走 orchestration（入力解決、artifact_actions 工程決定、orchestration stage モデル、execution_unit 並列実行、停止理由分類、bounded parent decision resolution、4状態区別報告、完了報告）を扱う。
case-auto は要求入口 2 つ（req-define、backlog-auto）から合流する標準実行コマンドであり、各工程の Workflow Skill を権威情報源として委譲先に読み込ませる。
Epic/Wave 実行モデルの実行規則は Epic と Wave 実行モデル REQ が所有する。

## 要件

| ID | 要件 |
|---|---|
| REQ-034-001 | case-auto は要件doc または GitHub Issue を入力とし内部 lifecycle（case-open、case-ready、case-run、case-close、例外経路の case-revise）を駆動する標準実行コマンドとして位置づけること |
| REQ-034-002 | case-auto の入力解決は引数種別（要件doc パス、セッション指定、数値、Issue URL）に応じて処理対象を特定し特定不可時は停止すること |
| REQ-034-003 | case-auto は draft 内の artifact_actions の有無に基づき工程を決定し work_type 固定分岐を採用しないこと |
| REQ-034-004 | case-auto の自走対象は repo にファイルとして残る変更に限定すること |
| REQ-034-005 | case-auto は repo 外実体変更（DB マイグレーション実行、デプロイ、cloud apply、外部 SaaS 設定変更、認証情報変更等）を自走対象外とすること |
| REQ-034-006 | case-auto はマイグレーションファイルと IaC ファイルの作成修正を自走対象としマイグレーション実行と IaC apply を自走対象外とすること |
| REQ-034-007 | case-auto は workflow 実装の権威情報源として Workflow Skill を参照すること。case-open、case-ready、case-close を各 Workflow Skill（agentdev-workflow-case-open / case-ready / case-close）の委譲契約に従ってサブエージェントへ委譲し、委譲起動の指定先は command ではなく Workflow Skill の load 指定とすること。case-auto は workflow 実装本体を複製しないこと |
| REQ-034-008 | case-auto は case-run をインライン実行する。インライン実行の読込主体は case-auto 自身であり、case-run の Workflow Skill（agentdev-workflow-case-run）を権威情報源として読み込む。起動手段は harness 責務。Workflow Skill が workflow 実装の権威情報源であり、実装本体を複製しないこと。case-auto は下位 workflow（case-open / case-ready / case-revise / case-run / case-close）の契約確定後に上位 orchestrator として再設計すること。内部 lifecycle 段階の public contract（入出力契約、副作用、安全性、承認境界、stop state、ordering contract）の正規文書は Command Design（docs/designs/commands/*.md）である。両者不一致時は Command Design を正とする（docs/designs/foundations/system.md の Workflow Architecture Inventory と整合） |
| REQ-034-009 | case-auto は case-open / case-ready / case-close の各工程を委譲先 subagent へ委譲する。委譲先 subagent は各工程の Workflow Skill を権威情報源として読み込み、工程固有手続きの再実装を回避する。委譲起動の指定先は command ではなく Workflow Skill の load 指定とし、workflow 実装の権威情報源は Workflow Skill のみとする。case-auto は下位 workflow 詳細処理を複製しない上位 orchestrator 化すること。内部 lifecycle 段階の public contract の正規文書は Command Design（docs/designs/commands/*.md）である。両者不一致時は Command Design を正とする（docs/designs/foundations/system.md の Workflow Architecture Inventory と整合） |
| REQ-034-010 | case-auto は operation_unit キューの管理と制御のみを担い operation_unit 本文の抽出、変換、REQ 操作解釈を行わないこと |
| REQ-034-011 | case-auto は必須依存のない execution_unit を並列実行し complete-then-next の逐次処理を行わないこと |
| REQ-034-012 | case-auto は同一 Epic 内の Wave 間を直列実行すること |
| REQ-034-013 | case-auto は depends_on（必須依存のみ）に基づいて直列化要因を判定し recommended_order を直列化ゲートとしないこと |
| REQ-034-014 | case-auto は blocked になった execution_unit だけを停止し他の ready 対象を継続すること |
| REQ-034-015 | case-auto は全対象が closed、blocked、failed になったら終了し一部 blocked 残存時は partial blocked として報告すること |
| REQ-034-016 | case-auto はクリティカルパス解析と優先順位制御を行わず実行可否を依存関係のみで判定すること |
| REQ-034-017 | case-auto は永続状態から進行と再開を行い Epic 全体の実装詳細を親コンテキストに累積しないこと |
| REQ-034-018 | case-auto は case-ready が確定した Epic、Wave、Issue 構造に従って進行すること |
| REQ-034-019 | case-auto は case-open 前だけ req_draft を orchestration pre-reader として読み、case-ready 成功後は invalid post-case reader として req_draft を読まないこと。case-ready 成功後の停止、再開、完了処理は Issue と Epic だけで成立させること |
| REQ-034-020 | case-auto は stage 2（case-ready）の対象群収束後・stage 3 開始前にクリーンアップ検証ゲート（ドラフト残存、RU 残存の検証）を実行し、当該ゲートは stage 2 を正常完了した対象について draft / RU の残存を検証して残存を検出した場合停止すること。stage 2 が blocked / failed / 中断等で正常完了していない対象について、既存 lifecycle 契約に従って保持された draft / RU を cleanup 違反として扱わないこと。クリーンアップ検証ゲートでの残存検出による停止は当該 case-auto 実行全体の停止（stage 3 開始の前提条件違反）とし、blocked 対象の部分継続原則（REQ-034-014）とは区別すること |
| REQ-034-021 | case-auto は停止時に完了済み、進行中、未実行の各委譲単位と再開に必要な次コマンドを報告すること |
| REQ-034-022 | case-auto は停止時に停止理由を分類（合意要件逸脱、スコープ拡大、repo 外実体変更、command 契約不整合等）で報告すること |
| REQ-034-023 | case-auto は実行開始時刻と完了報告生成時刻を記録し工程別の壁時計時間を完了報告に含めること |
| REQ-034-024 | case-auto は委譲工程の結果（Issue/PR 番号、保存済みファイル、pass/warn/fail）のみを受領し工程内部の調査過程や中間ログを親コンテキストに累積しないこと |
| REQ-034-025 | case-auto は orchestration stage モデル（stage 1 case-open（再合意済み Definition 変更の例外経路時は case-revise）、stage 2 case-ready、stage 3 case-run、stage 4 case-close）を採用し、stage 1a・stage 1b の下位分類を持たないこと。各 stage 内では、安全性・依存上の直列化要因（必須依存、実行安全上限、共有正規成果物や共有状態への競合書き込み、単一書き手契約に基づく排他等、既存契約に明示された直列化要因）を満たす処理を除き、当該 stage の開始時点で実行可能な対象を最大限並列に実行すること。全 stage 共通の新たな固定並列上限を設けないこと（stage 3 の case-run 同時起動数は REQ-034-027 の既存上限を維持し、最大同時起動数・起動間隔・順次フォールバック等の stage 内 scheduling 制約による batch 分割は orchestration stage の分割として扱わない）。ある stage に属する全対象が当該 stage を正常完了し、または当該実行において後続 stage へ進めないことが既存契約上確定した結果（blocked / failed / delegation-unavailable 等、その他既存 workflow が定める継続不能または再実行待ち状態への確定。ここでいう確定とは既存契約上いつ再実行・再開されるかが見えている状態を指し、実行継続中・状態不明・再試行の要否が未確定の対象は含めない）に収束するまで次 stage を開始せず（未実行・実行中・状態不明・再試行要否未確定の残存は収束済みとしない。delegation-unavailable は後続不能確定に含め、REQ-002-004 の pending 戻し対象は当該実行の後続 stage から除外して再開時に扱う）、対象単位で後続 stage を先行完結する縦切り pipeline として実行しないこと。後続 stage へ進めないことが確定した対象を後続 stage の対象から除外し、その存在だけを理由として独立した他対象の進行を停止しないこと。main への push、capture、commit、同一 Epic Issue 本文への更新等の競合する共有書き込みは、当該競合部分のみを必要な単位で局所的に直列化し、当該競合と無関係な対象を含む stage 全体の直列化を行わないこと（詳細は REQ-034-026）。Epic execution_unit における Wave 間および最終 Wave の case-close(#epic) は Wave 反復を進行・完遂させる stage 3 内部処理として扱い stage 4 の開始とみなさないこと（Epic execution_unit の stage 3 完了は後続 Wave が残存しない状態への Wave 反復の完遂とし、stage 4 では追加の case-close を行わない。stage 3 完了判定は既存 Epic/Wave workflow の execution_unit 完了状態基準に従い、その内部ロジックを複製しない）。再開時は stage cursor を新たな正規状態として保存せず、起動時対象集合と各対象の正規状態から現在 stage を最も早い未収束 stage として再構成し、完了済み対象を再実行せず同一対象だけを後続 stage へ先行させず、起動時対象集合の安定識別子を中断再開に必要な期間に限りローカル一時実行状態（REQ-002-036）として保持して draft / RU の削除によって対象を実行中の対象集合から消失させないこと（req_draft への依存は REQ-034-019 に従い持たない）。case-run internal lifecycle（state machine、self-healing loop 等）を複製せず case-run 側の正規所有に委譲すること |
| REQ-034-026 | case-auto は main への push、capture、commit、同一 Epic Issue 本文への更新等の共有状態への書き込みについて、書き込みの競合が実際に生じる部分（同一リポジトリへの push、同一 Issue 本文、採番・索引更新等の単一書き手対象）のみを必要な最小単位で局所的に直列化し、当該競合と無関係な対象の並列実行を妨げず、stage 全体を一括して扱う直列集約ポイントを設けないこと（局所直列化の対象範囲と単位の詳細は Design） |
| REQ-034-027 | case-auto の orchestration stage 3 における case-run 同時起動数は固定値とし実行安全境界として遵守すること（数値は Design） |
| REQ-034-028 | case-auto の並列実行はデフォルト挙動とし順次実行はフォールバック時にのみ許可しフォールバック理由を完了報告に含めること |
| REQ-034-029 | case-auto は background task が破棄された場合 commit 済み PR 未作成状態と未コミット変更残存状態を区別し対応する回復パターンを適用すること |
| REQ-034-030 | case-auto は case-close からのコンフリクトエスカレーション受領時コンフリクト文脈付きで case-run を再実行すること（再実行回数上限は Design） |
| REQ-034-031 | case-auto は、次の4状態を区別して集約・報告すること。(1) 各工程の実行結果（pass / warn / fail）(2) 各 artifact_action の適用結果（applied / skipped / failed / no-op）(3) 定義適用工程の完了状態（case-ready の Definition 保存・確定の成否）(4) OU ライフサイクルの完了状態（Issue 作成 / PR 作成 / PR マージ / Issue クローズ）。warn であっても全必須 action が applied または正当な no-op なら「警告付き工程完了」とできる。必須 action に skipped または failed が1件以上ある場合は「定義適用完了」と報告しないこと。warn を pass へ変換して集約しないこと。定義適用工程が成功していても OU ライフサイクルが完了していなければ OU 完了と報告しないこと。Phase 0 成功（artifact_actions 適用完了）と OU 完了（Issue/PR/Case 完了）は別々に報告すること |
| REQ-034-032 | case-auto は下位 command から raw finding ではなく解決不能な判断事項を構造化した decision context を受領し、現行正規成果物（REQ、Decision、Design、Issue その他合意済み情報）から一意に回答可能な場合はユーザー停止せず回答して下位 command を resume させること |
| REQ-034-033 | 外部仕様・互換性・データ保持・セキュリティ・対象範囲・受け入れ条件等を変更しない可逆的内部詳細は、既存契約で許容された範囲に限り case-auto が作業仮定と根拠を明示した上で自走継続できること |
| REQ-034-034 | 上位 REQ/Decision/Design/Issue 等の矛盾そのものが finding の対象である場合、case-auto が当該矛盾した情報を根拠として一方を勝手に採用せず、新しいユーザー価値判断・対象範囲変更・外部契約変更等が必要な場合は既存停止経路でユーザーへ返すこと |
| REQ-034-035 | case-auto は処理単位を一級概念（安定した識別子、入力、出力、依存関係、所有対象、現在状態、完了条件、検証結果、必要な場合の worktree との対応の意味）として扱うこと（具体的な格納形式は Design） |
| REQ-034-036 | case-auto は依存関係上独立した処理単位を並列実行可能と判定し、実行可能な処理単位、並列可能性、合流条件、一部失敗時の全体状態遷移を ADF 側の判断として所有すること。実際に起動するエージェント数、起動 API、実行基盤固有の並列化手段は正規契約へ固定しないこと |
| REQ-034-037 | case-auto は case-open → case-ready → case-run → case-close を自動継続し、req-define で再合意済みの Definition 変更がある場合は case-revise → case-ready → case-run → case-close を自動継続すること |
| REQ-034-038 | case-auto は新しい意味判断が必要となった場合 blocked とし resume_command: req-define で停止すること。req-define の壁打ちを自動化しないこと |
| REQ-034-039 | case-auto は GitHub Issue 入力時、Root Case の状態と resume_command から通常経路と例外経路を解決すること。resume_command が case-revise を指す場合、再合意済み Definition 変更の例外経路として case-revise → case-ready を駆動すること |

## 適用範囲

- 対象: case-auto（標準実行コマンド、入力解決、artifact_actions 工程決定、自走対象と対象外、構成工程委譲と case-run インライン実行、operation_unit キュー管理、execution_unit 並列実行、Wave 間直列、blocked 部分停止、永続状態進行と再開、クリーンアップ検証ゲート、停止理由分類と再開コマンド報告、壁時計時間計測、orchestration stage モデル、4状態区別報告、background task 回復、コンフリクトエスカレーション、bounded parent decision resolution）
- 対象外: 標準経路の置換（標準実行コマンドは case-auto に集約）、マクロフェーズ構成（REQ-005）、Epic/Wave 実行モデルの実行規則（Epic と Wave 実行モデル REQ）、同時起動数・再実行回数上限の数値（Design）、エージェント起動機構（harness 責務、REQ-011-018）
