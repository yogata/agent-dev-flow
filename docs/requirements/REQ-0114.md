---
id: REQ-0114
title: "/agentdev/case-auto 最大自走モード"
created: "2026-06-05"
updated: "2026-06-08"
tags: [command, orchestration, case-auto]
---

## 目的

AgentDevFlow の標準ワークフロー（req-define → req-save → case-open → case-run → case-close）を維持しつつ、ユーザーが明示した場合に req-define 完了後の後続工程を最大限自走させる入口を提供する。

repo に閉じる変更に限り、エージェントが自律的に実装・検証・PR作成・merge・close まで進める。DB migration 実行、deploy、cloud apply 等、repo 外の実体を変更する操作は自走対象外とする。

## 要件

| ID | 要件 |
|---|---|
| REQ-0114-001 | `/agentdev/case-auto` コマンド定義を `.opencode/commands/agentdev/case-auto.md`（runtime projection; sourceは`src/opencode/commands/agentdev/case-auto.md`）に追加すること（SHALL） |
| REQ-0114-002 | 入力解決は以下の優先順位で行うこと（SHALL）: (1) 明示パス指定 (2) `.sisyphus/drafts/req-draft-*.md` 単一自動検出 (3) セッション内要件doc (4) 特定不可時は停止 |
| REQ-0114-003 | 明示パス指定ファイルが存在しない場合、停止しエラーを報告すること（SHALL） |
| REQ-0114-004 | `.sisyphus/drafts/req-draft-*.md` が2件以上存在する場合、候補一覧を表示して停止すること（SHALL） |
| REQ-0114-005 | 入力を特定できない場合、停止し req-define の実行または要件docパス指定を求めること（SHALL） |
| REQ-0114-006 | 既存コマンド定義（req-save / case-open / case-run / case-close）を authoritative source として読み込み、各コマンドの Steps / Guardrails / Error handling に従って順次実行すること（SHALL） |
| REQ-0114-007 | 既存コマンドの手順を case-auto 定義内に再実装しないこと（SHALL） |
| REQ-0114-008 | work_type が feature の場合、req-save 相当 → case-open 相当 → case-run 相当 → case-close 相当の順で実行すること（SHALL） |
| REQ-0114-009 | work_type が bugfix / maintenance / docs_chore の場合、req-save をスキップし case-open 相当 → case-run 相当 → case-close 相当の順で実行すること（SHALL） |
| REQ-0114-010 | work_type は入力要件docの draft-meta セクションから読み取ること（SHALL） |
| REQ-0114-011 | 自走対象を repo にファイルとして残る変更に限定すること（SHALL） |
| REQ-0114-012 | 以下を自走対象外とすること（SHALL）: DB migration 実行、deploy/apply、クラウドリソース操作、外部SaaS設定変更、課金・権限・認証情報に関わる変更、repo 外の実データ操作、通知送信 |
| REQ-0114-013 | migration ファイル・IaC ファイルの作成・修正は自走対象とし、migration 実行・IaC apply は自走対象外とすること（SHALL） |
| REQ-0114-014 | GitHub Issue / PR / comment / merge / close は自走対象とすること（SHALL） |
| REQ-0114-015 | remote branch 削除は、当該 case-auto / case-run が作成した branch に限定すること（SHALL） |
| REQ-0114-016 | 以下の停止条件を検出した場合、実行を停止し停止理由・現在地点・再開可能な次コマンドを報告すること（SHALL）: (1) req-define 合意要件からの逸脱 (2) 要件未合意の scope 拡大 (3) repo 外実体変更の必要性 (4) DB migration 実行の必要性 (5) deploy/apply の必要性 (6) 認証・秘密・権限変更の必要性 (7) CI/test/lint 失敗が self-healing 不能 (8) merge conflict / remote hash 不一致 (9) 作成元不明 branch / user-owned branch / 他作業 branch の削除検出 (10) 未コミット変更の帰属不明 |
| REQ-0114-017 | 標準ワークフローを置き換えないこと。case-auto は明示指定時のみ使用する追加入口であることを明記すること（SHALL） |
| REQ-0114-018 | 既存の req-save / case-open / case-run / case-close の責務を変更しないこと（SHALL） |
| REQ-0114-019 | 工程固有の詳細手順と case-auto 定義が矛盾する場合、工程固有処理は既存コマンド定義を優先し、自走境界・入力解決・工程間制御は case-auto 定義を優先すること（SHALL） |
| REQ-0114-020 | case-auto は各工程の入力解決、工程分岐、工程間の状態引き継ぎ、停止時の現在地点・再開コマンド報告を担うこと（SHALL） |
| REQ-0114-021 | 自走対象に docs / REQ / ADR / SPEC / command reference / guide の更新を含めること（SHALL） |
| REQ-0114-022 | 以下のドキュメント整備を行うこと（SHALL）: `.opencode/commands/agentdev/case-auto.md`（runtime projection; sourceは`src/opencode/`配下）新規追加、`.opencode/commands/agentdev/README.md` への追加、`README.md` 入口表への追加、`docs/guides/command-selection.md` への追加、`docs/guides/req-case-flow.md` への最大自走モード追加 |
| REQ-0114-023 | case-auto は要件doc の Requirement Source セクションを case-open 相当処理の完了まで保持すること（SHALL） |
| REQ-0114-024 | case-open 相当処理において、case-open.md Step 13a と同一条件で RU ファイルを削除すること（SHALL） |
| REQ-0114-025 | Issue 作成または VERIFY に失敗した場合、RU ファイルを残置し完了扱いにしないこと（SHALL） |
| REQ-0114-026 | RU パターン（`.agentdev/backlog/req-units/RU-*.md`）に一致しない Requirement Source は削除しないこと（SHALL） |
| REQ-0114-027 | RU ファイル削除後に同期確認を行うこと（SHALL）: (1) main 作業ディレクトリの HEAD と `origin/main` が一致していること (2) `git status --porcelain` に削除済み RU ファイルが残っていないこと |
| REQ-0114-028 | 同期確認に失敗した場合、対象ファイル・現在の HEAD・`origin/main` を表示して完了扱いにせず停止すること（SHALL） |
| REQ-0114-029 | case-close 相当処理において、`agentdev-learning-capture` スキルに従い learning capture を実行すること（SHALL） |
| REQ-0114-030 | case-close 相当処理において、Post-run intake capture を実行すること（SHALL）: PR本文の `## Findings / Intake候補` セクションから回収し `.agentdev/intake/inbox/` に保存 |
| REQ-0114-031 | learning と intake を別々の成果物として扱い、混合した単一成果物にしないこと（SHALL） |
| REQ-0114-032 | learning/intake を `.agentdev/` 配下に commit/push すること（SHALL）: learning と intake を同一 commit に含める |
| REQ-0114-033 | `.agentdev/` push 失敗時は完了扱いにしないこと（SHALL）: 失敗内容を報告して停止 |
| REQ-0114-034 | `docs/specs/workflow-contracts.md` の case-auto intake 欄を `WRITE（capture）` に修正すること（SHALL） |
| REQ-0114-035 | case-auto の参照フローが case-close 相当処理と矛盾しない内容であること（SHALL） |
| REQ-0114-036 | `case-auto` は複数REQ docまたは `scale: large` の場合、REQ-0104-029~033 と同じIssue構造規則を使うこと（SHALL）。case-auto独自のIssue階層判定を持たないこと |
| REQ-0114-037 | `case-auto` は req-save相当処理から case-open相当処理への状態引き継ぎにおいて、複数REQ docの保存結果をそのまま引き継ぐこと（SHALL）。REQ操作の選別・再判断を行わないこと |
| REQ-0114-038 | `case-auto` は `case-open` 相当処理の完了後、Epic Issue 番号を case 全体の制御単位として保持すること（SHALL） |
| REQ-0114-039 | `case-auto` は後続工程で、Epic Issue から以下を読み取ること（SHALL）: (1) Epic Issue 本文の Wave 構成 (2) Epic Issue 本文の子Issue一覧 (3) Epic Issue コメント上の合意済み実行方針 (4) 子Issueの open / closed 状態 (5) 関連PRの open / merged 状態 (6) PR本文に記載された close 対象Issue |
| REQ-0114-040 | Epic Issue 上に合意済み方針が読み取れる場合、`case-auto` は同じ判断をユーザーに再確認しないこと（SHALL） |
| REQ-0114-041 | case-auto が case-open 相当処理を実行する場合、後続の case-auto / case-run / case-close 相当処理が読み取れるよう、Epic Issue に以下の実行制御情報を構造化して残すこと（SHALL）: Wave順序、Waveごとの対象子Issue、PR集約方針、branch方針、検証専用Waveの扱い、case-close到達条件 |
| REQ-0114-042 | `case-auto` は Epic Issue の Wave 構成を読み取り、Wave順序と子Issue状態に基づいて次工程を判断すること（SHALL） |
| REQ-0114-043 | `case-auto` は `case-close` 相当処理に到達する前に、関連PRと子Issueの状態を確認すること（SHALL） |

## 適用範囲

- **対象**:
  - `/agentdev/case-auto` コマンド定義の新規追加
  - 入力解決（3段階優先順位）
  - 実行モデル（authoritative source reference による orchestration）
  - work_type に基づく工程分岐（feature / bugfix / maintenance / docs_chore）
  - 自走対象・自走対象外の判定ルール
  - 停止条件と停止時報告
  - Epic Issue を制御単位として保持・読み取る振る舞い
  - case-open 相当処理が Epic Issue に残す実行制御情報
  - ドキュメント整備（README / command reference / guides）
- **対象外**:
  - 既存の req-save / case-open / case-run / case-close の責務変更
  - req-save への --auto 等パラメータ追加
  - case-run への --auto-close 等パラメータ追加
  - 標準ワークフローの置換
  - req-define / 要件深掘りの自走化
  - repo 外の実体変更の自走化
  - DB migration 実行・deploy・cloud apply の自走化
  - 進行状態の永続化（停止時は再開可能コマンドを報告し、ユーザーが個別コマンドで再開）

## 関連ドキュメント更新候補

### 直接矛盾（要更新）

- `docs/specs/workflow-contracts.md` — case-auto intake 欄が `—`（未処理）となっており `WRITE（capture）` への修正が必要（REQ-0114-034）

### 更新候補（影響確認必要）

- `docs/requirements/REQ-0101.md` — コマンド体系・入口表への case-auto 追加に伴う更新候補
- `docs/requirements/REQ-0103.md` — orchestration command の責務境界に関する追記候補
- `docs/requirements/REQ-0106.md` — case 実行・完了処理における case-auto との関係に関する追記候補
- `docs/specs/workflow-contracts.md` — case-auto の自走境界・停止条件に関する追記候補
- `docs/guides/artifacts-and-state.md` — case-auto 成果物に learning/intake を含むか確認（REQ-0114-029〜033）
- `docs/specs/system.md` — コマンド一覧への case-auto 追加候補
- `docs/DOC-MAP.md` — 新規コマンドに伴う索引更新候補
- `docs/guides/artifacts-and-state.md` — case-auto の成果物に関する追記候補

### 影響なし

- `docs/requirements/REQ-0108.md` — integrity 検証は case-auto 定義の検証対象外ではない
- `docs/requirements/REQ-0112.md` — work_type 分類は既存定義を利用

## Update Notes

| 日付 | 内容 |
|---|---|
| 2026-06-07 | RU-01対応: case-autoの複数REQ doc時Issue構造規則（REQ-0104-029~033準拠）と状態引き継ぎ仕様を追加(036-037) |
| 2026-06-08 | RU-01追加: case-auto が Epic Issue を制御単位として保持・読み取る要件、case-open 相当処理の Epic Issue 構造化情報残存要件を追加(038-043) |
