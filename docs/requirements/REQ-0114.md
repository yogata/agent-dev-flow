---
id: REQ-0114
title: "/agentdev/case-auto 最大自走モード"
created: "2026-06-05"
updated: "2026-06-15"
---

## 目的

AgentDevFlow の標準ワークフロー（req-define → req-save → case-open → case-run → case-close）を維持しつつ、ユーザーが明示した場合に req-define 完了後の後続工程を最大限自走させる入口を提供する。

repo に閉じる変更に限り、エージェントが自律的に実装・検証・PR作成・merge・close まで進める。DB migration 実行、deploy、cloud apply 等、repo 外の実体を変更する操作は自走対象外とする。

## 要件

| ID | 要件 |
|---|---|
| REQ-0114-001 | `/agentdev/case-auto` コマンド定義を `.opencode/commands/agentdev/case-auto.md`（runtime projection; sourceは`src/opencode/commands/agentdev/case-auto.md`）に追加すること |
| REQ-0114-002 | 入力解決は以下の優先順位で行うこと: (1) 明示パス指定 (2) `.agentdev/drafts/req-draft-*.md` 単一自動検出 (3) セッション内要件doc (4) 特定不可時は停止 |
| REQ-0114-003 | 明示パス指定ファイルが存在しない場合、停止しエラーを報告すること |
| REQ-0114-004 | `.agentdev/drafts/req-draft-*.md` が2件以上存在する場合、候補一覧を表示して停止すること |
| REQ-0114-005 | 入力を特定できない場合、停止し req-define の実行または要件docパス指定を求めること |
| REQ-0114-006 | 既存コマンド定義（req-save / case-open / case-run / case-close）を authoritative source として読み込み、各コマンドの Steps / Guardrails / Error handling に従って順次実行すること |
| REQ-0114-007 | 既存コマンドの手順を case-auto 定義内に再実装しないこと |
| REQ-0114-008 | work_type が feature の場合、req-save 相当 → case-open 相当 → case-run 相当 → case-close 相当の順で実行すること |
| REQ-0114-009 | work_type が bugfix / maintenance / docs_chore の場合、req-save をスキップし case-open 相当 → case-run 相当 → case-close 相当の順で実行すること |
| REQ-0114-010 | work_type は入力要件docの draft-meta セクションから読み取ること |
| REQ-0114-011 | 自走対象を repo にファイルとして残る変更に限定すること |
| REQ-0114-012 | 以下を自走対象外とすること: DB migration 実行、deploy/apply、クラウドリソース操作、外部SaaS設定変更、課金・権限・認証情報に関わる変更、repo 外の実データ操作、通知送信 |
| REQ-0114-013 | migration ファイル・IaC ファイルの作成・修正は自走対象とし、migration 実行・IaC apply は自走対象外とすること |
| REQ-0114-014 | GitHub Issue / PR / comment / merge / close は自走対象とすること |
| REQ-0114-015 | remote branch 削除は、当該 case-auto / case-run が作成した branch に限定すること |
| REQ-0114-016 | 以下の停止条件を検出した場合、実行を停止し停止理由・現在地点・再開可能な次コマンドを報告すること: (1) req-define 合意要件からの逸脱 (2) 要件未合意の scope 拡大 (3) repo 外実体変更の必要性 (4) DB migration 実行の必要性 (5) deploy/apply の必要性 (6) 認証・秘密・権限変更の必要性 (7) CI/test/lint 失敗が self-healing 不能 (8) merge conflict / remote hash 不一致 (9) 作成元不明 branch / user-owned branch / 他作業 branch の削除検出 (10) 未コミット変更の帰属不明 |
| REQ-0114-017 | 標準ワークフローを置き換えないこと。case-auto は明示指定時のみ使用する追加入口であることを明記すること |
| REQ-0114-018 | 既存の req-save / case-open / case-run / case-close の責務を変更しないこと |
| REQ-0114-019 | 工程固有の詳細手順と case-auto 定義が矛盾する場合、工程固有処理は既存コマンド定義を優先し、自走境界・入力解決・工程間制御は case-auto 定義を優先すること |
| REQ-0114-020 | case-auto は各工程の入力解決、工程分岐、工程間の状態引き継ぎ、停止時の現在地点・再開コマンド報告を担うこと |
| REQ-0114-021 | 自走対象に docs / REQ / ADR / SPEC / command reference / guide の更新を含めること |
| REQ-0114-022 | 以下のドキュメント整備を行うこと: `.opencode/commands/agentdev/case-auto.md`（runtime projection; sourceは`src/opencode/`配下）新規追加、`.opencode/commands/agentdev/README.md` への追加、`README.md` 入口表への追加、`docs/guides/command-selection.md` への追加、`docs/guides/req-case-flow.md` への最大自走モード追加 |
| REQ-0114-023 | case-auto は要件doc の Requirement Source セクションを case-open 相当処理の完了まで保持すること |
| REQ-0114-024 | case-open 相当処理において、case-open.md Step 13a と同一条件で RU ファイルを削除すること |
| REQ-0114-025 | Issue 作成または VERIFY に失敗した場合、RU ファイルを残置し完了扱いにしないこと |
| REQ-0114-026 | RU パターン（`.agentdev/backlog/req-units/RU-*.md`）に一致しない Requirement Source は削除しないこと |
| REQ-0114-027 | RU ファイル削除後に同期確認を行うこと: (1) main 作業ディレクトリの HEAD と `origin/main` が一致していること (2) `git status --porcelain` に削除済み RU ファイルが残っていないこと |
| REQ-0114-028 | 同期確認に失敗した場合、対象ファイル・現在の HEAD・`origin/main` を表示して完了扱いにせず停止すること |
| REQ-0114-029 | case-close 相当処理において、`agentdev-learning-capture` スキルに従い learning capture を実行すること |
| REQ-0114-030 | case-close 相当処理において、Post-run intake capture を実行すること: PR本文の `## Findings / Capture候補` セクションから回収し `.agentdev/intake/inbox/` に保存 |
| REQ-0114-031 | learning と intake を別々の成果物として扱い、混合した単一成果物にしないこと |
| REQ-0114-032 | learning/intake を `.agentdev/` 配下に commit/push すること: learning と intake を同一 commit に含める |
| REQ-0114-033 | `.agentdev/` push 失敗時は完了扱いにしないこと: 失敗内容を報告して停止 |
| REQ-0114-034 | `docs/specs/workflow-contracts.md` の case-auto intake 欄を `WRITE（capture）` に修正すること |
| REQ-0114-035 | case-auto の参照フローが case-close 相当処理と矛盾しない内容であること |
| REQ-0114-036 | `case-auto` は case-open 相当処理において REQ-0104-035〜042 の OU / execution_groups ベースの Issue 構造規則に従うこと。case-auto独自のIssue階層判定を持たないこと |
| REQ-0114-037 | `case-auto` は req-save相当処理から case-open相当処理への状態引き継ぎにおいて、複数REQ docの保存結果をそのまま引き継ぐこと。REQ操作の選別・再判断を行わないこと |
| REQ-0114-038 | `case-auto` は `case-open` 相当処理の完了後、Epic Issue 番号を case 全体の制御単位として保持すること |
| REQ-0114-039 | `case-auto` は後続工程で、Epic Issue から以下を読み取ること: (1) Epic Issue 本文の Wave 構成 (2) Epic Issue 本文の子Issue一覧 (3) Epic Issue コメント上の合意済み実行方針 (4) 子Issueの open / closed 状態 (5) 関連PRの open / merged 状態 (6) PR本文に記載された close 対象Issue |
| REQ-0114-040 | Epic Issue 上に合意済み方針が読み取れる場合、`case-auto` は同じ判断をユーザーに再確認しないこと |
| REQ-0114-041 | case-auto が case-open 相当処理を実行する場合、後続の case-auto / case-run / case-close 相当処理が読み取れるよう、Epic Issue に以下の実行制御情報を構造化して残すこと: Wave順序、Waveごとの対象子Issue、PR集約方針、branch方針、検証専用Waveの扱い、case-close到達条件 |
| REQ-0114-042 | `case-auto` は Epic Issue の Wave 構成を読み取り、Wave順序と子Issue状態に基づいて次工程を判断すること |
| REQ-0114-043 | `case-auto` は `case-close` 相当処理に到達する前に、関連PRと子Issueの状態を確認すること |
| REQ-0114-044 | `case-auto` は case-open 相当処理の出力から Epic Issue を検出した場合、draft の OU 構造に基づいて OU queue を制御すること。単一 Standard flow Issue の場合はキュー処理を適用せず、既存の単一工程フローに従うこと |
| REQ-0114-045 | `case-auto` は Epic Issue 検出時、case-run 相当処理に OU result の Issue 番号を渡すこと。case-run の Epic Orchestrator モードが Wave ベースの子Issue実行を処理するため、case-auto は Wave スケジューリング・依存関係分析を実装してはならない |
| REQ-0114-046 | `case-auto` は case-run 相当処理の完了後、Epic Issue の子Issue一覧から各子Issue の完了状態を確認し、case-run が正常完了した子Issue に対して case-close 相当処理を順次実行すること。子Issue の完了判定は case-run Epic Orchestrator の出力（成功/失敗/スキップ）に基づくこと |
| REQ-0114-047 | `case-auto` は case-run が部分失敗を報告した場合、正常完了した子Issue のみに対して case-close 相当処理を実行し、失敗・スキップされた子Issue の case-close を試みてはならない。部分完了の報告には成功・失敗・スキップそれぞれの子Issue一覧を含めること |
| REQ-0114-048 | `case-auto` は未完了の子Issue が残存する状態で全体完了を報告してはならない。全子Issueの case-close 相当処理が完了（成功または停止条件による中断）、かつ Epic Issue の状態が更新された場合のみ全体完了とすること |
| REQ-0114-049 | `case-auto` は停止条件（REQ-0114-016）の検出時または部分失敗時、以下を報告すること: (1) 完了した子Issue一覧（Issue番号・PR番号） (2) 進行中の子Issue一覧（ある場合） (3) 未実行の子Issue一覧 (4) 再開に必要な次コマンド |
| REQ-0114-050 | `case-auto` は case-close 相当処理を各子Issue ごとに独立して実行すること。各子Issue の learning/intake capture、`.agentdev/` commit/push は既存の case-close 定義に従い per-child で実行する。キュー全体での一括 capture は行わない |
| REQ-0114-051 | case-auto は OU queue の管理・制御のみを担い、OU 本文の抽出・変換・REQ 操作解釈を行わないこと |
| REQ-0114-052 | case-auto は req-save / case-open に draft path と OU ID のみを渡すこと。OU 本文の切り出しは行わない |
| REQ-0114-053 | case-auto は 1 OU を close まで完了した後に次の OU へ進むこと |
| REQ-0114-054 | case-auto は OU の統合・分割、REQ 操作分類、Issue 階層判定を再評価しないこと |
| REQ-0114-055 | OU 間依存は queue dependency として扱い、依存関係があるだけでは Epic Issue 化しないこと |
| REQ-0114-056 | case-auto は停止時に完了済み OU、進行中 OU、未実行 OU、再開可能な次コマンドを報告すること |
| REQ-0114-057 | case-auto は Epic Issue 化の判定に関与しないこと。case-open の判定結果に従うこと |
| REQ-0114-058 | case-auto は draft を OU 情報の SSoT として扱い、独自の OU 状態管理を持たないこと |
| REQ-0114-059 | case-auto の Epic Issue 分岐ロジックで "case-open 相当処理" に共通終了処理（コメント追加・ドラフト削除・RU削除・完了報告）が含まれること |
| REQ-0114-060 | case-auto に Epic Issue Step 4-1 キュー処理開始前のクリーンアップ検証ゲートがあること |
| REQ-0114-061 | クリーンアップ検証ゲートがドラフトファイル（`.agentdev/drafts/req-draft-*.md`）の残存を検出した場合に停止すること |
| REQ-0114-062 | クリーンアップ検証ゲートが RU ファイル（`.agentdev/backlog/req-units/RU-*.md`）の残存を検出した場合に停止すること |
| REQ-0114-063 | クリーンアップ検証ゲートの実行結果が case-auto 完了報告（Step 8）に含まれること |

## 適用範囲

- **対象**:
  - `/agentdev/case-auto` コマンド定義の新規追加
  - 入力解決（3段階優先順位）
  - 実行モデル（authoritative source reference による orchestration）
  - work_type に基づく工程分岐（feature / bugfix / maintenance / docs_chore）
  - 自走対象・自走対象外の判定ルール
  - 停止条件と停止時報告
  - Epic Issue を制御単位として保持・読み取る振る舞い
  - case-auto Epic flow クリーンアップ検証ゲート
  - case-open 相当処理の Epic Issue に残す実行制御情報
  - ドキュメント整備（README / command reference / guides）
- **対象外**:
  - 既存の req-save / case-open / case-run / case-close の責務変更
  - req-save への --auto 等パラメータ追加
  - case-run への --auto-close 等パラメータ追加
  - 標準ワークフローの置換
  - req-define / 要件深掘りの自走化
  - repo 外の実体変更の自走化
  - DB migration 実行・deploy・cloud apply の自走化
  - 進行状態の永続化（停止時は再開可能コマンドを報告し、ユーザーが個別コマンドで再開）

## Update Notes

| 日付 | 内容 |
|---|---|
| 2026-06-07 | case-autoの複数REQ doc時Issue構造規則（REQ-0104-029~033準拠）と状態引き継ぎ仕様を追加(036-037) |
| 2026-06-08 | case-auto が Epic Issue を制御単位として保持・読み取る要件、case-open 相当処理の Epic Issue 構造化情報残存要件を追加(038-043) |
| 2026-06-08 | case-auto の複数操作単位継続実行キュー要件を追加(044-050) |
| 2026-06-13 | REQ-0114-044,045 | UPDATE: OU queue モデルへの更新（draft SSoT・OU ID 渡し） |
| 2026-06-13 | REQ-0114-051-058 | APPEND: case-auto OU queue 処理モデル（逐次制御・draft SSoT・停止時 OU 状態報告） |
| 2026-06-15 | REQ-0114-059-063 | APPEND: case-auto Epic flow 共通終了処理の確実実行・クリーンアップ検証ゲート（RU-20260615-03 / OU-3） |
