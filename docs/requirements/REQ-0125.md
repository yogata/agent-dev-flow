---
id: REQ-0125
title: "inspect-skills / Command/Skill参照妥当性検出"
created: "2026-06-16"
updated: "2026-06-17"
---

## 目的

AgentDevFlow の command と skill は runtime で疎結合に協調するため、Command→Skill 参照、Skill frontmatter、Skill 本文構造、references 利用、template / script 参照に過不足や妥当性の逸脱が生じ得る。`/agentdev/inspect-skills` はこれらを read-only で診断し、finding として報告することで、command / skill 間の参照妥当性を維持する。診断は修正を行わず、推奨 route の提示と finding file の生成に限定する。

## 要件

| ID | 要件 |
|---|---|
| REQ-0125-001 | `/agentdev/inspect-skills` は public agentdev command として存在し、read-only 診断 command であること |
| REQ-0125-002 | 診断対象は Command 定義ファイル群、Skill 定義ファイル群、および必要に応じて関連する template / reference / script ファイル群であること |
| REQ-0125-003 | 診断観点は Command→Skill 参照妥当性、Skill frontmatter 整合、本文構造、references 利用、template / script 参照、粒度、段階的開示、責務境界、canonical name、内部構造依存を含むこと |
| REQ-0125-004 | 診断の詳細観点と判定基準は `agentdev-inspect-skills` skill に集約し、command 定義は薄い入口として skill への参照を持つこと |
| REQ-0125-005 | 出力は診断レポート（セッション内テキスト）、finding リスト（対象、観点、分類、根拠、推奨 route）、`.agentdev/inspect/inbox/inspect-skills-finding-{topic}.md`（finding file）に限定すること |
| REQ-0125-006 | finding file の出力先は `.agentdev/inspect/inbox/` 配下に限定すること |
| REQ-0125-007 | 許可される side effect は `.agentdev/inspect/inbox/inspect-skills-finding-*.md` の生成および `.agentdev/inspect/` 配下の git 永続化（commit / push）のみに限定すること（canonical docs 変更・REQ/ADR/SPEC 変更・Command/Skill/Template/Script 変更・Issue 作成・PR 作成・RU 保存・branch / worktree 操作は対象外） |
| REQ-0125-008 | finding に対する推奨 route の提示のみを行うこと（修正の実行は対象外） |
| REQ-0125-009 | `inspect-skills` は `.agentdev/inspect/` 配下の変更を git commit / push すること。変更なし時は commit/push せず完了報告で「変更なし」と報告すること |
| REQ-0125-010 | `inspect-skills` の git pull は `--ff-only` で実行すること |
| REQ-0125-011 | `inspect-skills` の commit 対象は `.agentdev/inspect/` 配下のみとし、他のパスを巻き込まないこと |
| REQ-0125-012 | `inspect-skills` の commit message は `chore(agentdev): capture inspect-skills finding` の形式であること |
| REQ-0125-013 | `inspect-skills` の push 失敗時は共通 template（`templates/common/git-error-messages.md`）の「Git Push エラー」形式で停止し、完了扱いにしないこと。pull 失敗時は同 template の「Git 同期エラー（pull --ff-only 失敗）」形式で停止すること |
| REQ-0125-014 | `inspect-skills` の完了報告に git 永続化結果（変更有無・ファイル一覧・commit hash・push 成否）を含めること |

## 適用範囲

- **対象**: `/agentdev/inspect-skills` コマンドの検出（Command→Skill 参照妥当性、Skill 構造、read-only 診断、finding 出力、推奨 route 提示）
- **対象外**: inspect-docs（REQ-0109）、inspect-promote（REQ-0126）、docs-check（REQ-0108）、個別 finding の修正実行、機械的整合性検査（docs-check の担当）

## 関連情報

- **Related**: REQ-0108（docs-check / Validation / Tests）、REQ-0109（inspect-docs / REQ体系整合性）、REQ-0124（AgentDevFlow inspect-* 検出コマンド群と inspect lifecycle）、REQ-0126（inspect-promote / 検出finding分類・昇格）

## Requirement Source

該当なし

## Update Notes

| 日付 | 対象 | 変更内容 |
|------|------|----------|
| 2026-06-16 | REQ-0125 全体 | CREATE: inspect-skills command-level REQ 新規作成（agentdev-system-reorganization OU-05 / #833） |
| 2026-06-17 | REQ-0125-007 | UPDATE: side-effect 定義を再定義（commit/push 禁止 → .agentdev/inspect/ 配下の git 永続化のみ許可）（RU-20260617-09） |
| 2026-06-17 | REQ-0125-009〜014 | APPEND: inspect-skills git 永続化要件（commit/push・pull --ff-only・commit scope・commit message・error handling・完了報告 git 永続化結果）（RU-20260617-09） |
| 2026-06-17 | REQ-0125-006,007,008 | UPDATE: 否定文主文を肯定文へ書き換え（positive-form-documents OU-8・REQ-0101-064〜066 適用） |
