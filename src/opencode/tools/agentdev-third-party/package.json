{
  "name": "agentdev-third-party-tool",
  "version": "0.1.0",
  "private": true,
  "description": "agentdev_third_party Custom Tool. Structured third-party Skill acquisition with an operation contract (input, output, guarantee, failure), non-destructive staging placement, and a fail-closed verification gate.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
