{
  "name": "agentdev-jev-tool",
  "version": "0.1.0",
  "private": true,
  "description": "agentdev-jev Custom Tool. Jev prior evaluation with a provider/SDK-independent public contract (per-question results, probability distributions, normalized confidence, structured failure classification, no auto-retry) plus observation write support (one Workflow run = one JSON under .agentdev/jev-observations/).",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
