{
  "name": "agentdev-model-escalation",
  "version": "0.1.0",
  "private": true,
  "description": "agentdev-model-escalation Plugin. Escalates the session model at turn boundaries while keeping the session identity (sessionID and conversation history) intact, and reverts to the pre-escalation model on request. Reads .agentdev/agentdev.jsonc (modelEscalation). Inactive when the config is absent or invalid.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
