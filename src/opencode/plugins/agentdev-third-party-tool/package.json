{
  "name": "agentdev-third-party-tool-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Registration wiring for the agentdev_third_party Custom Tool (third-party Skill acquisition). The tool owns the operation contract, non-destructive placement, and the fail-closed verification gate.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
