{
  "name": "agentdev-textlint-guard",
  "version": "0.1.0",
  "private": true,
  "description": "agentdev-textlint-guard Plugin. Common textlint execution core (project resolution, config loading, target resolution, rule composition, lint execution, result formatting) with a pre-write tool.execute.before hook (write/edit/apply_patch) and a standalone final-gate entry. Dependencies are resolved before distribution and shipped as a vendored engine bundle (no network at install).",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:engine": "bun run build/build-engine.ts"
  },
  "dependencies": {
    "@textlint/kernel": "^14.4.2",
    "@textlint/textlint-plugin-markdown": "^14.4.2",
    "@textlint-ja/textlint-rule-preset-ai-writing": "^1.1.2",
    "textlint-rule-preset-ja-technical-writing": "^12.0.1",
    "textlint-rule-prh": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
