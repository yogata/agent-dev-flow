{
  "name": "agentdev-workflow-case-open-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic cross-dependency inspection engine shared by case-open STEP-5 and the case-ready verification gate: mechanical comparison of agreed declarations (artifact paths, target requirement rows, shared-area registration state). Pure functions, argv input, stdout JSON output.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
