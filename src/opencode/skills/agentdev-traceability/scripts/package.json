{
  "name": "agentdev-traceability-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic traceability engine: coverage, impact, check over ADF-COVERS declarations by direct scanning of canonical artifacts. Pure functions, argv input, stdout JSON output.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
