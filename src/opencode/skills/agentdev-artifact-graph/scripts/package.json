{
  "name": "agentdev-artifact-graph-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic Artifact Graph engine: build, check, query, prepare, verify (REQ-{NNNN}, DEC-{N}). Pure functions, argv input, stdout JSON output.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "effectiveness": "bun effectiveness/run.ts",
    "effectiveness:json": "bun effectiveness/run.ts --json",
    "candidate-limit": "bun effectiveness/candidate_limit/run.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
