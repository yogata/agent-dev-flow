{
  "name": "agentdev-artifact-graph-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic Artifact Graph engine: build, check, query, prepare, verify (REQ-012, ADR-007). Pure functions, argv input, stdout JSON output.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
