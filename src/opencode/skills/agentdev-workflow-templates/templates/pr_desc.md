---
name: Pull Request Description
about: Pull Request作成時の本文テンプレート
---

# PR本文テンプレート

Pull Request作成時の本文テンプレート。

## 概要
<!-- 【必須】 -->

[Issueの要約]

## 実行識別情報
<!-- 【必須】 -->

<!-- 実行識別情報: workflow-contracts Design「ADF 実行識別情報の記録契約」に基づく構造化識別情報セクション。
機械的解析は本セクション内の adf_ 接頭辞付き key-value 行を正とし、自由文中に偶然出現する ID に依存しない。
adf_delegation は委譲 prompt の委譲識別情報ブロックから転記し、委譲実行と PR の対応付けの正規手段とする。
harness 側識別子は取得可能な場合の付加情報に限定し、必須契約としない。
識別情報の一部が取得不能な場合は「N/A」と記録し、workflow を停止しない。
本セクションは新規作成 PR のみに適用し、既存 PR への遡及適用は行わない -->
- adf_case: （対象 Case の Issue 番号。#N 形式）
- adf_execution_unit: （実行単位の flow 種別。standard または epic。対象 Issue 番号は「関連Issue」セクションの Refs 行を正として導出する）
- adf_delegation: （委譲単位識別子（DEL-{N}-{seq} 形式。N = Issue 番号、seq = 同一 Issue への委譲連番）。委譲 prompt の委譲識別情報ブロックから転記する）
- adf_harness_ref: （任意。harness 側識別子（OpenCode session ID 等）。取得可能な場合のみ記載し、省略できる）

## 実装内容
<!-- 【必須】 -->

[実装内容の概要]

## verify-only 根拠
<!-- 【任意】 verify-only PR（実装差分0件、検証のみで完了する PR）の場合のみ記入する。
通常実装 PR では本セクションを省略する。
verify-only PR の場合、「実装内容」欄は空欄にせず「実装差分なし」と理由を記録し、本セクションで詳細根拠を示す。
根拠は姉妹実装 PR、実装 PR、先行 commit、main 反映済み commit、既存成果物、検証のみで完結する理由のいずれかを許容する。
case-close と QG-{N} は当該欄を完了条件の証拠ソースとして読む（[case-run.md](../../../../../case-run Design)「verify-only 根拠欄の記入規則」、[case-close.md](../../../../../case-close Design)「verification-only PR の files_checked 空確認」参照）。
-->

- **種別**: verify-only
- **実装差分を含まない理由**: [姉妹実装 PR で実装済み / 先行 commit で反映済み / main 反映済み commit / 既存成果物が要件を満たす / 検証のみで完結]
- **根拠成果物または commit**: [姉妹 PR #N / commit SHA / 対象ファイルパス 等]
- **検証対象**: [検証した対象（仕様、既存実装、文書 等）]
- **検証結果**: [検証手順と結果。受け入れ基準が検証のみで充足されたことの evidence]

## 完了条件
<!-- 【必須】 -->

<!-- 契約テスト期待値確認: 構造変更（command、skill、template の構造様式変更）を伴う PR は、当該構造を固定する契約テストの期待値更新と、変更ファイルを参照する `*.test.ts` の固定トークン確認（grep）を実施し、その有無を下記チェックボックスへ記録する。構造変更なしの場合は「該当なし」と記録する -->
- [ ] {completion_criteria}
- [ ] 契約テスト期待値の更新と固定トークン確認: [実施済み（期待値更新対象: 記載）/ 該当なし（構造変更なし）]

## テスト結果
<!-- 【必須】 -->

<!-- テスト実行形態の記録: テスト実行の証拠として実行 cwd と起動コマンド形式（./ prefix・対象ディレクトリのパス指定を含む）を記録する。テスト実行が該当なしの場合は併せて「該当なし」とする -->

[テスト結果の概要]

- **実行 cwd**: [テスト実行時のカレントディレクトリ（リポジトリルート、worktree root 等）]
- **起動コマンド形式**: [起動コマンド（./ prefix・対象ディレクトリのパス指定を含む形式）]

## 品質メトリクス
<!-- 【必須】 -->

| メトリクス | 結果 | 基準 | 判定 |
|---|---|---|---|
| {メトリクス名} | {値} | {基準値} | ✅/❌ |

## 検証差分
<!-- 【必須】 -->

<!-- 検証差分: 検証種別、検証結果、前段階からの finding 差分を工程ごとに記録する構造化検証記録セクション。
記録先割当（PR 本文: 検証種別と検証結果）は workflow-contracts Design「ADF 実行識別情報の記録契約」に基づき、
形式は agentdev-workflow-templates の検証差分セクション規約に従う。
1行に1検証を記録し、実行工程には検証を実施した ADF 工程（case-run、case-close、レビュー等）を記録する。
finding 差分は 新規 / 修正済み / 既出 / 撤回 / 無効 の5分類で区別して記録する（撤回と無効は「撤回または無効となった finding」の内訳）。
前段階の同種検証が存在しない初回検証では、検出された全 finding を新規として記録する。
同種検証を複数工程で実施した場合は工程ごとに行を並べ、後続検証の追加価値を工程間で読み比べられるようにする。
finding の特定は要約と参照（セクション名、コメント番号等）で行い、各欄に該当する finding がない場合は「該当なし」と記載する。
検証で発見した intake / learning 候補は本セクションに記録せず `## Findings/ Capture候補` セクションへ記録する（共存、置換しない）。
本セクションは新規作成の PR にのみ適用し、既存 PR への遡及適用は行わない -->

| 実行工程 | 検証種別 | 検証結果 | 新規 | 修正済み | 既出 | 撤回 | 無効 |
|---|---|---|---|---|---|---|---|
| {実行工程} | {検証種別} | {検証結果} | {新規 finding の要約と参照。該当なしの場合は「該当なし」} | {修正済み finding の要約と参照。該当なしの場合は「該当なし」} | {既出 finding の要約と参照。該当なしの場合は「該当なし」} | {撤回となった finding の要約と参照。該当なしの場合は「該当なし」} | {無効となった finding の要約と参照。該当なしの場合は「該当なし」} |

## Findings/ Capture候補
<!-- 【必須】 -->

### intake

該当なし

### learning

該当なし

## Design確定候補
<!-- 【任意】 case-run/ driver が実装時に発見した Design レベルの詳細（Design に記載すべき schema、enum、判定表、内部アルゴリズム等）を記録する。
`## Findings / Capture候補` とは別セクション。
case-close STEP-3 で Design 確定チェックの入力となり、draft → 承認済み 昇格または design-save 再起動の判断材料となる。
Design確定候補がない場合はセクションごと省略する。
 -->

該当なし

## 決定事項
<!-- 【任意】 -->

- [PRで実現した決定事項のリンク（該当する場合）]

## 関連Issue
<!-- 【必須】 -->

Refs: #$ISSUE_NUMBER


