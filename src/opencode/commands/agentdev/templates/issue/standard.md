✅ issue 完了

完了コマンド:/agentdev/issue
操作種別: {操作種別}
対象: Issue #{N}（検索・参照操作時は対象範囲）
結果:
  - {操作結果の要約（起票、状態遷移、更新内容、検索結果等）}
検証結果: ✅ OK
git 永続化: 該当なし（GitHub 版はリポジトリ内ファイルを変更しない）
次のコマンド: なし（必要に応じて /agentdev/issue を継続して利用）
