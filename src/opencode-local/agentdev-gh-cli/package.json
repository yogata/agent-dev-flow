{
  "name": "agentdev-gh-cli-local-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Local implementation of the agentdev_gh Custom Tool operation contract (Case file I/O, same contract as the GitHub implementation, swappable via the local-mode projection).",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
